<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE sections SYSTEM "/dtd/book.dtd">

<!-- The content of this document is licensed under the CC-BY-SA license -->
<!-- See http://creativecommons.org/licenses/by-sa/1.0 -->

<!-- $Header: /var/cvsroot/gentoo/xml/htdocs/doc/tw/handbook/Attic/hb-install-ppc-bootloader.xml,v 1.5 2004/07/15 00:58:38 bennyc Exp $ -->

<!-- rev 1.4 by Benny -->

<sections>
<section>
<title>決定您的選擇</title>
<subsection>
<title>介紹</title>
<body>

<p>
現在您的核心已經設定和已經編譯好相關的系統設定檔了，是時候該安裝一個程式來驅動
開機時所需要的核心。這個程式叫做 <e>開機程式</e>。但是在您開始動作前，請仔細考
慮您可用的選項﹍
</p>

<p>
Linux/PPC 有多個開機程式。我們有 <uri link="#yaboot">yaBoot</uri>（適用於 New
World PPC 的機器）和 <uri link="#bootx">BootX</uri> （適用於 OldWord PPC 的機器
和 IBM 的機器）。Pegasos 不需要一個開機程式。您在這邊將無法使用 yaBoot 或是 
BootX。Pegasus 的使用者應該要從 <uri 
link="?part=1&amp;chap=10">安裝相關系統工具</uri> 繼續。
</p>

</body>
</subsection>
</section>
<section id="yaboot">
<title>使用 yaBoot</title>
<subsection>
<title>介紹</title>
<body>

<impo>
yaBoot 只適用於 NewWorld 的 PPC 和 IBM 的系統架構！
</impo>

<p>
設定 yaBoot 有兩個方法。您可以使用新的改善過的 <c>yabootconfig</c> 包含著
<path>yaboot-1.3.8-r1</path> 然後自動設定 yaboot。如果因為某種原因您無法執行
<c>yabootconfig</c> 自動設定 <path>/etc/yaboot.conf</path>，您可以編輯已經在您的
系統裡面的範例檔。如果您是在 G5 （<c>yabootconfig</c> 常常無法正確設定），您可以
依照已經安裝到系統上的範例檔直接設定。
</p>

<ul>
  <li><uri link="#yabootconfig">預設：使用 yabootconfig</uri></li>
  <li>
    <uri link="#manual_yaboot">其他選擇：手動 yaBoot 設定</uri>
  </li>
</ul>

</body>
</subsection>
<subsection id="yabootconfig">
<title>預設：使用 yabootconfig</title>
<body>

<p>
<c>yabootconfig</c> 將會自動偵測您的系統上的分割區然後設定 Linux, Mac OS 和 
Mac OS X 3的組合。
</p>

<p>
要使用 <c>yabootconfig</c>，您的磁碟一定要有一個 bootstrap 的分割區。
<path>/etc/fstab</path> 一定要依照您的 Linux 分割區設定。這兩個步驟一定要在少面
的步驟完成。要開始前，請確認您有最新版本的 yaboot 輸入 <c>emerge --update
yaboot</c>。這將會將 Portage 內的 yaboot 更新為最新，stage 檔案內的或許已經老就
。
</p>

<pre caption = "安裝 yaboot">
# <i>emerge --usepkg --update yaboot</i>
</pre>

<p>
現在執行 <c>yabootconfig</c>。這個程式將會執行然後確認 bootstrap 分割區。
如果正確，輸入 <c>Y</c>。如果錯誤，請檢查您的 <path>/etc/fstab</path>。
yabootconfig 將會掃描您的系統設定然後建立 <path>/etc/yaboot.conf</path>，然後
自動執行 <c>mkofboot</c>。<c>mkofboot</c> 是用來格式化您的 bootstrap 分割區，
然後將 yaboot 設定檔安裝到裡面。
</p>

<p>
您應該要確定 <path>/etc/yaboot.conf</path> 的內容。如果您更改 
<path>/etc/yaboot.conf</path> 的設定（像是更改預設開機），完成時確定要執行
<c>ybin -v</c> 套用相關更變。
</p>

<p>
接下來請到 <uri link="?part=1&amp;chap=10">安裝系統所需工具</uri>。
</p>

</body>
</subsection>
<subsection id="manual_yaboot">
<title>其他選擇：手動設定 yaBoot</title>
<body>

<p>
請先確認您有最新版本的 <c>yaboot</c> 安裝到您的系統：
</p>

<pre caption = "安裝 yaboot">
# <i>emerge --usepkg --update yaboot</i>
</pre>

<p>
以下您將可以看到完整的 <path>yaboot.conf</path> 檔案。G5 的使用者應該要注意您們
的 Serial ATA 磁碟，因為這將會被 Linux 核心判斷成 SCSI 磁碟（所以將
<path>/dev/hda</path> 換成 <path>/dev/sda</path>）。
</p>

<pre caption = "/etc/yaboot.conf">
<comment>## /etc/yaboot.conf
##
## run: "man yaboot.conf" for details. Do not make changes until you have!!
## see also: /usr/share/doc/yaboot/examples for example configurations.
##
## For a dual-boot menu, add one or more of:
## bsd=/dev/hdaX, macos=/dev/hdaY, macosx=/dev/hdaZ

## our bootstrap partition:</comment>

boot=/dev/hda9

<comment>##hd: is open firmware speak for hda</comment>
device=hd:
partition=11

<comment>## ofboot is the openfirmware way to specify the bootstrap partition.
## If this isn't defined, yaboot fails on the G5 (unless you pass the necessary
## arguments to the mkofboot/ybin program).
## hd:X means /dev/sdaX (or /dev/hdaX).
## 
## G5 users should uncomment this line!!

#ofboot=hd:9</comment>

delay=5
defaultos=macosx
timeout=30
install=/usr/lib/yaboot/yaboot
magicboot=/usr/lib/yaboot/ofboot

<comment>#################
## This section can be duplicated if you have more than one kernel or set of
## boot options
#################</comment>
image=/boot/vmlinux
  label=Linux
  root=/dev/hda11
  sysmap=/boot/System.map
  read-only
<comment>##################</comment>

<comment>## G5 users should set 
##   macos=hd:13
##   macosx=hd:12
## instead of the example values.</comment>
macos=/dev/hda13
macosx=/dev/hda12
enablecdboot
enableofboot
</pre>

<p>
當您完成 <path>yaboot.conf</path> 的設定時，您需要執行 <c>mkofboot -v</c> 來安裝
設定到 bootstrap 分割區。<e>請不要忘記！</e> 如果一切順利，您的設定檔和上面的一
樣，下次重開機時您將會看到一個簡單，五個選項的開機清單。如果您更新了您的 yaboot
設定檔，您只需要執行 <c>ybin -v</c> 更新 bootstrap 分割區 － <c>mkofboot</c>
只適用於第一次的設定。
</p>

<p>
關於更多相關 yaboot 的資料，請參考 <uri 
link="http://penguinppc.org/projects/yaboot">yaboot 計畫</uri>。
接下來請到 <uri link="?part=1&amp;chap=10">安裝系統所需工具</uri>。
</p>

</body>
</subsection>
</section>
<section id="bootx">
<title>其他選擇：BootX</title>
<body>

<impo>
BootX 只適用於 OldWorld 的 PPC 和 IBM 的系統架構！
</impo>

<p>
如果您想要使用 BootX，在這個階段您將不需要作任何事。但是重開機後，您將需要在
BootX 控制台內（MacOS 內）進行設定。我們將在 Gentoo 安裝完成時為您講解。
</p>

<p>
接下來請到 <uri link="?part=1&amp;chap=10">安裝系統所需工具</uri>。
</p>

</body>
</section>
</sections>
