<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE guide SYSTEM "/dtd/guide.dtd">

<guide link="/doc/id/gentoo-x86-install.xml">
	<title>Instruksi Instalasi Gentoo Linux 1.4_rc4</title>

	<author title="Chief Architect">
		<mail link="drobbins@gentoo.org">Daniel Robbins</mail>
	</author>

	<author title="Author">Chris Houser</author>

	<author title="Author">
		<mail link="">Jerry Alexandratos</mail>
	</author>

	<author title="Ghost">
		<mail link="g2boojum@gentoo.org">Grant Goodyear</mail>
	</author>

	<author title="Editor">
		<mail link="zhen@gentoo.org">John P. Davis</mail>
	</author>

	<author title="Editor">
		<mail link="Pierre-Henri.Jondot@wanadoo.fr">Pierre-Henri Jondot</mail>
	</author>

	<author title="Editor">
		<mail link="stocke2@gentoo.org">Eric Stockbridge</mail>
	</author>

	<author title="Editor">
		<mail link="rajiv@gentoo.org">Rajiv Manglani</mail>
	</author>

	<author title="Editor">
		<mail link="seo@gentoo.org">Jungmin Seo</mail>
	</author>

	<author title="Editor">
		<mail link="zhware@gentoo.org">Stoyan Zhekov</mail>
	</author>

	<author title="Editor">
		<mail link="jhhudso@gentoo.org">Jared Hudson</mail>
	</author>

	<author title="Editor">
		<mail link="">Colin Morey</mail>
	</author>

	<author title="Editor">
		<mail link="peesh@gentoo.org">Jorge Paulo</mail>
	</author>

	<author title="Editor">
		<mail link="carl@gentoo.org">Carl Anderson</mail>
	</author>

	<author title="Editor">
		<mail link="swift@gentoo.org">Sven Vermeulen</mail>
	</author>

	<author title="Penerjemah">
		<mail link="erwin@gentoo.org">Erwin</mail>
	</author>

	<abstract>Dokumen ini akan mengarahkan anda dalam proses instalasi 
		Gentoo Linux 1.4_rc4. Proses instalasi Gentoo Linux dapat dilakukan dalam 
		beberapa cara, tergantung seberapa jauh sistem yang anda mau dibuat dari awal.
	</abstract>

	<version>2.6.2</version>

	<date>19 Juli 2003</date>

	<chapter>
		<title>Tentang Instalasi</title>
		<section>
			<body>
				<p>CD booting baru ini dapat diboot dengan hampir semua CD-ROM 
				IDE modern, ataupun CD-ROM SCSI, dengan syarat CD-ROM dan BIOS 
				anda mendukung booting dari CD. Di dalam CD ini terdapat dukungan 
				linux untuk IDE (dan PCI IDE) (tercompile ke dalam kernel) dan juga 
				dukungan terhadap semua perangkat SCSI (tersedia sebagai modul). 
				Sebagai tambahan, kami menyediakan modul untuk semua network 
				card yang didukung linux, dan juga tool untuk mengkonfigurasi jaringan 
				anda dan membuat koneksi <c>ssh</c> ke luar (dan ke dalam) 
				berlangsung lancar serta untuk mendownload file. </p>

				<p>Untuk melakukan proses instalasi dari CD ini, anda memerlukan 
				paling sedikit prosesor 486+ dan	paling sedikit 64 MB RAM. (Gentoo 
				Linux telah berhasil diinstall dengan menggunakan 64 MB RAM + 64 MB swap, 
				tetapi proses instalasinya berjalan sangat lambat).</p>

				<p>Gentoo Linux dapat diinstall dengan menggunakan salah satu 
				dari tiga &quot;stage&quot; file. File yang anda pilih tergantung 
				terhadap seberapa jauh 	sistem yang anda mau compile sendiri. 
				File stage1 dipilih bila anda ingin mem-bootstrap (ed: memulai) dan 
				membangun seluruh sistem anda dari awal. File stage2 dipilih bila 
				anda ingin membangun sistem anda dari sistem yang sudah 
				ter-bootstrap. File stage3 telah mengandung sistem dasar Gentoo Linux.</p>

				<p><b>Jadi, yang manakah yang harus anda pilih? stage1, stage2, atau stage3?</b>
				Memulai dari stage1, anda memiliki kontrol penuh terhadap pilihan 
				optimisasi dan fungsi saat-compile tambahan yang diaktifkan 
				pertama-tama pada sistem anda. Hal ini membuat instalasi stage1 
				sangat bagus bagi user yang mengerti apa yang mereka lakukan. 
				Instalasi stage2 membiarkan anda melewati proses bootstrap, dan 
				sistem ini juga bagus jika anda puas dengan pilihan optimisasi yang 
				kami pilih untuk file stage2 tersebut. Jika anda memilih menginstall 
				menggunakan stage3, anda akan mendapatkan instalasi Gentoo 
				Linux yang paling cepat, tetapi anda juga akan menggunakan pilihan 
				optimisasi yang kami pilih. Karena rilis Gentoo Linux utama memiliki 
				file stage3 yang dioptimisasi untuk beberapa prosesor populer, stage3 
				mungkin cukup bagi anda. <b>Jika anda menginstall Gentoo Linux 
				untuk pertama kali, cobalah menggunakan file stage3 untuk instalasi.</b></p>

				<p> Jadi, bagaimana cara memulai proses instalasi? Pertama, anda 
				harus memilih file ISO LiveCD kami yang cocok bagi anda dari 
				<uri>ftp://ftp.ibiblio.org/pub/linux/distributions/gentoo/releases/1.4_rc4/x86/x86</uri>. 
				Usahakanlah untuk mendownload dari mirror server untuk meringankan 
				beban dari server utama. Daftar server-server dapat anda temui di 
				<uri>http://www.gentoo.org/main/en/mirrors.xml</uri>.</p>

				<p> LiveCD adalah imej CD yang harus diburn ke CDR ato CD-RW 
				menggunakan program CD burning. Saat ini, kami memiliki dua jenis 
				LiveCD. Yang pertama bernama &quot;gentoo-basic&quot;, dengan 
				besar 40 MB, dan mengandung file stage1 saja serta lives di direktori 
				<path>x86/livecd/</path>. LiveCD yang ini adalah yang paling kecil agar 
				dapat didownload dengan cepat dan memiliki file stage1 yang dapat 
				ditemui di <path>/mnt/cdrom/gentoo/</path> setelah CD diboot.</p>

				<p>LiveCD yang kedua yang kami tawarkan bernama &quot;gentoo-3stages.&quot;	
				CD ini juga dapat ditemui di <path>x86/livecd</path>. CD ini memiliki 
				file stage 1, 2 dan 3. Dengan LiveCD ini, anda dapat menginstall sistem 
				Gentoo Linux yang berfungsi penuh dengan cepat.</p>

				<p><b>Bagaimana dengan stage i686, pentium3, athlon, athlon-mp, 
				LiveCD dan GRP (Gentoo Reference Platform)?</b> 
				Gentoo 1.4_rc4 dirilis dengan tujuan sebagai kandidat rilis minimum saja. 
				1.4_final akan memiliki semua arsitektur x86 dan GRP seperti biasanya. 
				Jika anda ingin menginstall stage yang teroptimisasi untuk arsitektur x86 
				yang lain atau GRP, gunakanlah dokumentasi 1.4_rc2, yang dapat ditemui 
				di <uri>http://www.gentoo.org/doc/en/gentoo-x86-1.4_rc2-install.xml</uri></p>

				<impo>Jika anda menemui masalah dalam proses instalasi dan ingin 
				melaporkan masalah tersebut sebagai bug, laporkan ke <uri>http://bugs.gentoo.org</uri>. 
				Jika bug itu perlu dilaporkan sebagai masalah utama terhadap developer 
				asli dari program tersebut (mis. tim KDE), <e>Developer Gentoo Linux</e> 
				akan melakukannya untuk anda.</impo>

				<note>Instruksi instalasi pada LiveCD mungkin bukan yang paling baru.
				Pergunakanlah selalu <uri>http://www.gentoo.org/doc/en/gentoo-x86-install.xml
				</uri> untuk instruksi instalasi yang terbaru.</note>

				<p>Sekarang, mari kita rangkum proses instalasi dengan cepat. Pertama, 
				kita download, burn, dan boot dari LiveCD. Setelah menemui root prompt, 
				kita akan membuat partisi, membuat filesystem kita, dan meng-extract salah 
				satu dari file stage1, stage2 atau stage3. Jika kita memulai dari file stage1 atau 
				stage2, kita akan menempuh beberapa langkah sebelum memasuki stage3.
				Setelah sistem kita masuk ke stage3, kita dapat mengkonfigurasi sistem tersebut 
				(mengatur file konfigurasi, menginstall boot loader, dll). Setelah diboot, kita 
				akan mendapatkan sistem Gentoo Linux yang siap dipakai. Tergantung anda 
				memulai dari stage yang mana, berikut ini adalah hal hal yang diperlukan 
				untuk instalasi.</p>

				<table>
					<tr>
						<th>stage tarbal</th>
						<th>Hal-hal yang diperlukan untuk instalasi</th>
					</tr>
					<tr>
						<ti>1</ti>
						<ti>setup partisi/filesystem, emerge sync, bootstrap, emerge system, 
						emerge kernel sources, konfigurasi akhir</ti>
					</tr>
					<tr>
						<ti>2</ti>
						<ti>setup partisi/filesystem, emerge sync, emerge system, emerge 
						kernel sources, konfigurasi akhir</ti>
					</tr>
					<tr>
						<ti>3</ti>
						<ti>setup partisi/filesystem, emerge sync, konfigurasi akhir</ti>
					</tr>
				</table>

				<note>User yang menggunakan hardware ATA RAID sebaiknya membaca bab
				tentang ATA RAID pada bagian bawah dokumen ini sebelum melanjutkan.</note>
			</body>
		</section>
	</chapter>

	<chapter>
		<title>Booting</title>
		<section>
			<body>
				<warn>Bacalah seluruh bab ini dahulu sebelum melanjutkan, terutama
				pilihan-pilihan boot yang tersedia. Jika anda mengabaikan bab ini, anda mungkin 
				menemui masalah seperti setting keyboard yang salah, service pcmcia yang tidak
				jalan, dll.</warn>

				<p>Mulailah dengan memboot LiveCD. Anda akan melihat layar boot yang 
				menarik dengan lambang Gentoo Linux. Pada saat ini, anda dapat menekan 
				Enter untuk memulai proses boot, atau memboot LiveCD dengan pilihan boot 
				tambahan dengan mengetikkan nama kernel diikuti oleh pilihan boot lalu 
				menekan Enter. Sebagai contoh <c>gentoo nousb nohotplug</c>. Periksalah 
				tabel di bawah ini untuk melihat kernel dan pilihan boot yang tersedia atau 
				dengan menekan F2 untuk menampilkan layar bantuan.</p>

				<table>
					<tr>
						<th>Kernel yang tersedia</th>
						<th>Penjelasan</th>
					</tr>
					<tr><ti>gentoo</ti><ti>kernel dasar gentoo (default)</ti></tr>
					<tr><ti>800</ti><ti>modus framebuffer 800x600</ti></tr>
					<tr><ti>1024</ti><ti>modus framebuffer 1024x768 (default)</ti></tr>
					<tr><ti>1280</ti><ti>modus framebuffer 1280x1024</ti></tr>
					<tr><ti>nofb</ti><ti>non-aktifkan modus framebuffer</ti></tr>
					<tr><ti>smp</ti><ti>me-load kernel smp dalam modus noframebuffer</ti></tr>
					<tr><ti>acpi</ti><ti>mengaktifkan acpi=on + me-load modul acpi pada saat init</ti></tr>
					<tr><ti>memtest</ti><ti>mem-boot program testing memory</ti></tr>
				</table>

				<p><table>
					<tr>
						<th>Pilihan boot yang tersedia.</th>
						<th>Penjelasan</th>
					</tr>
					<tr><ti>doataraid</ti>
					<ti>me-load modul ide raid dari initrd</ti></tr>
					<tr><ti>dofirewire</ti>
					<ti>modprobe modul firewire di initrd (untuk cd-rom firewire, dll)</ti></tr>
					<tr><ti>dokeymap</ti>
					<ti>aktifkan pilihan keymap bagi kibor non-us</ti></tr>
					<tr><ti>dopcmcia</ti>
					<ti>menjalankan servis pcmcia</ti></tr>
					<tr><ti>doscsi</ti>
					<ti>men-scan perangkat scsi (konflik dengan beberapa kartu ethernet)</ti></tr>
					<tr><ti>noapm</ti>
					<ti>nonaktifkan loading modul apm</ti></tr>
					<tr><ti>nodetect</ti>
					<ti>tidak menjalankan hwsetup/kudzu dan hotplug</ti></tr>
					<tr><ti>nodhcp</ti>
					<ti>dhcp tidak langsung dijalankan walaupun nic terdeteksi</ti></tr>
					<tr><ti>nohotplug</ti>
					<ti>nonaktifkan loading servis hotplug</ti></tr>
					<tr><ti>noraid</ti>
					<ti>nonaktifkan loading modul evms</ti></tr>
					<tr><ti>nousb</ti>
					<ti>nonaktifkan loading modul usb dari initrd, nonaktifkan hotplug</ti></tr>
					<tr><ti>ide=nodma</ti>
					<ti>nonaktifkan dma dengan paksa bagi perangkat ide yang tidak berfungsi (bermasalah)</ti></tr>
					<tr><ti>cdcache</ti>
					<ti>meng-cache bagian dari cd yang diperlukan untuk instalasi ke ram, 
					proses ini menggunakan 40 MB RAM, tetapi anda dapat melakukan umount 
					/mnt/cdrom dan me-mount cdrom lain.</ti></tr>
				</table></p>

				<p> Setelah anda menekan Enter, anda akan melihat output dari booting kernel, pesan 
				initrd dan kernel, diikuti oleh tampilan langkah-langkah boot Gentoo Linux. Anda akan 
				otomatis login sebagai &quot;<c>root</c>&quot; dan password root akan diset dengan 
				kata yang telah diacak sebagai langkah keamanan. Anda akan mendapati prompt root 
				(&quot;<c>#</c>&quot;) di konsol tersebut, dan dapat berpindah konsol dengan menekan 
				Alt-F2, Alt-F3 dan Alt-F4. Kembalilah ke konsol yang pertama anda mulai dengan menekan 
				Alt-F1. Pada saat ini, anda sebaiknya mengeset password root, ketik <c>passwd</c> dan
				ikuti langkahnya.</p>

				<p>Anda juga mungkin telah menyadari kalau di atas prompt <c>#</c> anda terdapat 
				tulisan bantuan (ed. help) yang menjelaskan bagaimana melakukan beberapa hal seperti 
				mengkonfigurasi jaringan linux anda dan dimana anda dapat menemui file stage Gentoo 
				Linux serta package-package di CD.</p>
			</body>
		</section>
	</chapter>

	<chapter>
		<title>Me-load Modul Kernel</title>
		<section>
			<body>
				<p>Bila proses autodeteksi PCI melewatkan beberapa hardware anda, anda harus me-load 
				modul kernel tersebut secara manual. Untuk meilihat daftar modul network card yang 
				tersedia, ketik <c>ls /lib/modules/*/kernel/drivers/net/*</c>. Untuk me-load modul tersebut 
				ketik: </p>

<pre caption="Konfigurasi Modul PCI">
# <c>modprobe pcnet32</c>
<comment>(ganti pcnet32 dengan module NIC anda)</comment>
</pre>

				<p>Sama seperti di atas, jika anda ingin mengakses perangkat SCSI yang tidak terdeteksi 
				pada saat proses autodeteksi boot, anda harus me-load module tersebut dari /lib/modules, 
				dengan cara yang sama <c>modprobe</c>:</p>

<pre caption="Me-load Modul SCSI">
# <c>modprobe aic7xxx</c>
<comment>(ganti aic7xxx dengan module perangkat SCSI anda)</comment>
# <c>modprobe sd_mod</c>
<comment>(sd_mod adalah modul untuk dukungan disk SCSI)</comment>
</pre>

				<note>
				Dukungan terhadap CD-ROM SCSI dan disk telah tercompile ke dalam kernel.
				</note>

				<p>LiveCD Gentoo seharusnya telah mengaktifkan DMA pada disk anda, tetapi bila 
				DMA tidak teraktifkan, anda dapat mengaktifkan dengan perintah <c>hdparm</c></p>

<pre caption="Setting DMA">
<comment>Ganti hdX dengan perangkat disk anda.</comment>
# hdparm -d 1 /dev/hdX <comment>Aktifkan DMA </comment>
# hdparm -d1 -A1 -m16 -u1 -a64 /dev/hdX
<comment>(Aktifkan DMA dan beberapa pilihan aman yg dapat menambah performa disk)</comment>
# hdparm -X66 /dev/hdX
<comment>(Aktifkan Ultra-DMA dengan paksa -- berbahaya -- dapat menyebabkan beberapa drive rusak)</comment>
</pre>
			</body>
		</section>
	</chapter>

<!-- Bagian ini sudah tidak terpakai lagi..... 
	THIS SECTION SHOULD BE DEPRECATED WITH HOTPLUG ENABLED IN 1.4_rc3 (drobbins)
<chapter>
    <title>Loading PCMCIA Kernel Modules</title>
    <section>
      <body>
        <p>If you have a PCMCIA network card, you will need to perform a few extra steps.
			</p>
        <warn>To avoid problems with <c>cardmgr</c>, you <e>must</e> run it <e>before</e> you enter the chroot
			portion of the install. </warn>
<pre caption="Loading PCMCIA Modules">
# <i>modprobe pcmcia_core</i>
# <i>modprobe i82365</i>
# <i>modprobe ds</i>
# <i>cardmgr -f</i>
</pre>
        <p>As <c>cardmgr</c> detects which hardware is present, your speaker should emit a
			few reassuring beeps, and your PCMCIA network card should be active. You can
			of course insert the PCMCIA card after loading <c>cardmgr</c> too, if that is 
			preferable. (Technically, you need not run 
			<c>cardmgr</c> if you know exactly which module your PCMCIA card requires. 
			But if you don't, loading all PCMCIA modules and see which sticks won't work,
			as all PCMCIA modules load obligingly and hang around for a PCMCIA card to 
			drop by. <c>cardmgr</c> will also unload the module(s) for any card when you
			remove it). </p>
      </body>
    </section>
  </chapter>
-->

	<chapter>
		<title>Konfigurasi Jaringan</title>
		<section>
			<title>Mungkin sudah berjalan?</title>
			<body>
				<p>Jika anda menggunakan LiveCD 1.4_rc3 atau yang lebih baru, mungkin jaringan
				anda sudah terkonfigurasi secara otomatis untuk anda. Jika benar, anda sudah dapat 
				menggunakan perintah perintah yang berhubungan dengan jaringan yang telah tersedia
				di LiveCD seperti <c>ssh</c>, <c>scp</c>, <c>ping</c>, <c>irssi</c>, <c>wget</c> 
				dan <c>lynx</c>, dan lain lain.</p>

				<p>Jika jaringan telah terkonfigurasi untuk anda, perintah <c>/sbin/ifconfig</c> akan 
				menampilkan daftar perangkat internet selain <c>lo</c>, misalnya <c>eth0</c>:</p>

<pre caption="Tampilan /sbin/ifconfig pada network card yang telah berfungsi">
eth0      Link encap:Ethernet  HWaddr 00:50:BA:8F:61:7A
          inet addr:192.168.0.2  Bcast:192.168.0.255  Mask:255.255.255.0
          inet6 addr: fe80::50:ba8f:617a/10 Scope:Link
          UP BROADCAST RUNNING MULTICAST  MTU:1500  Metric:1
          RX packets:1498792 errors:0 dropped:0 overruns:0 frame:0
          TX packets:1284980 errors:0 dropped:0 overruns:0 carrier:0
          collisions:1984 txqueuelen:100
          RX bytes:485691215 (463.1 Mb)  TX bytes:123951388 (118.2 Mb)
          Interrupt:11
</pre>

				<p>Anda mungkin mau mencoba ping server DNS ISP anda (dapat ditemui di 
				<path>/etc/resolv.conf</path>), dan salah satu webiste, hanya untuk memastikan 
				paket yang anda kirim dapat mencapai internet, resolusi nama DNS berfungsi dengan 
				benar, dll.</p>

<pre caption="Testing Jaringan Lebih Lanjut">
# <c>ping -c 3 www.yahoo.com </c>
</pre>

				<p>Dapatkah anda pergunakan jaringan anda? Jika bisa, anda dapat melewati sisa dari
				bab ini.</p>
			</body>
		</section>

		<section>
			<title>Konfigurasi PPPoE</title>
			<body>
				<p>Anggap anda menggunakan PPPoE untuk terhubung ke internet, LiveCD (versi mana saja) 
				telah memudahkan anda dengan menyertakan <c>rp-pppoe</c>. Gunakan script 
				<c>adsl-setup</c> yang tersedia untuk mengkonfigurasi koneksi anda. Anda akan 
				ditanya perangkat ethernet yang terhubung ke modem adsl anda, username dan password
				anda, IP dari server DNS anda, dan apakah anda memerlukan firewall dasar atau tidak.</p>

<pre caption="Mengkonfigurasi PPPoE">
# <c> adsl-setup </c>
# <c> adsl-start </c>
</pre>

				<p>Jika anda menemui masalah, periksa kembali apakah anda menuliskan username 
				dan password yang benar dengan melihat di <path>/etc/ppp/pap-secrets</path> atau 
				<path>/etc/ppp/chap-secrets</path>, dan pastikan kalau anda menggunakan perangkat 
				ethernet yang tepat. </p>
			</body>
		</section>

		<section>
			<title>Konfigurasi Jaringan Otomatis</title>
			<body>
				<p>Cara yang paling mudah untuk men-setup jaringan anda bila tidak terkonfigurasi secara 
				otomatis adalah dengan menjalankan script <c>net-setup</c>.</p>

<pre caption="Net-Setup Script">
# <c>net-setup eth0</c>
</pre>

				<p>Tentu saja, jika anda mau, anda tetap dapat men-setup jaringan secara manual. Hal 
				ini akan dijabarkan di bagian berikutnya.</p>
			</body>
		</section>

		<section>
			<title>Konfigurasi DHCP Secara Manual</title>
			<body>
				<p>Konfigurasi jaringan dengan DHCP sangat mudah; Jika ISP anda tidak menggunakan DHCP 
				lewati bagian berikutnya sampai konfigurasi statis di bawah.</p>

<pre caption="Konfigurasi Jaringan dengan DHCP">
# <c>dhcpcd eth0</c>
</pre>

				<note>Beberapa ISP mengharuskan anda menyediakan hostname. Lakukan dengan menambahkan 
				<c>-h myhostname</c> pada perintah dhcpd di atas.	</note>

				<p>Jika anda mendapati warning <i>dhcpConfig</i>, jangan panik; error tersebuat biasanya tidak
				penting. Lewati sampai Test Jaringan di bawah.</p>
			</body>
		</section>

		<section>
			<title>Konfigurasi Statis Secara Manual</title>
			<body>
				<p>Kita cuman perlu men-setup jaringan minimum yang cukup bagi kita untuk mendownload 
				sumber sumber sistem, dan juga interface localhost yang diperlukan. Informasi yang diperlukan 
				dijelaskan di tabel berikut ini.</p>

				<table>
				<tr><th>Informasi</th><th>Penjelasan</th><th>Contoh</th></tr>
				<tr><ti>IP address</ti><ti>Alamat IP yang ingin anda berikan kepada kartu network anda</ti><ti>192.168.1.2</ti></tr>
				<tr><ti>Broadcast address</ti><ti>Alamat IP yang akan mem-broadcast paket paket ke seluruh host di jaringan tersebut.</ti><ti>192.168.1.255</ti></tr>
				<tr><ti>Network mask</ti><ti>Mask yang digunakan bersama alamat IP untuk melihat bagian mana dari alamat tersebut yang digunakan untuk network-identification dan host-identification</ti><ti>255.255.255.0</ti></tr>
				<tr><ti>Gateway</ti><ti>Alamat IP dari komputer yang akan meneruskan paket paket yang bukan dikirimkan ke jaringan lokal (biasanya komputer yang meng-share koneksi internet)</ti><ti>192.168.1.1</ti></tr>
				</table>

				<p>Ketik perintah perintah berikut ini, ganti $IFACE dengan interface jaringan anda 
				(biasanya <c>eth0</c>), $IPNUM	dengan alamat IP anda, $BCAST dengan alamat broadcast 
				anda, $NMASK dengan mask jaringan anda. Untuk perintah <c>route</c>, ganti $GTWAY 
				dengan gateway default anda.</p>

<pre caption="Konfigurasi Jaringan IP Statis">
# <c>ifconfig $IFACE $IPNUM broadcast $BCAST netmask $NMASK</c>
# <c>/sbin/route add -net default gw $GTWAY netmask 0.0.0.0 metric 1 $IFACE</c>
</pre>

				<p>Sekarang saatnya membuat file <path>/etc/resolv.conf</path> agar resolusi nama 
				(mencari situs Web/FTP dengan nama, daripada mempergunakan alamat IP) berfungsi dengan 
				benar. Anda dapat menggunakan <c>nano -w /etc/resolv.conf</c> untuk membuat 
				<path>/etc/resolv.conf</path>. <c>nano</c> adalah editor kecil yang mudah digunakan.</p>

				<p>Berikut ini adalah kerangka sebagai panduan untuk membuat file /etc/resolv.conf anda: </p>

<pre caption="Kerangka /etc/resolv.conf">
domain mydomain.com
nameserver 10.0.0.1
nameserver 10.0.0.2
</pre>

				<p>Ganti <c>10.0.0.1</c> dan <c>10.0.0.2</c> dengan alamat IP server DNS utama dan 
				secondary anda berturut-turut.</p>
			</body>
		</section>

		<section>
			<title>Konfigurasi Proxy</title>
			<body>
				<p>Jika anda menggunakan proxy, sebaiknya anda mengkonfigurasi proxy dahulu sebelum 
				melanjutkan instalasi. Kita akan meng-export beberapa variabel untuk men-setup proxy tersebut.</p>

<pre caption="Set Proxy">
<codenote>Jika proxy tersebut membatasi koneksi HTTP:</codenote>
# <c>export http_proxy=&quot;machine.company.com:1234&quot; </c>
<codenote>Jika proxy tersebut membatasi koneksi FTP:</codenote>
# <c>export ftp_proxy=&quot;machine.company.com&quot; </c>
<codenote>Jika proxy tersebut membatasi koneksi RSYNC:</codenote>
# <c>export RSYNC_PROXY=&quot;machine.company.com&quot; </c>
</pre>

			</body>
		</section>

		<section>
			<title>Jaringan telah berfungsi!</title>
			<body>
				<p>Jaringan anda sekarang seharusnya telah terkonfigurasi dan berfungsi. Anda harusnya telah 
				bisa menggunakan perintah perintah <c>ssh</c>, <c>scp</c>, <c>lynx</c>, <c>irssi</c> 
				and <c>wget</c> yang disertakan untuk menghubungi mesin lain di jaringan LAN anda atau 
				Internet.</p>
			</body>
		</section>

		<section>
			<title>Saya tidak memiliki jaringan (koneksi internet)!</title>
			<body>Jika anda tidak memiliki jaringan atau koneksi internet, tersedia beberapa bantuan di 
			<uri link="http://forums.gentoo.org/">Forum Gentoo</uri>.
			Beberapa link yang berguna dapat ditemui di <uri>http://forums.gentoo.org/viewtopic.php?t=43025</uri>.
			</body>
		</section>
	</chapter>

	<chapter>
		<title>Set tanggal dan waktu sistem anda</title>
		<section>
			<body>
				<p>Sekarang anda perlu mengeset tanggal dan waktu sistem anda. Anda dapat menggunakan 
				perintah <c>date</c>.</p>

<pre caption="Set tanggal sistem anda">
# <c>date</c>
Thu Feb 27 09:04:42 CST 2003
<comment>(Jika tanggal anda salah, set tanggal anda dengan perintah berikut ini)</comment>
# <c>date 022709042003</c>
<comment>(date MMDDhhmmCCYY)</comment>
</pre>

			</body>
		</section>
	</chapter>

	<chapter>
		<title>Filesystem, partisi and block device</title>
		<section>
			<title>Pengenalan terhadap block device</title>
			<body>
				<p>Pada bab ini, kita akan melihat lebih jauh tentang aspek disk di Gentoo Linux dan 
				Linux pada umumnya, termasuk filesystem Linux, partisi dan block device. Setelah anda mengenal 
				bagian dalam dan luar disk dan filesystem, anda akan diarahkan dalam proses membuat partisi 
				dan filesystem bagi instalasi Gentoo Linux anda.</p>

				<p>Pertama tama, saya akan memperkenalkan "block device". Block device yang paling sering 
				ditemui mungkin adalah block device yang mewakili drive IDE pertama di sistem Linux:</p>

<pre caption="/dev/hda, block device yang mewakili drive IDE master utama di sistem anda">
/dev/hda
</pre>

				<p>Jika sistem anda menggunakan drive SCSI, maka perangkat drive pertama anda adalah:</p>

<pre caption="/dev/sda, block device yang mewakili drive SCSI logical pertama di sistem anda">
/dev/sda
</pre>

				<p>Block device di atas mewakili interface <i>abstract</i> terhadap disk. Program program 
				dapat menggunakan block device ini untuk berhubungan dengan disk anda tanpa memperdulikan 
				apakah driver anda adalah IDE, SCSI atau yang lainnya. Program tersebut dapat dengan mudah 
				mengganggap data di disk tersebut sebagai blok blok 512-byte yang dapat diakses secara acak 
				yang saling terhubung satu sama lain.</p>
			</body>
		</section>

		<section>
			<title>Partisi dan fdisk</title>
			<body>
				<p>Di sistem Linux, kita membuat filesystem dengan menggunakan perintah khusus bernama 
				<c>mkfs</c> (atau <c>mke2fs</c>, <c>mkreiserfs</c>, dll,) dengan memberikan block 
				device tertentu sebagai parameter perintah tersebut.</p>

				<p>Bagaimanapun juga, walaupun secara teori dimungkinkan untuk menggunakan "seluruh 
				disk" block device (block device yang mewakili <i>seluruh</i> disk) seperti<c>/dev/hda</c> 
				atau <c>/dev/sda</c> untuk membuat sebuah filesystem tunggal, hal ini hampir tidak pernah 
				dipraktekkan sehari-hari. Melainkan, block device tersebut dipisah pisah ke block device yang 
				lebih kecil dan mudah diatur bernama "partisi". Partisi dibuat menggunakan program bernama 
				<c>fdisk</c>, yang digunakan untuk membuat dan mengubah tabel partisi yang tersimpan 
				di setiap disk. Tabel partisi tersebut mencantumkan dengan tepat bagaimana seluruh disk 
				tersebut dipisah.</p>

				<p>Kita dapat melihat tabel partisi sebuah disk dengan menjalankan <c>fdisk</c>, 
				dengan menyertakan block device yang mewakili seluruh disk sebagai parameter: </p>

				<note>Interface alternatif terhadap tabel partisi disk termasuk <c>cfdisk</c>, 
				<c>parted</c> dan <c>partimage</c>. Kami merekomendasikan <c>fdisk</c> karena 
				program ini lebih bagus dan populer di dunia Unix/Linux.</note>

<pre caption="Menjalankan fdisk">
# fdisk /dev/hda 
</pre>

				<p>atau</p>

<pre caption="Menjalankan fdisk untuk melihat tabel partisi /dev/sda">
# fdisk /dev/sda
</pre>

				<impo><b>Ingat untuk <i>tidak</i> menyimpan atau mengubah tabel partisi disk 
				jika salah satu dari partisinya mengandung filesystem yang sedang terpakai atau 
				berisi data data penting. Jika anda lakukan, akan menyebabkan data pada disk 
				tersebut hilang.</b></impo>

				<p>Setelah masuk ke fdisk, anda akan ditampilkan dengan prompt seperti ini:</p>

<pre caption="Prompt fdisk">
Command (m for help): 
</pre>

				<p>Ketik <c>p</c> untuk menampilkan konfigurasi partisi disk tersebut:</p>

<pre caption="Contoh konfigurasi partisi">
Command (m for help): p

Disk /dev/hda: 240 heads, 63 sectors, 2184 cylinders
Units = cylinders of 15120 * 512 bytes

Device Boot    Start       End    Blocks   Id  System
/dev/hda1             1        14    105808+  83  Linux
/dev/hda2            15        49    264600   82  Linux swap
/dev/hda3            50        70    158760   83  Linux
/dev/hda4            71      2184  15981840    5  Extended
/dev/hda5            71       209   1050808+  83  Linux
/dev/hda6           210       348   1050808+  83  Linux
/dev/hda7           349       626   2101648+  83  Linux
/dev/hda8           627       904   2101648+  83  Linux
/dev/hda9           905      2184   9676768+  83  Linux

Command (m for help): 
</pre>

				<p>Disk ini terkonfigurasi untuk menyimpan 7 filesystem (setiap filesystem tercantum 
				dengan system "Linux") dan juga partisi swap (tercantum sebagai "Linux swap").</p>

				<p>Perhatikan nama dari block device partisi tersebut di bagian kiri, mulai dari 
				<c>/dev/hda1</c> sampai <c>/dev/hda9</c>. Pada zaman awal komputer, program untuk
				mempartisi disk memperbolehkan maksimum empat partisi saja (dikenal sebagai partisi 
				"primary/utama"). Batasan ini tidak cukup, jadi sebagai penyelesaian <i>extended partitioning</i> 
				dibuat. Partisi extended sangat mirip dengan partisi primary, dan dibatasi sebanyak empat 
				partisi (sama dengan partisi primary). Tetapi, partisi extended dapat menampung seberapa 
				banyakpun partisi <i>logical</i> di dalamnya, yang menyelesaikan masalah batasan empat 
				partisi sebelumnya.</p>

				<p>Semua partisi <c>hda5</c> dan yang lebih tinggi adalah partisi logical. Nomor 1 sampai 
				4 di-reserve untuk partisi primary atau extended.</p>

				<p>Jadi, sebagai contoh, <c>hda1</c> sampai <c>hda3</c> adalah partisi primary.
				<c>hda4</c> adalah partisi extended yang mengandung partisi logical <c>hda5</c> 
				sampai <c>hda9</c>. Anda sebenarnya tidak akan pernah <i>menggunakan</i> 
				<c>/dev/hda4</c> untuk menyimpan filesystem apapun secara langsung -- partisi ini 
				bertindak sebagai penampung dari partisi partisi <c>hda5</c> sampai <c>hda9</c>. </p>

				<p> Anda juga harus mengetahui bahwa setiap partisi memiliki sebuah "id", dikenal juga 
				sebagai "jenis/tipe partisi". Setiap kali anda membuat partisi baru, anda harus memastikan 
				bahwa tipe partisi-nya telah diset dengan benar. '83' adalah tipe partisi yang tepat untuk 
				partisi yang menggunakan filesystem Linux, '82' adalah tipe partisi yang tepat untuk partisi 
				swap Linux dan 'fd' adalah tipe partisi yang direkomendasikan bagi partisi RAID. Anda  dapat mengeset 
				tipe partisi menggunakan opsi <c>t</c> di <c>fdisk</c>. Kernel Linux menggunakan setting 
				tipe partisi untuk mendeteksi filesystem dan swap secara otomatis pada saat boot.</p>
			</body>
		</section>

		<section>
			<title>Menggunakan fdisk untuk men-set up partisi</title>
			<body>
				<p>Sekarang anda telah mengenal cara partisi disk di linux dilakukan, saatnya mengarahkan anda 
				dalam proses men-setup partisi disk untuk instalasi Gentoo Linux anda. Setelah kami memandu 
				anda membuat partisi disk anda, konfigurasi partisi anda akan tampil seperti ini:</p>

<pre caption="Konfigurasi partisi yang akan anda dapati setelah mengikuti beberapa langkah di bawah">
Disk /dev/hda: 30.0 GB, 30005821440 bytes
240 heads, 63 sectors/track, 3876 cylinders
Units = cylinders of 15120 * 512 = 7741440 bytes

   Device Boot    Start       End    Blocks   Id  System
/dev/hda1   *         1        14    105808+  83  Linux
/dev/hda2            15        81    506520   82  Linux swap
/dev/hda3            82      3876  28690200   83  Linux

Command (m for help):
</pre>

				<p>Sebagai konfigurasi partisi "pemula", kami menyarankan anda membuat tiga partisi. 
				Yang Pertama (<c>/dev/hda1</c>) berada di awal disk adalah partisi kecil bernama partisi 
				boot. Tujuan dari partisi boot ini adalah untuk menyimpan semua data penting yang 
				berhubungan dengan boot -- data boot loader GRUB (Jika anda nanti menggunakan GRUB) 					dan juga kernel Linux. Partisi boot menyediakan kita sebuah tempat yang aman untuk 
				menyimpan segala sesuatu yang berhubungan dengan booting Linux. Selama pemakaian 
				sehari-hari Gentoo Linux, partisi boot anda sebaiknya <e>tidak termount</e> sebagai 
				langkah keamanan. Jika anda menggunakan sistem SCSI, partisi boot anda mungkin berupa 
				<c>/dev/sda1</c>.</p> 

				<p>Anda disarankan untuk membuat partisi boot (menyimpan segala yang penting bagi 
				boot loader untuk berfungsi dengan benar) pada awal disk. Walaupun hal tersebut tidak 
				harus lagi, ini sudah digunakan sejak dulu sebagai tradisi sewaktu boot loader lilo belum 
				bisa me-load kernel dari filesystem yang berada di atas cylinder 1024.</p>

				<p>Partisi kedua (<c>/dev/hda2</c>) digunakan sebagi swap. Kernel menggunakan swap 
				sebagai memory virtual bila RAM yang tersedia sangat sedikit. Partisi ini biasanya tidak besar,
				kira-kira sekitar 512 MB. Jika anda menggunakan sistem SCSI, partisi ini mungkin berupa 
				<c>/dev/sda2</c>.</p>

				<p>Partisi ketiga (<c>/dev/hda3</c>) biasanya agak besar dan menggunakan sisa space 
				dari disk tersebut. Partisi ini bernama partisi "root" dan akan bertindak sebagi penyimpan 
				filesystem utama anda yang menampung Gentoo Linux. Pada sistem SCSI, partisi ini mungkin 
				berupa <c>/dev/sda3</c>.</p>


				<p>Sebelum kita mem-partisi disk, berikut ini adalah rangkuman dari partisi yang disarankan 
				beserta konfigurasi filesystemnya untuk digunakan pada saat instalasi Gentoo Linux:</p>

				<table>
					<tr>
						<th>Partisi</th>
						<th>Besar</th>
						<th>Tipe/Jenis</th>
						<th>contoh device</th>
					</tr>
					<tr>
						<ti>partisi boot, mengandung kernel dan data-data boot</ti>
						<ti>32 Megabytes</ti>
						<ti>ext2/3 sangat disarankan (paling mudah); Jika ReiserFS, pergunakan perintah mount dengan <c>-o notail</c>. Jika anda menggunakan ext3 atau ReiserFS, anda harus menuliskan besar dari journal; kami sarankan 64 MB</ti>
						<ti>/dev/hda1</ti>
					</tr>
					<tr>
						<ti>partisi swap (Tidak lagi dibatasi sampai 128 Megabyte, sekarang 2GB)</ti>
						<ti>Biasanya, buatlah partisi swap anda satu atau dua kali lebih besar dari RAM anda.</ti>
						<ti>Linux swap</ti>
						<ti>/dev/hda2</ti>
					</tr>
					<tr>
						<ti>partisi root, mengandung filesystem utama (/usr, /home, dll)</ti>
						<ti>&gt;=1.5 Gigabytes</ti>
						<ti>ReiserFS, ext3 (disarankan); ext2 ok</ti>
						<ti>/dev/hda3</ti>
					</tr>
				</table>

				<p>Baiklah, sekarang saatnya membuat partisi seperti yang tampak di atas.
				Pertama, jalankan program fdisk dengan mengetikkan <c>fdisk /dev/hda</c> 
				atau <c>fdisk /dev/sda</c>, tergantung drive yang anda pakai IDE atau SCSI.
				Lalu, ketik <c>p</c> untuk menampilkan konfigurasi partisi disk tersebut.
				Jika ada data-data yang perlu anda pakai atau penting bagi anda di disk tersebut, 
				<b>berhenti sekarang</b>. Jika anda melanjutkan dengan langkah-langkah 
				berikutnya, <b>semua data pada disk tersebut akan terhapus</b></p>

				<impo>Jika anda mengikuti langkah-langkah di bawah ini, seluruh data pada 
				disk tersebut <b>akan terhapus</b>! Jika ada data pada drive anda, pastikan 
				bahwa data tersebut tidak penting atau anda tidak perduli bila data tersebut hilang.
				Pastikan juga kalau anda <b>telah memilih drive yang benar</b> agar data anda 
				tidak terhapus dengan tidak sengaja pada drive yang salah.</impo>

				<p>Sekarang saatnya untuk menghapus segala partisi pada disk tersebut. Ketik 
				<c>d</c> dan tekan Enter. Anda akan ditanya nomor partisi yang ingin dihapus.
				Untuk menghapus <c>/dev/hda1</c> yang telah ada, ketik:</p>

<pre caption="Menghapus sebuah partisi">
Command (m for help): d
Partition number (1-4): 1
</pre>

				<p>Partisi telah dijadwalkan untuk dihapus. Partisi tersebut tidak akan lagi tampak 
				bila anda ketik <c>p</c>, tetapi tidak akan terhapus sampai segala perubahan yang 
				anda lakukan disimpan. Jika anda melakukan kesalahan dan ingin membatalkan tanpa 
				menyimpan perubahan anda, ketik <c>q</c> segera dan tekan Enter; dan partisi anda 
				tidak akan terhapus.</p>

				<p>Sekarang, saya menganggap anda benar benar ingin menghapus semua partisi di disk anda, 
				ketikkan berturut turut <c>p</c> untuk menampilkan daftar partisi dan ketik <c>d</c> serta 
				nomor partisi untuk dihapus. Pada akhirnya, anda akan mendapatkan tabel partisi yang kosong:</p>

<pre caption="tabel partisi yang kosong">
Disk /dev/hda: 30.0 GB, 30005821440 bytes
240 heads, 63 sectors/track, 3876 cylinders
Units = cylinders of 15120 * 512 = 7741440 bytes

   Device Boot    Start       End    Blocks   Id  System

Command (m for help):
</pre>

				<p>Sekarang tabel partisi telah kosong (tersimpan di memory), kita siap membuat partisi boot.
				Ketik <c>n</c> untuk membuat partisi baru, lalu <c>p</c> untuk memberitahu fdisk agar 
				membuat partisi primary. Lalu ketik <c>1</c> untuk membuat partisi primary pertama. Sewaktu 
				ditanya cylinder pertama (first cylinder), tekan Enter. Sewaktu ditanya cylinder terakhir (last cylinder), 
				ketik <c>+32M</c> untuk membuat partisi sebesar 32 MB. Anda dapat melihat tampilan dari 
				langkah langkah tersebut di bawah ini:</p>

				<note>Journal filesystem memerlukan space tambahan untuk journal mereka. Konfigurasi standar 
				memerlukan 33 MB. Karena itu, jika anda menggunakan journal filesystem pada <path>/boot</path>, 
				anda harus mengetikkan <c>+64M</c> sewaktu ditanya cylinder akhir.</note>

<pre caption="Langkah-langkah untuk membuat partisi boot">
Command (m for help): n
Command action
   e   extended
   p   primary partition (1-4)
p
Partition number (1-4): 1
First cylinder (1-3876, default 1):
Using default value 1
Last cylinder or +size or +sizeM or +sizeK (1-3876, default 3876): +32M
</pre>

				<p>Sekarang, bila anda ketik <c>p</c>, anda akan mendapatkan tampilan partisi:</p>

<pre caption="Partisi pertama kali telah dibuat">
Command (m for help): p

Disk /dev/hda: 30.0 GB, 30005821440 bytes
240 heads, 63 sectors/track, 3876 cylinders
Units = cylinders of 15120 * 512 = 7741440 bytes

   Device Boot    Start       End    Blocks   Id  System
/dev/hda1             1        14    105808+  83  Linux
</pre>

				<p>Selanjutnya, mari kita buat partisi swap. Ketik <c>n</c> untuk membuat partisi baru,
				lalu <c>p</c> untuk memberitahu fdisk bahwa anda ingin partisi primary. Lalu ketik <c>2</c>
				untuk membuat partisi primary kedua, <c>/dev/hda2</c> dalam contoh ini. Sewaktu ditanya 
				cylinder pertama (first cylinder), tekan Enter. Sewaktu ditanya cylinder akhir (last cylinder), ketik 
				<c>+512M</c> untuk membuat partisi sebesar 512MB. Setelah anda melakukan ini, ketik 
				<c>t</c> untuk mengeset tipe partisi, lalu <c>2</c> untuk memilih partisi yang baru saja 
				anda buat, dan masukkan <c>82</c> untuk mengeset partisi 
				tersebut sebagai "Linux Swap". Setelah menyelesaikan langkah-langkah di atas, ketik <c>p</c>
				dan anda akan menemui tabel partisi yang hampir sama dengan ini:</p>

<pre caption="Partisi swap kita telah dibuat">
Command (m for help): p

Disk /dev/hda: 30.0 GB, 30005821440 bytes
240 heads, 63 sectors/track, 3876 cylinders
Units = cylinders of 15120 * 512 = 7741440 bytes

   Device Boot    Start       End    Blocks   Id  System
/dev/hda1             1        14    105808+  83  Linux
/dev/hda2            15        81    506520   82  Linux swap
</pre>

				<p>Terakhir, mari kita buat partisi root. Ketik <c>n</c> untuk membuat partisi baru, 
				lalu <c>p</c> untuk memberitahu fdisk bahwa anda ingin partisi primary. Lalu ketik 
				<c>3</c> untuk membuat partisi primary ketiga, <c>/dev/hda3</c> dalam contoh ini.
				Sewaktu ditanya cylinder pertama (first cylinder), tekan Enter. Sewaktu ditanya cylinder 
				akhir (last cylinder), tekan Enter untuk membuat partisi tersebut menggunakan seluruh 
				sisa space di disk tersebut. Setelah menyelesaikan langkah-langkah di atas, ketik <c>p</c> 
				dan anda akan menemui tabel partisi seperti ini:</p>

<pre caption="Partisi root kita telah dibuat">
Command (m for help): p

Disk /dev/hda: 30.0 GB, 30005821440 bytes
240 heads, 63 sectors/track, 3876 cylinders
Units = cylinders of 15120 * 512 = 7741440 bytes

   Device Boot    Start       End    Blocks   Id  System
/dev/hda1             1        14    105808+  83  Linux
/dev/hda2            15        81    506520   82  Linux swap
/dev/hda3            82      3876  28690200   83  Linux
</pre>

				<p>Terakhir, kita perlu mengeset flag "bootable" pada partisi boot kita dan simpan 
				semua perubahan kita pada disk tersebut. Untuk memberi <c>/dev/hda1</c> flag 
				"bootable", ketik <c>a</c> pada menu dan masukkan <c>1</c> sebagai nomor 
				partisi. Jika anda mengetikkan <c>p</c> sekarang, anda akan melihat bahwa 
				<c>/dev/hda1</c> sudah memiliki tanda <c>*</c> di kolom "Boot". Sekarang, mari 
				kita simpan segala perubahan kita pada disk tersebut. Ketik <c>w</c> lalu tekan Enter. 
				Partisi disk anda sekarang telah terkonfigurasi untuk instalasi Gentoo Linux.</p>

				<note>Jika <c>fdisk</c> atau <c>cfdisk</c> menyuruh anda untuk me-reboot, 
				lakukanlah supaya sistem anda dapat mendeteksi konfigurasi partisi baru anda.</note>
			</body>
		</section>

		<section>
			<title>Membuat filesystem</title>
			<body>
				<p>Setelah partisi anda dibuat, sekarang saatnya men-setup filesystem pada partisi 
				boot dan root supaya partisi tersebut dapat di-mount dan dipakai untuk menyimpan data.
				Kita juga akan mengkonfigurasi partisi swap.</p>

				<p>Gentoo Linux mendukung beberapa tipe filesystem; setiap tipe memiliki keuntungan 
				dan kelemahannya serta performa khusus masing masing. Pada saat ini, kami mendukung 
				pembuatan filesystem ext2, ext3, XFS, JFS dan ReiserFS.</p>

				<p>ext2 adalah filesystem yang asli dan telah dicoba/dipakai tetapi tidak memiliki journal 
				metadata, yang berarti waktu yang diperlukan untuk memeriksa filesystem ext2 kadang bisa 
				sangat lama. Saat ini telah ada beberapa filesystem <i>journal</i> generasi baru yang dapat 
				diperiksa konsistensinya dengan cepat dan karena itu lebih dipilih daripada partisi non-journal.
				Journal filesystem mencegah waktu tunda sewaktu anda boot dari sistem dimana filesystem anda 
				kebetulan berada pada saat <i>inkonsisten</i>.</p>

				<p>ext3 adalah filesystem ext2 versi journal, memiliki journal metadata untuk pemulihan sistem 
				yang cepat dan juga beberapa tambahan modus journal seperti journal data terstruktur dan data 
				penuh. ext3 adalah filesystem yang sangat bagus dan dapat dihandalkan. filesystem ini umumnya 
				memberikan performa yang bagus dalam hampir semua kondisi. Karena filesystem ini tidak 
				menggunakan sistem "tree (pohon)" pada disain internalnya, filesystem ini tidak berskala bagus, 
				artinya filesystem ini bukanlah pilihan yang ideal bagi filesystem yang sangat besar, atau situasi 
				dimana anda akan menangani file yang sangat besar atau file dalam jumlah yang sangat besar 
				dalam sebuah direktori. Tetapi bila digunakan pada tempat yang memang target dari disain ext3, 
				ext3 adalah filesystem yang handal.</p>

				<p>ReiserFS adalah filesystem berbasis B*-tree yang memiliki performa keseluruhan yang bagus 
				dan performanya melebihi ext2 dan ext3 pada saat menangani file kecil (file yang besarnya kurang 
				dari 4k), sering kali melebihi dalam faktor 10x-15x. ReiserFS juga berskala sangat bagus dan 
				memiliki journal metadata. Setelah kernel 2.4.18+, ReiserFS telah mapan dan sangat direkomendasikan 
				untuk digunakan sebagai pemakaian umum atau kasus khusus seperti pembuatan filesystem 
				yang besar, penggunaan file kecil yang banyak, file yang besar, dan directory yang mengandung 
				puluhan ribu file. ReiserFS adalah filesystem yang kami rekomendasikan pada semua partisi non-boot.</p>

				<p>XFS adalah filesystem dengan journal metadata yang didukung penuh oleh kernel 
				<path>xfs-sources</path> Gentoo Linux. Filesystem ini datang dengan fungsi fungsi mapan 
				dan teroptimasi untuk skalabilitas. Kami hanya merekomendasikan anda menggunakan filesystem 
				ini pada system Linux dengan disk SCSI kelas atas dan/atau disk fibre serta power supply yang tidak 
				pernah terputus. Karena XFS meng-cache data yang lewat dengan agresif ke dalam RAM, data-data 
				mungkin hilang pada saat program yang tidak didisain dengan benar (program-program yang tidak 
				mengambil langkah yang aman sewaktu menulis ke disk, dan ada beberapa program seperti itu) berjalan 
				dan sistem tiba-tiba mati.</p>  

				<p>JFS adalah filesystem journal dengan performa bagus dari IBM. Filesystem ini baru-baru ini 
				menjadi filesystem yang siap pakai, dan sampai saat ini tidak banyak record tentang filesystem ini 
				jadi kami tidak dapat berkomentar apakah filesystem ini bagus atau tidak dalam hal stabilitas, dll.</p>

				<p>Jika anda mencari filesystem journal yang paling mapan, gunakan ext3. Jika anda mencari 
				filesystem dengan performa bagus secara keseluruhan dan juga mendukung journal, gunakan 
				ReiserFS; ext3 dan ReiserFS dua-duanya telah mapan, dan direkomendasikan sebagai pemakaian
				umum.</p>

<!-- Corner case, confusing
		<p>But before creating filesystems, you may want to initialize the
			beginning of your partition using <c>dd</c> if you are using a pre-existing partition that has been used before. 
			This is particularly helpful when you're going to create a new XFS filesystem on a partition that previously contained
			a ReiserFS filesystem. Doing this will ensure that your new filesystem
			will not be mis-identified by Linux's filesystem auto-detection code.
			This can be done as follows:	
			</p>
<pre caption="Initializing first 1024 bytes of your partition">
# <c>dd if=/dev/zero of=/dev/hda3 bs=1k count=1</c>
<comment>(Replace /dev/hda3 with the partition you wish to &quot;clean.&quot;)</comment>
</pre>
	<warn>The command above will destroy all data from <path>/dev/hda3</path>. 
	Be careful and check twice which partition you specify for zeroing.
	If you make a mistake it might result in a loss of data.
	</warn>
-->

				<p>Berpatokan dengan contoh di atas, kita akan menggunakan perintah-perintah berikut ini 
				untuk mengeset partisi kita:</p>

<pre caption="mengeset partisi kita (contoh)">
# mke2fs -j /dev/hda1
# mkswap /dev/hda2
# mkreiserfs /dev/hda3
</pre>

				<p>Kami memilih ext3 untuk partisi boot <c>/dev/hda1</c> kita karena filesystem journal 
				ini mapan dan didukung oleh semua boot loader populer. Kita menggunakan <c>mkswap</c> 
				untuk partisi swap <c>/dev/hda2</c> kita. Dan untuk partisi root 
				utama kita pada <c>/dev/hda3</c> kami pilih ReiserFS, karena filesystem ini sangat mapan 
				dan memberikan performa yang sangat bagus. Sekarang, mulailah dan set partisi anda.</p>

				<p>Sebagai referensi anda, berikut ini adalah beberapa perintah <c>mkfs</c> yang bisa 
				dipakai saat proses instalasi:</p>

				<p><c>mkswap</c> adalah perintah yang digunakan untuk mengeset partisi swap:</p>

<pre caption="Mengeset Swap">
# <c>mkswap /dev/hda2</c>
</pre>

				<p>Anda dapat menggunakan perintah <c>mke2fs</c> untuk membuat filesystem ext2:</p>

<pre caption="Membuat Filesystem ext2">
# <i>mke2fs /dev/hda1</i>
</pre>

				<p>Jika anda ingin menggunakan ext3, anda dapat membuat filesystem ext3 dengan 
				<c>mke2fs -j</c>:</p>

<pre caption="Membuat Filesystem ext3">
# <c>mke2fs -j /dev/hda3</c>
</pre>

				<note>Anda dapat membaca lebih lanjut tentang ext3 di Linux 2.4 di 
				<uri>http://www.zip.com.au/~akpm/linux/ext3/ext3-usage.html</uri>.</note>

				<p>Untuk membuat filesystem ReiserFS, gunakan perintah <c>mkreiserfs</c>:</p>

<pre caption="Membuat Filesystem ReiserFS">
# <c>mkreiserfs /dev/hda3</c>
</pre>

				<p>Untuk membuat filesystem XFS, gunakan perintah <c>mkfs.xfs</c>:</p>

<pre caption="Membuat Filesystem XFS">
# <c>mkfs.xfs /dev/hda3</c>
</pre>

				<note>Anda mungkin mau memberikan beberapa flag tambahan ke perintah <c>mkfs.xfs</c>: 
				<c>-d agcount=3 -l size=32m</c>. Perintah <c>-d agcount=3</c> akan menurunkan jumlah 
				alokasi grup. XFS akan memaksa untuk menggunakan paling sedikit 1 alokasi grup per 4 GB 
				partisi anda, jadi, sebagai contoh, anda memiliki partisi 20GB, anda akan memerlukan paling sedikit
				5 alokasi grup. Perintah <c>-l size=32m</c> meningkatkan besar journal sampai 32 MB, juga 
				meningkatkan performa.</note>

				<p>Untuk membuat filesystem JFS, gunakan perintah <c>mkfs.jfs</c>:</p>

<pre caption="Membuat Filesystem JFS">
# <c>mkfs.jfs /dev/hda3</c>
</pre>
			</body>
		</section>
	</chapter>

	<chapter>
		<title>Mount Partisi</title>
		<section>
			<body>
				<p>Sekarang, kita akan mengaktifkan swap yang baru kita set, karena kita mungkin akan 
				memerlukan virtual memory nanti:</p>

<pre caption="Mengaktifkan Swap">
# <c>swapon /dev/hda2</c>
</pre>

				<p>Selanjutnya, kita akan membuat <path>/mnt/gentoo</path> dan 
				<path>/mnt/gentoo/boot</path> sebagai tempat mount, dan kita akan me-mount filesystem 
				kita ke sana. Setelah filesystem boot dan root kita ter-mount, segala file yang kita copy atau 
				buat di <path>/mnt/gentoo</path> akan disimpan di filesystem baru kita. Perhatian! bila anda 
				membuat filesystem <path>/usr</path> atau <path>/var</path> terpisah, filesystem ini akan 
				termount di <path>/mnt/gentoo/usr</path> dan <path>/mnt/gentoo/var</path> berturut-turut.</p>
 
				<impo>Jika partisi <e>boot</e> anda (yang menyimpan kernel) adalah ReiserFS, pastikan 
				bahwa parameter <c>notail</c> berada di belakang nama partisi boot anda di <path>/etc/fstab</path>.
				Kami akan mengulas hal ini sebentar lagi. Jika anda menggunakan LILO dan ReiserFS, maka 
				parameter <c>-o notail</c> tidak diperlukan. Selalu lebih aman untuk memberikan parameter 
				<c>-o notail</c> dengan ReiserFS jika anda tidak yakin.</impo>

<pre caption="Membuat Tempat Mount">
# <c>mount /dev/hda3 /mnt/gentoo</c>
# <c>mkdir /mnt/gentoo/boot</c>
# <c>mount /dev/hda1 /mnt/gentoo/boot</c>
</pre>

				<impo>Jika anda menemui masalah saat me-mount partisi boot anda dengan ext2, cobalah 
				menggunakan perintah <c>mount /dev/hXX /mnt/gentoo/boot -t ext2</c></impo>
			</body>
		</section>
	</chapter>

	<chapter>
		<title>File tarbal Stage dan chroot</title>
		<section>
			<title>Memilih file stage yang tepat</title>
			<body>
				<p>Sekarang, anda harus memilih file stage mana yang akan anda pergunakan sebagai 
				basis untuk instalasi anda.</p>

				<p>Jika anda menggunakan metode &quot;bangun segalanya dari awal&quot;, anda harus 
				menggunakan imej file <path>stage1-x86-1.4_rc4.tar.bz2</path>. Jika anda menggunakan 
				LiveCD tipe kedua yaitu ISO 3stage, anda juga dapat memilih menggunakan stage2 atau 
				stage3. File imej ini dapat mengurangi waktu anda dalam mengkonfigurasi (kami telah memilih 
				optimisasi compiler dan USE default bagi anda). Stage-stage pada CD dapat anda temui di 
				<path>/mnt/cdrom/gentoo</path>; anda dapat juga mengetikkan <c>ls /mnt/cdrom/gentoo</c> 
				untuk melihat stage-stage apa yang tersedia di CD anda.</p>

				<p>Jika anda ingin melakukan instalasi menggunakan file stage yang <i>tidak</i> berada di 
				CD anda, hal ini dimungkinkan, tapi anda harus mendownload file stage yang anda inginkan 
				dengan mengikuti instruksi berikut ini. Jika anda telah memiliki file stage yang akan digunakan 
				(kebanyakan orang), maka lompati bagian ini dan sambung dengan bagian "Meng-extract file stage".</p>

<pre caption="Mendownload Stage yang Diperlukan">
# <c>cd /mnt/gentoo</c>
<comment>Gunakan lynx untuk  memasukkan URL dari tarbal tersebut:</comment>
# <c>lynx http://www.ibiblio.org/pub/Linux/distributions/gentoo/releases/1.4_rc4/x86/</c>
<comment>Gunakan panah <c>Atas</c> dan <c>Bawah</c> (atau <c>TAB</c>) untuk berpindah ke direktori yang tepat.
Pilih stage yang ingin anda download
Tekan <c>d</c> yang akan memulai download
Simpan file tersebut dan keluar dari browser

<b>atau</b> gunakan wget dari command line:</comment>
# <c>wget <comment>masukkan URL dari file stage tersebut di sini.</comment></c>
</pre>

			</body>
		</section>

		<section>
			<title>Meng-extract file stage</title>
			<body>
				<p>Sekarang saatnya untuk meng-extract file stage pilihan anda yang terkompresi ke 
				<path>/mnt/gentoo/</path>. Ingat, anda cuma perlu untuk meng-extract <b>satu</b> 
				file stage saja, yaitu stage1, stage2 atau stage3. Jadi, Jika anda ingin melakukan instalasi 
				Gentoo dari stage3, maka anda cuma perlu meng-extract file stage3 saja. Extract file (tarbal) 
				stage tersebut seperti ini:</p>

				<impo>Pastikan untuk menggunakan parameter <c>p</c> pada <c>tar</c>. Jika anda 
				melupakan ini, akan mengakibatkan beberapa file memiliki permission yang salah.</impo>

<pre caption="Meng-extract file Stage">
# <c>cd /mnt/gentoo</c>
<comment>Ganti "stage3" menjadi "stage2" atau "stage1" jika anda ingin memulai dari stage ini.</comment>
<comment>Jika anda mendownload file (tarbal) stage anda, ganti "/mnt/cdrom/gentoo/" menjadi "/mnt/gentoo/".</comment>
# <c>tar -xvjpf /mnt/cdrom/gentoo/stage3-*.tar.bz2</c>
</pre>

				<p>Jika anda mendownload file stage anda ke <path>/mnt/gentoo</path>, anda sekarang sudah 
				dapat menghapus file tersebut dengan mengetikkan <c>rm /mnt/gentoo/stage*.tar.bz2</c>.</p>
			</body>
		</section>

		<section>
			<title>Memasuki chroot</title>
			<body>
				<p>Selanjutnya, kita akan <c>chroot</c> ke sistem Gentoo Linux kita yang baru agar instalasinya 
				berjalan dengan benar.</p>

				<note>Anda mungkin menemui pesan sewaktu <c>env-update</c> bahwa 
				<path>/etc/make.profile/make.defaults</path> tidak ditemukan: cuek saja. Kita akan 
				menjalankan <c>emerge sync</c> nantinya yang akan menyelesaikan masalah ini.</note>

<pre caption="Mempersiapkan dan Memasuki Sistem chroot">
# <c>mount -t proc proc /mnt/gentoo/proc</c>
# <c>cp /etc/resolv.conf /mnt/gentoo/etc/resolv.conf</c>
# <c>chroot /mnt/gentoo /bin/bash</c>
# <c>env-update</c>
Regenerating /etc/ld.so.cache...
# <c>source /etc/profile</c>
<comment>(Perintah di atas mengarahkan shell anda ke path yang baru dan program-program yang telah ter-update.)</comment>
</pre>

				<p>Setelah anda menjalankan perintah-perintah di atas, anda akan beradai di &quot;dalam&quot; 
				sistem Gentoo Linux baru anda di <path>/mnt/gentoo</path>. Kita dapat melanjutkan proses 
				instalasi di dalam sistem chroot.</p>
			</body>
		</section>
	</chapter>

	<chapter>
		<title>Meng-update Portage Tree menggunakan sync</title>
		<section>
			<body>
				<p>Sekarang anda perlu menjalankan <c>emerge sync</c>. Perintah ini meingstruksikan portage 
				untuk mendownload isi dari Gentoo Linux Portage tree yang paling baru. Portage Tree mengandung 
				semua script (bernama ebuild) yang digunakan untuk membangun semua package di Gentoo Linux.
				Saat ini, kami memiliki script ebuild untuk hampir 4000 package (program). Setelah <c>emerge sync</c> 
				selesai, anda akan mendapatkan Portage tree yang lengkap di <path>/usr/portage</path>.</p>

<pre caption="Meng-update Menggunakan sync">
# <c>emerge sync</c>
</pre>
 
			</body>
		</section>
	</chapter>

	<chapter>
		<title>Mengeset optimisasi Gentoo (make.conf)</title>
		<section>
			<body>
				<p>Setelah anda memiliki Portage tree yang baru, sekarang saatnya untuk mengkonfigurasi 
				optimisasi dan setting saat-compile tambahan untuk digunakan pada sistem Gentoo Linux anda. 
				Portage akan menggunakan setting ini sewaktu meng-compile semua program untuk anda. 
				Edit file <path>/etc/make.conf</path>. Pada file ini, anda sebaiknya mengeset flag <c>USE</c> 
				anda, yang memberitahu portage untuk menambahkan fungsi tambahan yang tersedia pada 
				program-program pada saat dicompile; Umumnya, standardnya (<e>kosong</e> atau variabel 
				<c>USE</c> tidak diset) sudah bagus. Untuk informasi lebih lanjut mengenai flag <c>USE</c> 
				dapat ditemui di <uri link="http://www.gentoo.org/doc/en/use-howto.xml">sini</uri>. Daftar 
				lengkap dari flag USE dapat ditemui di <uri link="http://www.gentoo.org/dyn/use-index.xml">
				sini</uri>.</p>

				<p>Anda juga sebaiknya mengeset <c>CHOST</c>, <c>CFLAGS</c> dan <c>CXXFLAGS</c> 
				yang tepat bagi sistem yang akan anda buat (contoh yang ter-comment dapat ditemui di file 
				tersebut). Setting ini akan digunakan untuk memberitahu compiler C dan C++ bagaimana 
				mengoptimasi program bagi sistem anda. Sebagai contoh, biasanya user yang memakai 
				prosesor Athlon XP 	menggunakan setting "-march=athlon-xp" di variabel CFLAGS dan CXXFLAGS 
				agar program-program tercompile dengan optimisasi yang tepat bagi karakteristik CPU mereka. 
				File <path>/etc/make.conf</path> memiliki instruksi umum bagi setting CFLAGS dan CXXFLAGS 
				yang tepat.</p>

				<p>Jika perlu, anda dapat juga mengeset proxy di sini jika anda berada di belakang firewall. 
				Gunakan perintah berikut ini untuk mengubah file <path>/etc/make.conf</path> dengan 
				<c>nano</c>, sebuah editor visual sederhana.</p>

<pre caption="Mengeset make.conf">
# <c>nano -w /etc/make.conf</c>
<comment>(Edit CHOST, CFLAGS, CXXFLAGS dan setting setting proxy dan USE yang penting)</comment>
</pre>

				<note>Bagi yang benar-benar ingin mengkonfigurasi proses compile dapat melihat di file 
				<path>/etc/make.globals</path>. File ini mengandung default dari Gentoo dan sebaiknya 
				jangan pernah diubah. Jika defaultnya tidak cukup, maka nilai baru sebaiknya ditaruh di file 
				<path>/etc/make.conf</path>, sebab nilai dari <path>make.conf</path> akan <comment>
				menimpa</comment> nilai dari file <path>make.globals</path>. Jika anda tertarik untuk 
				menkustomisasi setting USE, coba liat di file <path>/etc/make.profile/make.defaults</path>. 
				Jika anda ingin menonaktifkan setting USE yang ditemukan di sana, tambahkan 
				<c>USE=&quot;-foo&quot;</c> pada file <path>/etc/make.globals</path> atau 
				<path>/etc/make.profile/make.defaults</path>.</note>

				<warn>Pastikan untuk tidak menambahkan '<c>static</c>' pada variable <c>USE</c> 
				sampai proses stage1 selesai.</warn>
			</body>
		</section>
	</chapter>

	<chapter>
		<title>Memulai dari Stage1</title>
		<section>
			<body>
				<note>Jika anda bukan memulai dari stage1, lewati bagian ini.</note>

				<p>File stage1 digunakan bagi mereka yang menginginkan optimisasi dan kustomisasi penuh. 
				Jika anda memilih file ini, anda akan memiliki sistem yang up-to-date dan teroptimisasi. 
				Bersenang-senanglah, sebab Gentoo Linux adalah distro yang penuh dengan optimisasi! 
				Instalasi dari stage1 memerlukan waktu yang lama, tetapi hasilnya adalah sistem yang teroptimisasi 
				bagi arsitektur anda dan sistem yang benar-benar anda inginkan.</p>

				<p>Sekarang saatnya untuk memulai proses &quot;bootstrap&quot;. Proses ini memerlukan 
				waktu sekitar dua jam pada sistem AMD Athlon 1200 Mhz saya. Selama proses ini, GNU C library, 
				compiler set dan program program dasar sistem yang lain akan dicompile. Mulailah bootstrap dengan 
				cara ini:</p>

<pre caption="Bootstrap">
# <c>cd /usr/portage</c>
# <c>scripts/bootstrap.sh</c>
</pre>

				<p>Proses &quot;bootstrap&quot; akan dimulai sekarang.</p>

				<note><c>bootstrap.sh</c> sekarang telah mendukung opsi <c>--fetchonly</c>. User yang
				menggunakan dial-up akan sangat terbantu dengan opsi ini. Opsi tersebut akan mendownload 
				semua file-file yang berhubungan dengan proses bootstrap untuk digunakan nantinya pada saat
				kompilasi. Ketik <c>bootstrap.sh -h</c> untuk informasi lebih lanjut.</note>

				<note>Portage secara default menggunakan <c>/var/tmp</c> selama meng-compile program, 
				sering kali menggunakan beratus-ratus megabyte tempat. Jika anda ingin mengubah tempat dimana 
				Portage menaruh semua file file temporari (sementara), set variabel PORTAGE_TMPDIR <e>sebelum</e> 
				anda memulai proses bootstrap:</note>

<pre caption="Mengubah Tempat Penyimpanan Portage">
# <c>export PORTAGE_TMPDIR=&quot;/dir_lain/tmp&quot;</c>
</pre>

				<p><c>bootstrap.sh</c> akan membangun (mengcompile) <c>binutils</c>, <c>gcc</c>, 
				<c>gettext</c>, dan <c>glibc</c>, lalu mengcompile kembali <c>gettext</c> setelah 
				<c>glibc</c>. Tidak perlu diungkit lagi, proses ini memerlukan waktu 
				yang cukup lama. Setelah proses ini selesai, sistem anda telah sama dengan sistem 
				&quot;stage2&quot; yang berarti anda sekarang dapat melanjutkan ke instruksi stage2.</p>
			</body>
		</section>
	</chapter>

	<chapter>
		<title>Memulai dari Stage2 dan melanjutkan dari Stage1</title>
		<section>
			<body>
				<note>Bab ini ditujukan bagi mereka yang melanjutkan dari instalasi stage1 atau memulai dari 
				stage2. Jika anda bukan salah satu (anda menggunakan stage3), lompati bab ini.</note>

				<warn>Jika anda memulai dari stage2, jangan mengubah variable CHOST pada
				<path>/etc/make.conf</path>. Jika anda mengubah variable tersebut akan mengakibatkan 
				kesalahan kompilasi yang sangat banyak dan aneh. Kami tentu saja sedang berusaha 
				memperbaiki bug ini.</warn>

				<p>File (tarbal) stage2 telah melakukan proses bootstrap bagi anda. Yang perlu anda lakukan 
				adalah meng-install sisa dari sistem anda.</p>

				<note>Jika anda memulai dari stage2 yang belum dibangun (dicompile) dan ingin memastikan 
				bahwa toolchain compiler anda benar-benar up-to-date, tambahkan opsi <c>-u</c> pada 
				perintah di bawah. Jika anda tidak tahu apa yang dimaksud pada kalimat sebelumnya, lebih aman 
				untuk tidak melakukannya.</note>

<pre caption="Meng-install Sisa Sistem">
# <c>emerge -p system</c>
<comment>(menampilkan semua package (program) yang akan diinstall)</comment>
# <c>emerge system</c>
</pre>

				<p>Proses ini memerlukan waktu yang cukup lama untuk selesai meng-compile semua sistem 
				dasar anda. Sebagai gantinya, anda perlu mencari sesuatu untuk dilakukan selama proses ini 
				berlangsung. Penulis menyarankan bermain &quot;Star Wars - Super Bombad Racing&quot; 
				di PS2</p>

				<p>Proses ini sekarang telah selesai. Lanjutkan dan lewati bab di bawah ini sampai bagian 
				"Mengeset Time Zone Anda"</p>
			</body>
		</section>
	</chapter>

	<chapter>
		<title>Memulai dari Stage3</title>
		<section>
			<body>
				<note>Bab ini ditujukan bagi mereka yang <b>memulai</b> dari stage3, dan bukan bagi 
				mereka yang telah memulai dari stage1 atau stage2 yang seharusnya melewati bab ini.</note>

				<warn>Jika anda memulai dari stage3, jangan mengubah variable CHOST pada
				<path>/etc/make.conf</path>. Jika anda mengubah variable tersebut akan mengakibatkan 
				kesalahan kompilasi yang sangat banyak dan aneh. Kami tentu saja sedang berusaha 
				memperbaiki bug ini.</warn>

				<p>File (tarbal) stage3 menyediakan sistem dasar Gento yang siap pakai, jadi segala proses 
				compile tidak diperlukan. Bagaimanapun juga, karena file (tarbal) stage3 pre-built, mungkin 
				telah out-of-date. Jika anda perduli, anda dapat meng-update secara otomatis seluruh program 
				(package) stage3 anda dengan versi yang paling up-to-date dengan melakukan beberapa 
				langkah. Catat bahwa proses ini akan memakan waktu yang lama jika file stage3 anda sudah 
				lama; Sebaliknya, proses ini akan cepat dan akan memberikan segala update Gentoo. 
				Tergantung terhadap anda kembali, anda boleh saja melewati bagian ini dan melanjutkan ke 
				bab berikutnya jika anda mau.</p>
		
<pre caption="Mendapatkan sistem yang up-to-date">
# <c>export CONFIG_PROTECT="-* /etc/make.conf"</c>
# <c>emerge -up system</c>
<comment>(Menampilkan semua package yang akan diinstall)</comment>
# <c>emerge -u system</c>
<comment>(benar-benar mengupdate program)</comment>
# <c>unset CONFIG_PROTECT</c>
</pre>
			</body>
		</section>
	</chapter>

	<chapter>
		<title>Mengeset Time Zone Anda</title>
		<section>
			<body>
				<p>Sekarang saatnya anda mengeset time zone anda.</p>
				<p>Carilah time zone anda (atau GMT kalau anda menggunakannya) di 
				<path>/usr/share/zoneinfo</path>. Lalu, buatlah symbolic link ke /etc/localtime dengan 
				mengetikkan:</p>

<pre caption="Membuat symbolic link bagi time zone anda">
# <c>ln -sf /usr/share/zoneinfo/path/to/timezonefile /etc/localtime</c>
</pre>
			</body>
		</section>
	</chapter>

	<chapter>
		<title>Meng-install kernel dan System Logger</title>
		<section>
			<body>
				<note>Editlah file <path>/etc/make.conf</path> seperti yang anda mau, jika anda belum 
				melakukannya.</note>

				<p>Sekarang anda perlu me-merge source kernel linux. Gentoo menyediakan beberapa ebuild
				kernel; Daftar kernel tersebut dapat ditemui di <uri link="/doc/en/gentoo-kernel.xml">sini</uri>.
				Jika anda tidak tau ingin memilih source kernel yang mana, kami merekomendasikan 
				<c>gentoo-sources</c> atau <c>vanilla-sources</c>. Jika anda menginginkan dukungan XFS,
				pergunakan <c>xfs-sources</c>. Sebagai info, LiveCD Gentoo menggunakan <c>xfs-sources</c>.
				</p>

				<warn>Jika anda mengkonfigurasi kernel anda sendiri, hati hati dengan opsi <i>grsecurity</i>. 
				Jika anda mengeset security anda terlalu agresif dapat mengakibatkan beberapa program 
				(seperti X) tidak berjalan dengan semestinya. Jika anda ragu, biarkan saja kosong.</warn>

				<p>Pilihlah sebuah kernel dan merge dengan cara berikut ini:</p>

<pre caption="Emerge Kernel Sources">
# <c>emerge sys-kernel/gentoo-sources</c>
</pre>

				<p>Setelah anda memiliki source kernel Linux, sekarang saatnya mengcompile kernel anda.</p>

				<p>Perhatikan bahwa <path>/usr/src/linux</path> adalah symlink ke direktori source kernel 
				anda yang barusan ter-merge, dan diset secara otomatis oleh Portage pada saat emerge. 
				Jika anda memiliki beberapa package source kernel, sebaiknya mengeset symlink 
				<path>/usr/src/linux</path> ke direktori yang benar sebelum melanjutkan.</p>

				<note>Jika anda ingin mempergunakan konfigurasi yang sama dengan kernel LiveCD atau 
				mendasarkan konfigurasi anda pada konfigurasi LiveCD, ketikkan 
				<c>cd /usr/src/linux &amp;&amp; cat /proc/config > .config &amp;&amp; make oldconfig</c>.
				Jika anda tidak menggunakan <c>xfs-sources</c>, perintah tersebut akan menanyakan beberapa 
				pertanyaan tentang perbedaan antara pilihankernel anda dan <c>xfs-sources</c>.</note>

<pre caption="Mengkonfigurasi Kernel Linux">
# <c>cd /usr/src/linux</c>
# <c>make menuconfig</c>
</pre>

				<warn>Agar kernel anda berfungsi dengan benar, ada beberapa opsi yang perlu anda pastikan 
				telah berada pada kernel -- yaitu, opsi opsi tersebut harus <i>diaktifkan dan bukan di-compile 
				sebagai modul</i>. Pastikan untuk mengaktifkan &quot;ReiserFS&quot; jika anda memiliki 
				partisi ReiserFS; hal yang sama berlaku bagi &quot;Ext3&quot;. Jika anda menggunakan XFS, 
				aktifkan opsi &quot;SGI XFS filesystem support&quot;. Sebaiknya anda tetap mengaktifkan opsi 
				ext2 baik anda menggunakannya atau tidak. Di bawah ini adalah opsi opsi umum yang anda 
				perlukan:</warn>

<pre caption="opsi make menuconfig">
Code maturity level options ---&gt;
  [*] Prompt for development and/or incomplete code/drivers&quot;
<comment>(Anda memerlukan ini untuk mengaktifkan beberapa opsi di bawah.)</comment>
     ...

File systems ---&gt;
  &lt;*&gt; Reiserfs support
<comment>(Hanya diperlukan jika anda menggunakan ReiserFS.)</comment>
       ... 
  &lt;*&gt; Ext3 journalling file system support
<comment>(Hanya diperlukan jika anda menggunakan ext3.)</comment>
       ...
  [*] Virtual memory file system support (former shm fs)
<comment>(Diperlukan oleh Gentoo Linux.)</comment>
       ...
  &lt;*&gt; JFS filesystem support
<comment>(Hanya diperlukan jika anda menggunakan JFS.)</comment>
       ...
  [*] /proc file system support
<comment>(Diperlukan oleh Gentoo Linux.)</comment>
  [*] /dev file system support (EXPERIMENTAL)
  [*]   Automatically mount at boot          
<comment>(Diperlukan oleh Gentoo Linux.)</comment>
  [ ] /dev/pts file system for Unix98 PTYs
<comment>(Nonaktifkan ini, opsi ini tidak diperlukan.)</comment>
       ...
  &lt;*&gt; Second extended fs support
<comment>(Hanya diperlukan jika anda menggunakan ext2.)</comment>
       ...
  &lt;*&gt; XFS filesystem support
<comment>(Hanya diperlukan jika anda menggunakan XFS.)</comment>
</pre>

				<p>Jika anda menggunakan PPPoE untuk koneksi ke internet, anda memerlukan beberapa opsi 
				berikut ini pada kernel (built-in atau ter-compile sebagai modul): &quot;PPP (point-to-point protocol) 
				support&quot;, &quot;PPP support for async serial ports&quot;, &quot;PPP support for sync tty ports&quot;. 
				Kedua opsi kompresi tidak akan merusak tetapi tidak benar benar diperlukan, opsi &quot;PPP over 
				Ethernet&quot; juga tidak diperlukan, opsi tersebut mungkin hanya akan digunakan oleh 
				<i>rp-pppoe</i> saat terkonfigurasi untuk melakukan PPPoE bermodus kernel.</p>

				<p>Jika anda memiliki cd burner IDE, maka anda perlu mengaktifkan SCSI emulation di kernel. 
				Aktifkan &quot;ATA/IDE/MFM/RLL support&quot; ---&gt; &quot;IDE, ATA and ATAPI Block devices&quot; 
				---&gt; &quot;SCSI emulation support&quot; (saya biasanya membuat sebagai modul), lalu pada 
				&quot;SCSI support&quot; aktifkan &quot;SCSI support&quot;, &quot;SCSI CD-ROM support&quot; dan 
				&quot;SCSI generic support&quot; (lagi, saya biasanya meng-compile sebagai modul). Jika anda 
				memilih untuk meng-compile sebagai modul juga, ketikkan <c>echo -e &quot;ide-scsi\nsg\nsr_mod&quot; 
				&gt;&gt; /etc/modules.autoload</c> agar modul modul tersebut diload secara otomatis pada saat 
				boot.</p>

				<p>Jika anda membutuhkan dukungan ethernet, jangan lupa untuk menambahkan dukungan 
				ethernet card anda pada kernel.</p>

				<note>Sekarang telah dimungkinkan untuk meng-install Gentoo Linux dengan kernel 2.2. Tetapi, 
				menggunakan kernel ini memiliki kekurangan: anda tidak akan dapat menggunakan feature feature 
				bagus yang ditemui di kernel 2.4 (seperti filesystem XFS dan tmpfs, iptables, dll), walaupun kernel 
				2.2 dapat di-patch dengan dukungan ReiserFS dan devfs. Script boot Gentoo Linux memerlukan 
				salah satu dukungan tmpfs atau ramdisk pada kernel, jadi user yang menggunakan kernel 2.2 harus 
				memastikan bahwa ramdisk support ter-compile pada kernel (bukan sebagai modul). Sangat 
				<comment>penting</comment> untuk menambahkan flag <e>gentoo=notmpfs</e> pada 
				baris kernel di <path>/boot/grub/grub.conf</path> atau menambahkan baris di 
				<path>/etc/lilo.conf</path> untuk kernel 2.2 agar ramdisk ter-mount bagi script boot daripada 
				tmpfs. Jika anda memilih untuk tidak menggunakan devfs, maka <e>gentoo=notmpfs,nodevfs</e> 
				harus digunakan.</note>

<pre caption = "Meng-compile dan Meng-install kernel">
# <c>make dep &amp;&amp; make clean bzImage modules modules_install</c>
# <c>cp /usr/src/linux/arch/i386/boot/bzImage /boot</c>
</pre>

				<p>Kernel anda (dan modul) telah terinstall sekarang. Sekarang anda perlu memilih system 
				logger yang akan di-install. Kami menawarkan sysklogd, yaitu system logging daemon tradisional. 
				Kami juga memiliki msyslog dan syslog-ng serta metalog. User user expert tampaknya mulai 
				berpindah dari sysklogd (tidak begitu bagus performanya) ke alternatif yang lebih baru. Jika anda 
				ragu, anda mungkin ingin mencoba metalog, sebab logger ini tampaknya cukup populer. Untuk 
				me-merge logger pilihan anda, ketik <e>satu</e> dari 4 baris berikut ini:</p>

<pre caption="Emerge System Logger">
# <c>emerge app-admin/sysklogd</c>
# <c>rc-update add sysklogd default</c>
<comment>atau</comment>
# <c>emerge app-admin/syslog-ng</c>
# <c>rc-update add syslog-ng default</c>
<comment>atau</comment>
# <c>emerge app-admin/metalog</c>
# <c>rc-update add metalog default</c>
<comment>atau</comment>
# <c>emerge app-admin/msyslog</c>
# <c>rc-update add msyslog default</c>
</pre>

				<impo>Metalog mem-flush output ke disk dalam hitungan blok, jadi pesan pesan tidak langsung 
				tersimpan ke log system. Jika anda ingin men-debug sebuah daemon, performa tambahan ini 
				tidak menguntungkan. Pada saat sistem Gentoo Linux anda telah siap dan berjalan, anda dapat 
				mengirimkan signal USR1 ke metalog untuk menonatifkan sementara fungsi message buffering ini 
				(artinya <i>tail -f <path>/var/log/everything/current</path></i> telah dapat berfungsi real time, 
				seperti yang diharapkan), dan signal USR2 untuk mengaktifkan buffering kembali. Jika anda ingin 
				menonaktifkan buffering secara permanen, anda dapat mengubah opsi METALOG_OPTS="-B" 
				menjadi METALOG_OPTS="-B -s" di <path>/etc/conf.d/metalog</path>.</impo>

<pre caption="Me(non)aktifkan buffering metalog">
<codenote>Menonaktifkan buffering:</codenote>
# <c>killall -USR1 metalog</c>
<codenote>Mengaktifkan buffering kembali:</codenote>
# <c>killall -USR2 metalog</c>
</pre>

				<p>Sekarang, anda mungkin mau memilih package cron yang ingin anda gunakan. Saat ini, 
				kami menawarkan dcron, fcron dan vcron. Jika anda tidak tau ingiin memilih yang mana, anda 
				mungkin mau mencoba vcron. Package ini dapat di-install dengan cara:</p>

<pre caption="Memilih CRON Daemon">
# <c>emerge sys-apps/dcron</c>
# <c>rc-update add dcron default</c>
# <c>crontab /etc/crontab</c>
<comment>atau</comment>
# <c>emerge sys-apps/fcron</c>
# <c>rc-update add fcron default</c>
# <c>crontab /etc/crontab</c>
<comment>atau</comment>
# <c>emerge sys-apps/vcron</c>
# <c>rc-update add vcron default</c>
<comment>Anda tidak perlu menjalankan <c>crontab /etc/crontab</c> jika menggunakan vcron.</comment>
</pre>

				<p>Untuk informasi lebih lanjut dalam menjalankan program dan daemon pada saat startup, 
				baca <uri link="/doc/en/rc-scripts.xml">rc-script guide</uri>.</p>
			</body>
		</section>
	</chapter>

	<chapter>
		<title>Menginstall package tambahan yang penting</title>
		<section>
			<body>
				<p>Jika anda memerlukan rp-pppoe untuk koneksi ke internet, sebagai informasi, pada saat 
				ini package tersebut belum di-install. Sekarang saat yang tepat untuk menginstallnya.</p>

<pre caption="Menginstall rp-pppoe">
# <c>USE="-X" emerge rp-pppoe</c>
</pre>

				<note><i>USE="-X"</i> mencegah pppoe untuk menginstall opsi interface X, karena X 
				dan segala dependency nya akan ikut ter-merge. Anda selalu dapat meng-compile kembali 
				<i>rp-pppoe</i> dengan dukungan X nantinya.</note>

				<note>Perhatikan bahwa rp-pppoe telah ter-merge tetapi belum terkonfigurasi. Anda harus 
				melakukannya kembali menggunakan <c>adsl-setup</c> pada saat anda boot ke sistem 
				Gento pertama kali.	</note>

				<p>Anda mungkin perlu menginstall package tambahan di Portage tree jika anda menggunakan 
				beberapa feature tambahan seperti XFS, ReiserFS atau LVM. Jika anda menggunakan XFS, anda 
				sebaiknya meng-emerge package <c>xfsprogs</c>:</p>

<pre caption="Emerge Tool Filesystem">
# <c>emerge sys-apps/xfsprogs</c>
<comment>Jika anda menggunakan ReiserFS, anda sebaiknya meng-emerge tool ReiserFS:</comment>
# <c>emerge sys-apps/reiserfsprogs</c>
<comment>Jika anda menggunakan JFS, anda sebaiknya meng-emerge tool JFS: </comment>
# <c>emerge jfsutils</c>
<comment>Jika anda menggunakan LVM, anda sebaiknya meng-emerge package <c>lvm-user</c>: </comment>
# <c>emerge sys-apps/lvm-user</c>
</pre>

				<p>Jika anda adalah user yang menggunakan laptop dan ingin menggunakan slot PCMCIA anda 
				pada saat reboot pertama kali, sebaiknya anda pastikan untuk menginstall package 
				<i>pcmcia-cs</i>.</p>

<pre caption="Emerge PCMCIA-cs">
# <c>emerge sys-apps/pcmcia-cs</c>
</pre>

				<warn>Anda perlu untuk re-emerge <i>pcmcia-cs</i> setelah instalasi agar PCMCIA dapat berfungsi.
				</warn>
			</body>
		</section>
	</chapter>

	<chapter>
		<title>Meng-edit /etc/fstab bagi sistem anda</title>
		<section>
			<body>
				<p>Sistem Gentoo Linux anda sudah hampir dapat dipakai. Yang kita perlukan adalah konfigurasi 
				beberapa file system penting dan meng-install boot loader. File pertama yang perlu kita konfigurasi 
				adalah <path>/etc/fstab</path>. Ingat bahwa anda memerlukan opsi <c>notail</c> bagi partisi 
				boot anda jika anda memilih membuat filesystem ReiserFS pada partisi tersebut. Ingat untuk 
				menuliskan tipe filesystem <c>ext2</c>, <c>ext3</c> atau <c>reiserfs</c> yang tepat.</p>

				<p>Gunakan contoh <path>/etc/fstab</path> seperti di bawah ini, tetapi tentu saja anda harus 
				mengganti &quot;BOOT&quot;, &quot;ROOT&quot; dan &quot;SWAP&quot; dengan block device 
				yang anda gunakan sebenarnya (seperti <c>hda1</c>, dll)</p>

<pre caption="Edit fstab">
<comment># /etc/fstab: static file system information.
#
# noatime turns off atimes for increased performance (atimes normally aren't
# needed; notail increases performance of ReiserFS (at the expense of storage
# efficiency).  It is safe to drop the noatime options if you want and to 
# switch between notail and tail freely.

# &lt;fs&gt;           &lt;mount point&gt;   &lt;type&gt;   &lt;opts&gt;          &lt;dump/pass&gt;

# NOTE: If your BOOT partition is ReiserFS, add the notail option to opts.
</comment>
/dev/BOOT           /boot       ext2	 noauto,noatime	 1 2
/dev/ROOT           /           ext3	 noatime         0 1
/dev/SWAP           none        swap	 sw              0 0
/dev/cdroms/cdrom0  /mnt/cdrom  iso9660	 noauto,ro       0 0
proc                /proc       proc	 defaults        0 0
</pre>

				<warn>Perhatikan bahwa <i>/boot</i> TIDAK termount saat boot. Hal ini bertujuan untuk 
				melindungi data di <i>/boot</i> dari kerusakan. Jika anda ingin mengakses <i>/boot</i>, 
				mountlah partisi tersebut!</warn>
			</body>
		</section>
	</chapter>

	<chapter>
		<title>Manajemen User</title>
		<section>
			<title>Set Password Root</title>
			<body>
				<p>Sebelum anda lupa, setlah password root dengan mengetikkan:</p>

<pre caption="Set Password root">
# <c>passwd</c>
</pre>

			</body>
		</section>

		<section>
			<title>Menambah user untuk penggunaan sehari-hari</title>
			<body>
				<p>Bekerja sebagai root pada sistem Unix/Linux sangat <e>berbahaya</e> dan sebaiknya 
				dihindari sebisa mungkin. Karena itu, kami <e>sangat</e> merekomendasikan untuk
				menambahkan satu user untuk penggunaan sehari-hari.</p>

<pre caption = "Menambah user">
# <i>useradd user_anda -m -G users,wheel,audio -s /bin/bash</i>
# <i>passwd user_anda</i></pre>

				<p>Tentu saja, gantikan <c>user_anda</c> dengan username anda.</p>

				<p>Kapanpun anda ingin melakukan perintah yang hanya dapat dilakukan root, gunakan 
				<c>su -</c> untuk mengubah privilege anda menjadi root-privilege, atau cobalah melihat 
				package <c>sudo</c>.</p>
			</body>
		</section>
	</chapter>

	<chapter>
		<title>Mengeset Hostname anda</title>
		<section>
			<body>
				<p>Edit file <path>/etc/hostname</path> agar file tersebut mengandung hostname anda dalam 
				satu baris, misalnya <c>mymachine</c>.</p>

<pre caption="Mengkonfigurasi Hostname">
# <i>echo mymachine &gt; /etc/hostname</i>
</pre>

				<p>Lalu edit file <path>/etc/dnsdomainname</path> agar file tersebut mengandung nama 
				domain DNS anda, misalnya <c>mydomain.com</c>.</p>

<pre caption="Mengkonfigurasi Nama Domain">
# <i>echo mydomain.com &gt; /etc/dnsdomainname</i>
</pre>

				<p>Jika anda memiliki domain NIS, setlah di file <path>/etc/nisdomainname</path>.</p>

<pre caption="Mengkonfigurasi Nama Domain NIS">
# <i>echo nis.mydomain.com &gt; /etc/nisdomainname</i>
</pre>
			</body>
		</section>
	</chapter>

	<chapter>
		<title>Mengubah /etc/hosts</title>
		<section>
			<body>
				<p>File ini mengandung daftar alamat IP dan hostname mereka. File ini digunakan oleh 
				sistem untuk me-resolve alamat IP dari semua hostname yang mungkin tidak berada pada 
				nameserver anda. Berikut ini adalah kerangka bagi file ini:</p>

<pre caption="Kerangka Hosts">
127.0.0.1      localhost
<comment># Baris berikutnya mengandung IP bagi LAN anda dan nama mesin mereka.</comment>
192.168.1.1    mymachine.mydomain.com	mymachine
</pre>

				<note>Jika anda berada pada jaringan DHCP, mungkin akan membantu jika anda menambahkan 
				hostname mesin anda yang sebenarnya setelah <i>localhost</i>. Hal ini akan membantu GNOME 
				dan beberapa program lain dalam resolusi nama.</note>
			</body>
		</section>
	</chapter>

	<chapter>
		<title>Konfigurasi Jaringan Terakhir</title>
		<section>
			<body>
				<p>Tambahkan modul modul yang penting bagi sistem anda untuk berfungsi dengan benar 
				di file <path>/etc/modules.autoload</path> (anda dapat juga menambahkan opsi yang anda 
				perlukan pada baris yang sama). Pada saat Gentoo Linux boot, modul modul ini akan di-load 
				secara otomatis. Yang paling penting adalah modul ethernet card anda, jika anda meng-compile 
				sebagai modul:</p>

<pre caption="/etc/modules.autoload">
<comment>Anggap anda menggunakan 3com card. 
Periksa file <path>/lib/modules/`uname -r`/kernel/drivers/net</path> untuk card anda. </comment>
3c59x
</pre>

				<p>Edit script <path>/etc/conf.d/net</path> agar jaringan anda terkonfigurasi pada saat 
				boot pertama kali:</p>

<pre caption="Konfigurasi Jaringan pada saat boot">
# <c>nano -w /etc/conf.d/net</c>
# <c>rc-update add net.eth0 default</c>
</pre>

				<p>Jika anda memiliki beberapa network card atau interface tokenring, anda memerlukan script 
				<path>net.eth<comment>x</comment></path> atau <path>net.tr<comment>x</comment>
				</path> tambahan berturut-turut untuk setiap network card (<comment>x</comment> = 1, 2, ...):
				</p>

<pre caption="Beberapa Interface Jaringan">
# <c>cd /etc/init.d</c>
# <c>cp net.eth0 net.eth<comment>x</comment></c>
# <c>rc-update add net.eth<comment>x</comment> default</c>
</pre>

				<p>Jika anda memiliki PCMCIA card terinstall, periksa file <path>/etc/init.d/pcmcia</path> untuk 
				memastikan bahwa segala sesuatunya telah tepat bagi anda, lalu tambahkan baris ini di atas file 
				<path>/etc/init.d/net.ethx</path>:</p>

<pre caption="PCMCIA depend di /etc/init.d/net.ethx">
depend() {
	need pcmcia
}
</pre>

				<p>Hal ini memastikan driver PCMCIA di load secara otomatis pada saat jaringan anda terload.</p>
			</body>
		</section>
	</chapter>

	<chapter>
		<title>Langkah terakhir: Konfigurasi setting dasar (termasuk setting keymap internasional)</title>
		<section>
			<body>

<pre caption="Konfigurasi Dasar">
# <c>nano -w /etc/rc.conf</c>
</pre>

				<p>Ikut langkah-langkah pada file tersebut untuk mengkonfigurasi setting dasar. Semua user 
				sebaiknya memastikan <c>CLOCK</c> telah ter-set seperti yang mereka mau. User yang 
				menggunakan keyboard internasional sebaiknya mengeset variabel <c>KEYMAP</c> (buka 
				file <path>/usr/share/keymaps</path> untuk melihat segala keymap yang tersedia).</p>
			</body>
		</section>
	</chapter>

	<chapter>
		<title>Konfigurasi Bootloader</title>
		<section>
			<title>Catatan</title>
			<body>
				<p>Dalam semangat Gentoo, sekarang user memiliki pilihan lebih dari satu bootloader. 
				Menggunakan sistem package virtual kami, user sekarang dapat memilih antara GRUB dan 
				LILO sebagai bootloader mereka.</p>

				<p>Perlu anda ingat bahwa anda tidak perlu untuk menginstall kedua bootloader. Sebenarnya, 
				memiliki dua bootlader dapat menjadi penyakit, jadi sebaiknya anda menggunakan satu saja.</p>

				<impo>Jika anda menginstall Gentoo Linux pada sistem dengan chipset NVIDIA nForce atau 
				nForce2 dengan graphic card GeForce yang terintegrasi, anda perlu menggunakan LILO dan 
				hindari GRUB. Dengan video on-board aktif, daerah low memory dari RAM anda dapat digunakan 
				sebagai RAM video. Karena GRUB juga menggunakan low memory pada saat boot, GRUB mungkin 
				akan menemui status "out of memory". Jadi, jika anda memiliki nForce atau motherboard lain 
				dengan video on-board, gunakan LILO. Walaupun anda menggunakan video off-board sekarang, 
				lebih bagus jika anda dapat menggunakan video on-board, bukan?</impo>

				<p>Mereka yang telah memilih dukungan framebuffer pada kernel mereka sebaiknya menambahkan 
				opsi <c>vga=xxx</c> pada file konfigurasi bootloader mereka sebagai parameter kernel. <c>xxx</c> 
				adalah salah satu dari nilai pada tabel berikut: </p>

				<table>
					<tr><ti></ti><th>640x480</th><th>800x600</th><th>1024x768</th><th>1280x1024</th></tr>
					<tr><th>8 bpp</th><ti>769</ti><ti>771</ti><ti>773</ti><ti>775</ti></tr>
					<tr><th>16 bpp</th><ti>785</ti><ti>788</ti><ti>791</ti><ti>794</ti></tr>
					<tr><th>32 bpp</th><ti>786</ti><ti>789</ti><ti>792</ti><ti>795</ti></tr>
				</table>
			</body>
		</section>

		<section>
			<title>Mengkonfigurasi GRUB</title>
			<body>
				<p>Yang paling penting dalam memahami GRUB adalah terbiasa dengan bagaimana GRUB 
				menangani hard drive dan partisi. Partisi Linux <path>/dev/hda1</path> anda dikenal sebagai 
				<path>(hd0,0)</path> pada GRUB. Perhatikan tanda kurung di hd0,0 diperlukan. Hard drive 
				dihitung mulai dari nol daripada &quot;a&quot;, dan partisi dimulai dari nol daripada satu. 
				Perhatikan juga bahwa pada hd device, hanya harddrive yang dihitung, bukan device atapi-ide 
				seperti cdrom, cd-burner, dan hal yang sama berlaku juga bagi drive SCSI. (Biasanya drive 
				SCSI memiliki nomor yang lebih tinggi daripada drive IDE kecuali kalau bios di konfigurasi untuk 
				boot dari device SCSI). Anggap anda memiliki harddrive pada /dev/hda, dan sebuah cdrom pada 
				/dev/hdb, cd-burner pada /dev/hdc, harddrive kedua pada /dev/hdd dan tidak ada harddrive SCSI, 
				maka <path>/dev/hdd7</path> dikenal sebagai <path>(hd1,6)</path>. Memang kedengarannya 
				membingungkan, tetapi jika kita lihat, grub menawarkan mekanisme tab completion yang sangat 
				membantu bagi mereka yang memiliki banyak harddrive dan partisi serta bagi mereka yang agak 
				bingung dengan skema penomoran grub. Setelah anda terbiasa dengan GRUB, sekarang saatnya 
				menginstall GRUB.</p>

				<p>Cara paling mudah untuk menginstall GRUB adalah dengan mengetikkan <c>grub</c> pada 
				shell chroot anda:</p>

<pre caption="Menginstall GRUB">
# <c>emerge grub</c>
# <c>grub</c>
</pre>

				<p>Anda akan ditampilkan dengan prompt <c>grub&gt;</c>. Sekarang, anda perlu mengetikkan 
				perintah perintah yang benar untuk menginstall boot record GRUB pada harddrive anda. Pada 
				contoh konfigurasi saya, saya ingin menginstall GRUB pada MBR (Master Boot Record) harddrive 
				saya, jadi hal pertama yang saya lihat ketika menghidupkan komputer adalah prompt GRUB. 
				Dalam hal ini, perintah perintah yang saya ketik adalah:</p>

<pre caption="GRUB pada MBR">
grub&gt; <c>root (hd0,0)</c> <codenote>Partisi boot anda</codenote>
grub&gt; <c>setup (hd0)</c> <codenote>Dimana boot record ter-install, di sini, adalah MBR</codenote>
</pre>
	
<pre caption="GRUB bukan pada MBR">
<comment>Cara lain, jika anda ingin menginstall bootloader di tempat lain selain MBR</comment>
grub&gt; <c>root (hd0,0)</c> <codenote>Partisi boot anda</codenote>
grub&gt; <c>setup (hd0,4)</c> <codenote>Dimana boot record ter-install, di sini, adalah /dev/hda5</codenote>
grub&gt; <c>quit</c>
</pre>
        
				<p>Berikut ini adalah penjelasan bagaimana perintah tersebut bekerja. Perintah <c>root ( )</c> 
				pertama memberitahu GRUB lokasi dari partisi boot anda (dalam contoh kita, <path>/dev/hda1</path> 
				atau <path>(hd0,0)</path> dalam penomoran GRUB). Lalu, perintah <c>setup ( )</c> kedua 
				memberitahu GRUB tempat dimana akan diinstall boot record - GRUB akan dikonfigurasi untuk 
				mencari file khusus pada lokasi <c>root ( )</c> yang anda berikan. Dalam contoh saya, saya ingin 
				boot loader ditempatkan pada MBR, jadi saya mengetikkan <path>/dev/hda</path> (atau 
				<path>(hd0)</path>). Jika saya ingin menggunakan boot loader lain dan menggunakan GRUB sebagai 
				boot loader kedua (secondary), saya dapat menginstall GRUB pada boot record di lokasi lain. Dalam 
				hal ini, saya akan menuliskan lokasi partisi tersebut daripada menggunakan seluruh disk. Setelah boot 
				record GRUB selesai di-install, anda dapat mengetikkan <c>quit</c> untuk keluar dari GRUB.</p>

				<note>Mekanisme tab completion GRUB dapat digunakan di dalam grub, anggap anda menulis 
				<c>root (</c> dan jika anda menekan tombol TAB, anda akan ditampilkan dengan daftar device 
				yang tersedia (bukan hanya hardddrive); Jika anda menekan tombol TAB setelah menulis <c>root 
				(hd</c>, anda akan ditampilkan dengan harddrive yang tersedia; dan jika anda menekan tombol 
				TAB setelah menulis <c>root (hd0,</c> , GRUB akan menampilkan daftar partisi dari harddrive 
				pertama. Memeriksa syntax dari lokasi GRUB dengan TAB completion sangat membantu dalam 
				membuat pilihan yang tepat.</note>

				<p>Gentoo Linux sekarang telah terinstall, tetapi kita perlu membuat file <path>/boot/grub/grub.conf
				</path>, agar kita mendapatkan boot menu GRUB yang bagus sewaktu sistem kita reboot. Berikut 
				ini adalah cara melakukannya.</p>

				<impo>Untuk memastikan kompatibilitas dengan GRUB versi lama, pastikan untuk membuat link 
				dari <i>grub.conf</i> ke <i>menu.lst</i>. Anda dapat melakukannya dengan <c>ln -s 
				/boot/grub/grub.conf /boot/grub/menu.lst </c>.</impo>

				<p>Sekarang, buat file grub.conf (<c>nano -w /boot/grub/grub.conf</c>), dan tambahkan:</p>

<pre caption="Grub.conf untuk GRUB">
default 0
timeout 30
splashimage=(hd0,0)/boot/grub/splash.xpm.gz

title=My example Gentoo Linux
root (hd0,0) 
kernel (hd0,0)/boot/bzImage root=/dev/hda3 

<comment># Berikut ini hanya diperlukan bagi mereka yang menggunakan dual-boot</comment>
title=Windows XP
root (hd0,5) 
chainloader (hd0,5)+1
</pre>

				<note>(hd0,0) harus ditulis tanpa spasi di dalam tanda kurung.</note>

				<impo>Jika anda memilih SCSI emulation bagi cd burner IDE sebelumnya, maka untuk membuat 
				cd burner tersebut bekerja anda perlu menambahkan &quot;hdx=ide-scsi&quot; pada baris kernel 
				di grub.conf (dimana &quot;hdx&quot; adalah device dari cd burner anda).</impo>

				<p>Setelah menyimpan file ini, instalasi Gentoo Linux telah selesai. Memilih opsi pertama akan 
				memberitau GRUB untuk boot ke Gentoo Linux langsung. Bagian kedua dari grub.conf adalah 
				pilihan (anda boleh tidak membuatnya) untuk memperlihatkan anda cara menggunakan GRUB 
				untuk boot ke partisi windows.	</p>

				<note>Di atas, <path>(hd0,0)</path> harus mengarah ke partisi &quot;boot&quot; anda 
				(<path>/dev/hda1</path> dalam contoh konfigurasi kita) dan <path>/dev/hda3</path> 
				harus mengarah ke filesystem/partisi root anda. <path>(hd0,5)</path> mengandung boot 
				loader NT.</note>

				<note>Path (direktori) ke imej kernel tergantung pada partisi boot. Misalnya, jika anda memiliki partisi 
				boot <path>(hd0,0)</path> dan partisi root <path>(hd0,1)</path> yang terpisah, semua path di 
				file grub.conf akan menjadi <path>/bzImage</path>.</note>

				<p>Jika anda perlu memberikan opsi tambahan pada kernel, tambahkan saja pada akhir perintah 
				<c>kernel</c>. Kita telah memberikan satu opsi (<c>root=/dev/hda3</c>), tetapi anda tetap 
				dapat memberikan opsi yang lain juga. Terutama, anda dapat menonaktifkan devfs secara default 
				(tidak direkomendasikan kecuali anda tau apa yang anda lakukan) dengan menambahkan opsi 
				<c>gentoo=nodevfs</c> pada perintah <c>kernel</c>.</p>

				<note>Tidak seperti versi Gentoo Linux sebelumnya, anda tidak lagi perlu menambahkan opsi 
				<c>devfs=mount</c> pada akhir baris <c>kernel</c> untuk mengaktifkan devfs. Pada rc6, 
				devfs diaktifkan secara default.</note>
			</body>
		</section>

		<section>
			<title>Mengkonfigurasi LILO</title>
			<body>
				<p>Walaupun GRUB adalah alternatif baru bagi kebanyakan orang, GRUB bukanlah selalu pilihan 
				terbaik. LILO, LInuxLOader, adalah boot loader linux asli yang telah terpercaya. Berikut ini adalah 
				cara menginstall LILO jika anda memilih menggunakannya daripada GRUB:</p>

				<p>Langkah pertama adalah emerge LILO:</p>

<pre caption="Emerge LILO">
# <c>emerge lilo</c>
</pre>

				<p>Sekarang saatnya mengkonfigurasi LILO. Berikut ini adalah contoh konfigurasi file 
				<path>/etc/lilo.conf</path></p>

<pre caption="Contoh lilo.conf">
boot=/dev/hda
map=/boot/map
install=/boot/boot.b
prompt
timeout=50
lba32
default=linux

image=/boot/bzImage
	label=linux
	read-only
	root=/dev/hda3
	
# Bagi dual boot windows/OS lain
other=/dev/hda1
	label=dos
</pre>

				<ul>
					<li><i>boot=/dev/hda</i> memberitahu LILO untuk diinstall pada hard disk pertama pada 
					controller IDE pertama.</li>
					<li><i>map=/boot/map</i> menunjukkan file map. Dalam pemakaian normal, opsi ini 
					sebaiknya jangan diubah.</li>
					<li><i>install=/boot/boot.b</i> memberitahu LILO untuk menginstall file tersebut sebagai boot 
					sector baru. Dalam pemakaian normal, opsi ini sebaiknya jangan diubah. Jika baris install hilang, 
					LILO akan menganggap defaultnya sebagai /boot/boot.b untuk digunakan.</li>
					<li>Baris <i>prompt</i> memberitahu LILO untuk menampilkan prompt <i>lilo:</i> pada saat 
					boot. Walaupun tidak direkomendasikan untuk membuang baris prompt ini, jika anda 
					membuang baris tersebut, anda tetap dapat menemui prompt dengan menahan tombol [Shift] 
					pada saat mesin anda mulai boot.</li>
					<li><i>timeout=50</i> mengeset waktu tunggu LILO terhadap input dari user sebelum melanjutkan 
					dengan mem-boot baris default. Waktu ini diukur dalam hitungan puluhan detik, dengan 50 
					sebagai default.</li>
					<li><i>lba32</i> memberitahu LILO geometry dari harddisk. Nilai umum lain adalah linear. 
					Anda sebaiknya tidak mengubah baris ini kecuali anda benar benar tau apa yang anda lakukan. 
					Jika tidak, anda dapat membuat sistem anda tidak dapat diboot.</li>
					<li><i>default=linux</i> mengarah ke OS default yang akan diboot LILO dari daftar opsi yang 
					berada di bawah baris ini. Nama linux mengarah ke baris label di bawah pada setiap opsi boot.</li>
					<li><i>image=/boot/bzImage</i> menunjukkan kernel linux untuk diboot pada opsi boot tersebut.</li>
					<li><i>label=linux</i> menamakan opsi OS yang tampak pada layar LILO. Dalam hal ini, nama ini 
					juga adalah nama yg diarahkan dari baris default.</li>
					<li><i>read-only</i> menunjukkan bahwa partisi root (lihat baris root di bawah) adalah read-only dan 
					tidak dapat diubah sewaktu proses boot berlangsung.</li>
					<li><i>root=/dev/hda3</i> memberitahu LILO partisi disk mana yang digunakan sebagai partisi 
					root.</li>
				</ul>

				<p>Setelah anda edit file <i>lilo.conf</i> anda, sekarang saatnya menjalankan LILO untuk me-load 
				semua data ke MBR:</p>

<pre caption="Menjalankan LILO">
# <c>/sbin/lilo</c>
</pre>

				<p>LILO telah terkonfigurasi, dan sekarang mesin anda telah siap untuk boot ke Gentoo Linux!</p>
			</body>
		</section>
	</chapter>

	<chapter>
		<title>Membuat Bootdisk</title>
		<section>
			<title>GRUB Bootdisk</title>
			<body>
				<p>Membuat boot disk pada saat pertama kali meng-install distro Linux adalah ide yang bagus. 
				Hal ini merupakan lapisan keamanan, dan umumnya bukan hal buruk untuk dilakukan. Jika 
				hardware anda tidak membiarkan anda meng-install bootloader dari shell chroot, anda mungkin
				<e>perlu</e> membuat GRUB boot disk. Jika anda berada pada keadaan ini, buatlah sebuah boot
				disk GRUB, dan ketika anda reboot pertama kali, anda dapat menginstall GRUB ke MBR. Buatlah
				bootdisk anda dengan cara ini:</p>

<pre caption="Membuat Bootdisk GRUB">
# <c>cd /usr/share/grub/i386-pc/</c>
# <c>cat stage1 stage2 > /dev/fd0</c>
</pre>

				<p>Sekarang reboot dan load floppy tersebut. Pada prompt floppy <c>grub&gt;</c>, anda dapat 
				menjalankan perintah perintah <c>root</c> dan <c>setup</c>.</p>
			</body>
		</section>

		<section>
			<title>LILO Bootdisk</title>
			<body>
				<p>Jika anda menggunakan LILO, ide yang bagus juga untuk membuat bootdisk:</p>

<pre caption="Membuat Bootdisk">
# <c>dd if=/boot/your_kernel of=/dev/fd0 </c>
<comment>Ini hanya dapat berfungsi jika kernel anda besarnya lebih kecil dari 1.4MB</comment>
</pre>
			</body>
		</section>
	</chapter>

	<chapter>
		<title>Instalasi Selesai!</title>
		<section>
			<body>
				<p>Sekarang, instalasi Gentoo Linux telah selesai. Langkah terakhir adalah meng-update beberapa 
				file konfigurasi penting, keluar dari shell chroot, unmount partisi anda dengan aman, dan reboot 
				sistem:</p>

<pre caption="Reboot Sistem">
# <c>etc-update</c>
# <c>exit</c> 
<comment>(Perintah ini membawa anda keluar dari shell chroot; anda dapat juga mengetikkan <c>^D</c>)</comment>
# <c>cd / </c>
# <c>umount /mnt/gentoo/boot</c>
# <c>umount /mnt/gentoo/proc</c>
# <c>umount /mnt/gentoo</c>
# <c>reboot</c>
</pre>

				<note>Setelah reboot, adalah ide yang bagus untuk menjalankan perintah <c>update-modules</c> 
				untuk membuat file <path>/etc/modules.conf</path>. Lebih baik untuk membuat perubahan pada 
				file file di <path>/etc/modules.d</path> daripada mengubah file ini langsung.</note>

				<p>Jika anda memiliki pertanyaan atau ingin terlibat dengan pengembangan Gentoo Linux, 
				bergabunglah dengan milis gentoo-user dan gentoo-dev kami (informasi lebih lanjut dapat ditemui di  
				<uri link="http://www.gentoo.org/main/en/lists.xml">mailing lists</uri>). Kami juga memiliki 
				<uri link="http://www.gentoo.org/doc/en/desktop.xml">Desktop configuration guide</uri> yang akan 
				membantu anda dalam melanjutkan konfigurasi sistem Gentoo Linux baru anda, dan juga 
				<uri link="http://www.gentoo.org/doc/en/portage-user.xml">Portage user guide</uri> yang sangat 
				berguna dalam memahami dasar dasar Portage lebih lanjut. Anda dapat menemui Dokumentasi Gentoo 
				yang lain di <uri link="http://www.gentoo.org/main/en/docs.xml">sini</uri>. Jika anda memiliki 
				pertanyaan lain mengenai instalasi atau hal hal yang penting, silahkan periksa 
				<uri link="http://www.gentoo.org/doc/en/faq.xml">FAQ</uri> Gentoo Linux. Selamat menikmati dan 
				selamat datang ke Gentoo Linux!</p>
			</body>
		</section>
	</chapter>

	<chapter>
		<title>Gentoo-Stats</title>
		<section>
			<body>
				<p>Program statistik penggunaan Gentoo Linux dimulai sebagai sebuah percobaan agar para 
				developer tau user base mereka. Gentoo-stats mengambil informasi mengenai penggunaan 
				Gentoo Linux untuk membantu kami dalam mengeset prioritas development kami. Menginstall 
				package ini tidak diharuskan, dan kami akan sangat menghargai jika anda memilih untuk 
				menggunakannya. Statistik proyek ini dapat dilihat di <uri>http://stats.gentoo.org/</uri>.</p>

				<p>Server gentoo-stats akan memberikan anda ID unik bagi sistem anda. ID ini digunakan untuk 
				memastikan satu sistem hanya dihitung sekali saja. ID ini tidak akan digunakan untuk 
				mengidentifikasi sistem anda secara pribadi, ataupun digunakan untuk mencocokkan alamat IP 
				atau segala informasi pribadi lainnya. Berikut ini adalah hal-hal yang kami monitor saat ini melalui 
				program &quot;gentoo-stats&quot; kami:</p>

				<ul>
					<li>Package yang terinstall dan versinya</li>
					<li>Informasi CPU: speed (MHz), nama vendor, nama model, flag CPU (seperti &quot;mmx&quot; 
					atau &quot;3dnow&quot;)</li>
					<li>Informasi memori (total RAM yang tersedia, total swap yang tersedia)</li>
					<li>PCI card dan chip controller jaringan</li>
					<li>Profile Gentoo Linux yang digunakan mesin anda (yaitu, link yang diarahkan /etc/make.profile).</li>
				</ul>

				<p>Kami mengerti bahwa pembeberan informasi penting adalah ancaman bagi user Gentoo Linux (dan 
				juga bagi developer).</p>

				<ul>
					<li>Kecuali anda mengubah program gentoo-stats, program ini tidak akan pernah mengirimkan 
					informasi penting seperti password anda, data konfigurasi, ukuran sepatu...</li>
					<li>Pengiriman alamat e-mail anda tidak diharuskan (pilihan anda) dan dinonaktifkan secara 
					default.</li>
					<li>Alamat IP dari transmisi data anda tidak akan pernah di-log sehingga kami tidak dapat 
					mengidentifikasi anda. Tidak ada data &quot;IP address/system ID&quot;.</li>
				</ul>

				<p>Instalasi program ini sangat mudah - jalankan saja perintah berikut ini:	</p>

<pre caption="Menginstall gentoo-stats">
# <c>emerge gentoo-stats</c>   <codenote>Install gentoo-stats</codenote>
# <c>gentoo-stats --new</c>    <codenote>Meminta ID sistem baru</codenote>
</pre>

				<p>Perintah kedua di atas akan meminta ID sistem yang baru dan memasukkannya ke file 
				<path>/etc/gentoo-stats/gentoo-stats.conf</path> secara otomatis. Anda dapat melihat file 
				ini untuk melihat opsi konfigurasi tambahan.</p>

				<p>Setelah itu, program ini sebaiknya dijalankan dalam jadwal yang teratur (gentoo-stats tidak 
				harus dijalankan sebagai root). Tambahkan baris ini ke <path>crontab</path> anda:</p>

<pre caption="Meng-update gentoo-stats menggunakan cron">
<c>0 0 * * 0,4 /usr/sbin/gentoo-stats --update &gt; /dev/null</c>
</pre>

				<p>Program <c>gentoo-stats</c> adalah sebuat script perl sederhana yang dapat dilihat 
				menggunakan editor atau pager favorit anda: <path>/usr/sbin/gentoo-stats</path>.</p>
			</body>
		</section>
	</chapter>

	<chapter>
		<title>Gentoo Pada Hardware yang kurang Umum</title>
		<section>
			<title>Hardware ATA RAID</title>
			<body>
				<p>User yang ingin menginstall Gentoo pada Hardware ATA RAID harus memperhatikan
				langkah-langkah berikut untuk menginstall Gentoo Linux dengan sukses:</p>

				<ul>
					<li>Pastikan untuk menjalankan LiveCD dengan opsi kernel <c>doataraid</c>.</li>
					<li>Jika anda lupa memilih <c>doataraid</c> pada saat boot, atau modul tersebut 
					dengan misterius tidak terload, loadlah module tersebut:
<pre caption = "Load modul RAID">
# <i>modprobe ataraid</i>
<comment>Untuk Promise Raid Controllers:</comment>
# <i>modprobe pdcraid</i> 
<comment>Untuk Highpoint Raid Controllers:</comment>
# <i>modprobe hptraid</i>
</pre></li>
					<li>Beberapa kontroler ATA RAID memerlukan reboot setelah proses mempartisi; Jika
					tidak, proses format akan gagal.</li>
					<li>Sebelum melakukan chroot, mount-lah devicetree pada environmen baru:
<pre caption = "Mount /dev ke /mnt/gentoo/dev">
# <i>mount -o bind /dev /mnt/gentoo/dev</i>
</pre></li>
					<li>Sewaktu konfigurasi kernel, pilihlah opsi RAID yang diperlukan:
<pre caption = "RAID pada Konfigurasi Kernel Linux">
<comment>Untuk Highpoint RAID controllers:</comment>
ATA/IDE/MFM/RLL support  ---&gt;
  [*] HPT36X/37X chipset support 
  [*] Support for IDE Raid controllers
  [*] Highpoint 370 software RAID
<comment>Untuk Promise RAID controllers:</comment>
ATA/IDE/MFM/RLL support  ---&gt;
  [*] PROMISE PDC202{46|62|65|67} support 
  <comment>dan/atau</comment>
  [*] PROMISE PDC202{68|69|70|71|75|76|77} support
  [*] Support for IDE Raid controllers
  [*] Support Promise software RAID (Fasttrak(tm))
</pre></li>
					<li>Sewaktu menggunakan GRUB, tambahkan <c>--stage2=/boot/grub/stage2</c>
					pada perintah <c>setup</c> sewaktu menjalankan <c>grub</c>:
<pre caption = "Menginstal GRUB pada sistem Hardware RAID">
grub&gt; <i>root (hd0,0)</i>
grub&gt; <i>setup --stage2=/boot/grub/stage2 (hd0)</i>
grub&gt; <i>quit</i>
</pre>
					Dan pada konfigurasi GRUB, pastikan untuk mengarahkan <c>root</c> ke device
					RAID yang tepat:
<pre caption = "grub.conf untuk RAID">
title=My Gentoo Linux on RAID
root (hd0,0)
kernel (hd0,0)/boot/bzImage root=/dev/ataraid/dXpY
</pre></li>
					<li>User yang menggunakan LILO harus mengeset opsi <c>root</c> ke device RAID
					yang tepat:
<pre caption = "lilo.conf untuk RAID">
image=/boot/bzImage
    label=linux
    read-only
    root=/dev/ataraid/dXpY
</pre></li>
				</ul>
			</body>
		</section>
	</chapter>
</guide>
