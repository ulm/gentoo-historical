<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE sections SYSTEM "/dtd/book.dtd">

<!-- The content of this document is licensed under the CC-BY-SA license -->
<!-- See http://creativecommons.org/licenses/by-sa/1.0 -->


<sections>

<version>1.22</version>
<date>2004-10-24</date>

<section>
<title>Caratteristiche di Portage</title>
<body>

<p>
Portage ha molte altre caratteristiche che rendono Gentoo ancora migliore. Molte di queste comprendono tool software che migliorano le prestazioni, l'affidabilità, la sicurezza, ...
</p>

<p>
Per abilitare o disabilitare alcune caratteristiche di Portage, bisogna modificare la variabile <c>FEATURES</c> di <path>/etc/make.conf</path>. In molti casi si devono installare ulteriori tool sui quali sono basate le caratteristiche.
</p>

<p>
Non sono elencate qui tutte le caratteristiche che Portage supporta. Per una descrizione completa si veda la manpage <path>make.conf</path>:
</p>

<pre caption="Vedere la manpage make.conf">
$ <i>man make.conf</i>
</pre>

<p>
Per scoprire quali sono le caratteristiche di default, eseguire <c>emerge info</c> e cercare la variabile FEATURES o eseguire un grep:
</p>

<pre caption="Scoprire quali caratteristiche sono già impostate">
$ <i>emerge info | grep FEATURES</i>
</pre>

</body>
</section>
<section>
<title>Compilazione Distribuita</title>
<subsection>
<title>Usare distcc</title>
<body>

<p>
<c>distcc</c> è un programma per distribuire la compilazione su diverse macchine, non necessariamente identiche, su una rete. Il client <c>distcc</c> trasmette tutte le informazioni necessarie ai server distcc che vengono resi disponibili tramite l'esecuzione di <c>distccd</c>, in modo che possano compilare parte del codice sorgente per il client. Il risultato è un tempo di compilazione inferiore.
</p>

<p>
E' possibile trovare più informazioni su <c>distcc</c> (e informazioni su come deve funzionare con Gentoo) nella nostra <uri link="/doc/it/distcc.xml">Documentazione Gentoo su distcc</uri>.
</p>

</body>
</subsection>
<subsection>
<title>Installare distcc</title>
<body>

<p>
Distcc include un tool grafico per tenere sotto controllo i task che il computer sta inviando per la compilazione. Se si usa Gnome si inserisca 'gnome' nella variabile USE. Se non si usa Gnome e si desidera comunque utilizzare il monitor, si inserisca 'gtk' nella variabile USE.
</p>

<pre caption="Installare distcc">
# <i>emerge distcc</i>
</pre>

</body>
</subsection>
<subsection>
<title>Attivare il supporto di Portage</title>
<body>

<p>
Aggiungere <c>distcc</c> alla variabile FEATURES in <path>/etc/make.conf</path>. Modificare la variabile MAKEOPTS a proprio piacimento. In "-jX" la X è il numero di CPU che eseguono <c>distccd</c> (incluso l'host attuale) più uno, ma si potrebbero avere migliori risultati con altri numeri.
</p>

<p>
Eseguire <c>distcc-config</c> e impostare la lista di server distcc disponibili. Per esempio si assume che i server distcc disponibili sono 192.168.1.102 (l'host attuale), 192.168.1.103 and 192.168.1.104 (due host remoti):
</p>

<pre caption="Configurare distcc per usare tre server disponibili DistCC">
# <i>distcc-config --set-hosts "192.168.1.102 192.168.1.103 192.168.1.104"</i>
</pre>

<p>
Non dimenticarsi di eseguire anche il demone <c>distccd</c>:
</p>

<pre caption="Avviare il demone distccd">
# <i>rc-update add distccd default</i>
# <i>/etc/init.d/distccd start</i>
</pre>

</body>
</subsection>
</section>
<section>
<title>Compilazione caching</title>
<subsection>
<title>Cosa è ccache</title>
<body>

<p>
<c>ccache</c> è un veloce compilatore di cache. Dopo aver compilato un programma, esso immagazzina i risultati intermedi, in modo che se si dovesse ricompilare lo stesso programma, il tempo di compilazione sia notevolmente ridotto. Nelle compilazioni comuni, il tempo di compilazione risulta di 5-10 volte più veloce.
</p>

<p>
Per maggiori informazioni su <c>ccache</c>, è possibile consultare la <uri link="http://ccache.samba.org">homepage di ccache</uri>.
</p>

</body>
</subsection>
<subsection>
<title>Installare ccache</title>
<body>

<p>
Per installare <c>ccache</c>, eseguire <c>emerge ccache</c>:
</p>

<pre caption="Installare ccache">
# <i>emerge ccache</i>
</pre>

</body>
</subsection>
<subsection>
<title>Attivare il supporto di Portage</title>
<body>

<p>
Aprire <path>/etc/make.conf</path> e aggiungere <c>ccache</c> alla variabile FEATURES. Poi, aggiungere una nuova variabile chiamata CCACHE_SIZE e impostarla a "2G":
</p>

<pre caption="Editare CCACHE_SIZE in /etc/make.conf">
CCACHE_SIZE="2G"
</pre>

<p>
Per controlare se ccache funziona, si possono vedere le statistiche:
</p>

<pre caption="Esaminare le statistiche di ccache">
# <i>ccache -s</i>
</pre>

</body>
</subsection>
<subsection>
<title>Usare ccache per la compilazione di C non-Portage</title>
<body>

<p>
Se si desidera usare ccache per compilazioni non-Portage, si aggiunga <path>/usr/lib/ccache/bin</path> all'inizio della variabile PATH (prima di <path>/usr/bin</path>). Può essere fatto modificando <path>/etc/env.d/00basic</path>:
</p>

<pre caption="Modificare /etc/env.d/00basic">
PATH="/usr/local/bin:/opt/bin<i>:/usr/lib/ccache/bin</i>"
</pre>

</body>
</subsection>
</section>
<section>
<title>Supporto per pacchetti binari</title>
<subsection>
<title>Creare pacchetti precompilati</title>
<body>

<p>
Portage supporta l'installazione di pacchetti precompilati. Anche se Gentoo non fornisce pacchetti precompilati (tranne GRP), Portage può essere informato dei pacchetti precompilati. 
</p>

<p>
Per creare un pacchetto precompilato si può usare <c>quickpkg</c> se il pacchetto è già installato sul sistema, o <c>emerge</c> con le opzioni <c>--buildpkg</c> o <c>--buildpkgonly</c>.
</p>

<p>
Se si desidera che Portage crei pacchetti precompilati di ogni singolo pacchetto che si installa, aggiungere <c>buildpkg</c> alla variabile FEATURES.
</p>

<p>
Supporto più esteso per le impostazioni sui pacchetti precompilati può essere ottenuto con il <c>catalyst</c>. Per ulteriori informazioni sul catalyst leggere <uri link="/proj/en/releng/catalyst/reference.xml">Catalyst Reference Manual</uri> e <uri link="/proj/en/releng/catalyst/catalyst-howto.xml">Catalyst Howto</uri>.
</p>

</body>
</subsection>
<subsection>
<title>Installare pacchetti precompilati</title>
<body>

<p>
Anche se Gentoo non li fornisce, si può creare un repository centrale dove mettere i pacchetti precompilati. Se si desidera usare questo repository, si deve far puntare la variabile PORTAGE_BINHOST ad esso. Per esempio, se i pacchetti precompilati sono su ftp://buildhost/gentoo:
</p>

<pre caption="Impostare PORTAGE_BINHOST in /etc/make.conf">
PORTAGE_BINHOST="ftp://buildhost/gentoo"
</pre>

<p>
Quando si desidera installare un pacchetto precompilato, si deve aggiungere l'opzione <c>--getbinpkg</c> al comando emerge accanto all'opzione <c>--usepkg</c>. Il primo (<c>--getbinpkg</c>) dice a emerge di scaricare il pacchetto precompilato dal server precedentemente definito mentre il secondo (<c>--usepkg</c>) chiede a emerge di cercare di installare il pacchetto precompilato prima di scaricare i sorgenti e compilarlo.
</p>

<p>
Per esempio, per installare <c>gnumeric</c> con i pacchetti precompilati:
</p>

<pre caption="Installare il pacchetto precompilato gnumeric">
# <i>emerge --usepkg --getbinpkg gnumeric</i>
</pre>

<p>
Più informazioni sulle opzioni di emerge con i pacchetti precompilati possono essere trovate nella manpage emerge:
</p>

<pre caption="Vedere manpage emerge">
$ <i>man emerge</i>
</pre>

</body>
</subsection>
</section>
</sections>
