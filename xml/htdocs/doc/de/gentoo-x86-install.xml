<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE guide SYSTEM "/dtd/guide.dtd">

<!-- English CVS version: 1.160 -->

<guide link="/doc/de/gentoo-x86-install.xml">
	<title>Gentoo Linux 1.4 Installationsanweisungen</title>

	<author title="Chief Architect">
		<mail link="drobbins@gentoo.org">Daniel Robbins</mail>
	</author>

	<author title="Author">Chris Houser</author>

	<author title="Author">Jerry Alexandratos</author>

	<author title="Ghost, Reviewer">
		<mail link="g2boojum@gentoo.org">Grant Goodyear</mail>
	</author>

	<author title="Editor">
		<mail link="zhen@gentoo.org">John P. Davis</mail>
	</author>

	<author title="Editor">
		<mail link="Pierre-Henri.Jondot@wanadoo.fr">Pierre-Henri Jondot</mail>
	</author>

	<author title="Editor">
		<mail link="stocke2@gentoo.org">Eric Stockbridge</mail>
	</author>

	<author title="Editor">
		<mail link="rajiv@gentoo.org">Rajiv Manglani</mail>
	</author>

	<author title="Editor">
		<mail link="seo@gentoo.org">Jungmin Seo</mail>
	</author>

	<author title="Editor">
		<mail link="zhware@gentoo.org">Stoyan Zhekov</mail>
	</author>

	<author title="Editor">
		<mail link="jhhudso@gentoo.org">Jared Hudson</mail>
	</author>

	<author title="Editor">Colin Morey</author>

	<author title="Editor">
		<mail link="peesh@gentoo.org">Jorge Paulo</mail>
	</author>

	<author title="Editor">
		<mail link="carl@gentoo.org">Carl Anderson</mail>
	</author>

	<author title="Editor">
		<mail link="swift@gentoo.org">Sven Vermeulen</mail>
	</author>

	<author title="Editor">
		<mail link="avenj@gentoo.org">Jon Portnoy</mail>
	</author>

	<author title="Editor">
		<mail link="klasikahl@gentoo.org">Zack Gilburd</mail>
	</author>

	<author title="Reviewer">
		<mail link="gerrynjr@gentoo.org">Gerald J. Normandin Jr.</mail>
	</author>

	<author title="Reviewer">
		<mail link="spyderous@gentoo.org">Donnie Berkholz</mail>
	</author>

	<author title="Reviewer">
		<mail link="antifa@gentoo.org">Ken Nowack</mail>
	</author>

	<author title="Übersetzer">
		<mail link="mgreiner@berlios.de">Markus Greiner</mail>
	</author>

	<author title="Übersetzer">
		<mail link="pylon@gentoo.org">Lars Weiler</mail>
	</author>


	<abstract>Diese Anleitung führt Sie schrittweise durch den Gentoo Linux 1.4
	(keine _rc Version) Installationsprozess für die x86 Prozessorarchitektur
	(AMD, Intel).  Die Gentoo Linux Installation unterstützt verschiedene
	Installationsarten, abhängig davon wie weit des Systems von Grund auf selbst
	compiliert werden soll.</abstract>

	<license/>

	<version>2.6.5</version>
	<date>15. August 2003</date>

	<chapter>
	<title>Über die Installation</title>
		<section>
			<body>

				<p>Zuallererst, wenn Sie neu dabei sind, willkommen zu Gentoo Linux!
				Gentoo Linux kann über verschiedene Wege installiert werden.  Wenn Sie
				schnell installieren möchten, können Sie fertig erstellte Pakete
				installieren.  Oder wünschen Sie andererseits eine komplette Anpassung,
				können Sie Gentoo Linux von Grund auf aus dem Quellcode kompilieren.
				Die Wahl der Methode liegt bei Ihnen.</p>

				<p>Eine entscheidende Änderung in unserem offziellen 1.4 Release ist
				unser neues 2-CD Installasionsset, das in unserem <uri
				link="http://store.gentoo.org">Gentoo Linux Store</uri> geordert werden
				kann, als auch auf unseren Mirror-Servern bereitliegt.  Wir haben
				derzeit 2-CD Installationssets für x86 (486 und neuer), i686 (Pentium
				Pro, Pentium II, Athlon/Duron und neuer), Pentium III, Pentium 4 und
				Athlon XP.  Welches 2-CD-Set das richtige für Sie ist, können Sie in der
				detaillierten Beschreibung in unserem <uri
				link="http://store.gentoo.org">Laden</uri> nachlesen.  Diese
				Beschreibungen beinhalten recht ausführliche Informationen über die
				CPU-Kompatibilitäten.</p>

				<p>Soweit zum 2-CD-Set -- hier ist beschrieben, was auf jeder CD ist.
				Die erste CD ("CD 1") heißt "Live CD Installation" und ist eine bootbare
				CD-ROM, das heißt, Sie können "CD 1" in Ihr CD-ROM-Laufwauk einlegen und
				Gentoo Linux davon laufen lassen.  Sie können diese CD-basierte Version
				von Gentoo dafür verwenden, Gentoo Linux 1.4 auf Ihre Festplatte zu
				installieren.  Neben der bootbaren Gentoo Linux Umgebung beinhaltet CD 1
				alles, was Sie für eine schnelle Installation von Gentoo Linux
				benötigen, selbst ohne eine Anbindung an das Internet.  Zusätzlich
				befinden sich einige vorkompilierte Pakete auf CD 1, wie den wichtigen
				XFree86 X Server.  Haben Sie ein ISO CD-ROM Image von CD 1 ist das
				Dateiende "<path>-cd1.iso</path>" benannt.</p>

				<p>Im Gegensatz dazu ist die zweite CD ("CD 2") nicht bootbar und
				enthält viele vorkompilierte Pakete für Ihr System.  Auf dieser CD sind
				optimierte Versionen von Paketen wie KDE, GNOME, OpenOffice, Mozilla,
				Evolution und andere verfügbar.  CD 2 ist <e>optional</e> und für
				diejenigen gedacht, die Gentoo Linux sehr schnell installieren
				möchten.  Die Pakete auf dieser CD brauchen auf einem üblichen, modernen
				Einzelprozessorsystem etwa 36 Stunden für eine Kompilierung aus dem
				Quellcode.  Haben Sie ein ISO CD-ROM Image von CD 2 ist das Dateiende
				"<path>-cd2.iso</path>" benannt.</p>

				<note>Ein komplettes Gentoo Linux 2-CD Set enthält die Gentoo Reference
				Platform ("GRP"), das ein komplettes vorkompiliertes Gentoo Linux System
				mit GNOME, KDE, Mozilla und OpenOffice ist.  Die Gentoo Reference
				Platform wurde erstellt, um eine schnelle Gentoo Linux paketbasierte
				Installation bereitzustellen.  Die Möglichkeit der "Kompilierung vom
				Quellcode", die der Grundstein von Gentoo Linux ist, wird immer eine
				komplett unterstützte Installationsoption bleiben.  Der Grund für die
				GRP ist, Gentoo Linux angenehmer für einige Benutzer zu machen, ohne
				Gentoos mächtigen "Kompilierung vom Quellcode" Installationsprozess in
				irgendeiner Weise zu beeinträchtigen.</note>

				<p>Neben dem 2-CD Set haben wir auch eine sehr kleine "basic" Live
				CD, die Sie zum Booten Ihres Systems verwenden können.  Sobald Ihr
				System gebootet ist, können Sie den Zugang zum Internet einrichten und
				dann Gentoo Linux über das Netzwerk installieren.  Der Vorteil dieser
				"basic" CD liegt in ihrer Größe, sodass das ISO CD-ROM Image schnell
				heruntergeladen werden kann.  Wenn Sie ein erfahrener Benutzer sind, die
				allerneueste Version von Gentoo Linux installieren möchten und eine
				schnelle Internetanbindung haben, dann sollten Sie diese Möglichkeit
				bevorzugen.  Haben Sie ein ISO CD-ROM Image von unserer "basic" Live CD
				ist das Dateiende "<path>-basic.iso</path>" benannt.</p>

				<p>Für jede der CD-basierten Gentoo Linux Installationsmethoden
				benötigen Sie einen 486+ Prozessor und idealerweise mindestens 64
				Megabyte RAM.  (Gentoo Linux wurde schon erfolgreich mit 64MB RAM und
				64MB Swap installiert, jedoch ist der Build Process furchtbar langsam
				unter diesen Umständen.)</p>

				<p>Nachdem Sie von einer unserer Live CDs gebootet haben, gibt es
				mehrere Optionen.  Gentoo Linux kann über drei verschiedene "stage"
				tar-Archiven installiert werden.  Ausgehend von dem gewählten
				tar-Archiv, entscheiden Sie, wie viel Ihres Systems selbst kompiliert
				werden soll.  Wählen Sie das stage1 tar-Archiv, wenn Sie den
				bootstrap-Prozess durchführen und das komplette System von Grund auf
				erzeugen wollen.  Mit dem stage2 tar-Archiv kompilieren Sie ihr gesamtes
				System mit einem vorab durchgeführten bootstrap-Prozess.  Das stage3
				tar-Archiv enthält ein bereits kompiliertes Gentoo Linux Basissystem.
				Für die "GRP" Installationsmethode verwenden Sie das stage3
				tar-Archiv.</p>

				<p><b>Sie möchten keine Installation mit der GRP machen -- mit welchem
				stage1, stage2 oder stage3 tar-Archiv sollen Sie dann beginnen?</b> Hier
				gibt es einige Informationen, die Ihnen helfen werden, die Entscheidung
				zu fällen:</p>

				<p>Ausgehend von stage1 haben Sie die totale Kontrolle über die
				Optimierungseinstellungen und optionalen Funktionen während des
				Kompilierens, die von Beginn an für Ihr System aktiviert sind.  Dadurch
				ist stage1 für "Power-User" gut geeignet, die wissen, was sie tun.  Es
				ist auch eine gute Installationsmethode für diejenigen, die mehr über
				die innere Funktionsweise von Gentoo Linux erfahren möchten.</p>

				<p>Die Installation von stage2 überspringt den bootstrap-Prozess; das
				ist recht hilfreich, wenn Sie mit den von uns vorgenommenen
				Optimierungen für Ihr stage2 tar-Archiv zufrieden sind.</p>

				<p>Eine Installation mit stage3 ist die schnellste Möglichkeit, Gentoo
				Linux zu installieren.  Jedoch wird dadurch das Basissystem mit den
				Optimierungen, die wir für Sie gewählt haben (die, um ehrlich zu sein,
				gute Einstellungen sind und sorgfältig ausgewählt wurden, um die
				Performanz bei gleichzeitiger Gewährleistung der Stabilität zu
				steigern), installiert.  Da jedes größere Release von Gentoo Linux
				stage3 tar-Archive mit Optimierungen für die gängigen Prozessoren
				bereitstellt, ist die Installation von stage3 die Methode der Wahl --
				eine schnelle Installation und ein gut optimiertes System.</p>

				<p><b>Sollten Sie zum ersten Mal Gentoo Linux installieren, benutzen Sie
				ein stage3 tar-Archiv oder ein stage3 mit GRP.</b></p>

				<note><b>Erfahrene Benutzer:</b> Wenn Sie ein stage3 zur Installation
				verwenden, ändern Sie nicht die CHOST Einstellung in
				<path>/etc/make.conf</path>.  Wenn Sie diese Änderung benötigen, sollten
				Sie mit einem stage1 tar-Archiv beginnen und Ihr System mit der nötigen
				CHOST-Einstellung bauen.  Die CHOST Einstellung ist üblicherweise sowas
				wie <c>i686-pc-linux-gnu</c></note>

				<impo>Fällt Ihnen ein Problem während der Installation auf und möchten
				Sie uns über diesen "Bug" informieren, füllen Sie bitte einen Bug-Report
				unter <uri>http://bugs.gentoo.org</uri> aus.  Soll der Bug die
				ursprünglichen Entwickler einer Software erreichen (wie z.B. das KDE
				Team), dann werden die <e>Gentoo Linux Developer</e> das für Sie
				übernehmen.  Sie helfen mit einem Bug-Report auch anderen Gentoo Linux
				Benutzern, die möglicherweise ebenso Ihr Problem haben.  Mit einer
				Behebung des Problems helfen Sie von daher der gesamten Gentoo Linux
				Gemeinschaft!</impo>

				<note>Die Installationsanweisungen auf der Live CD sind möglicherweise
				nicht auf den neuesten Stand.  Bitte verwenden Sie
				<uri>http://www.gentoo.org/doc/de/gentoo-x86-install.xml</uri> für die
				aktuellen Installationsanweisungen  (oder die wirklich immer aktuelleste
				Version der Englischen Anweisungen unter
				<uri>http://www.gentoo.org/doc/en/gentoo-x86-install.xml</uri>).</note>

				<p>Jetzt lassen Sie uns kurz den Installationsprozess skizzieren.
				Zuerst laden Sie die CD(s) herunter, brennen diese und booten eine Live
				CD.  Nachdem Sie am root-Prompt angelangt sind, erstellen Sie die
				Partitionen, erzeugen die Dateisysteme und entpacken entweder das
				stage1, stage2 oder stage3 tar-Archiv. Wenn Sie das stage1 oder stage2
				tar-Archiv verwenden, müssen Sie die nötigen Schritte vornehmen, um Ihr
				System auf den Stand des stage3 Archivs zu bringen. Wenn Sie stage3
				erreicht haben, können Sie das System konfigurieren (die
				Konfigurationsdateien anpassen, einen Bootloader installieren, usw.) und
				in ein voll funktionsfähiges Gentoo Linux booten.  Nachdem Ihr Gentoo
				Linux Basissystem läuft, können Sie optional die "CD 2" unseres 2-CD Set
				verwenden und weitere fertig erstellte Pakete wie KDE, GNOME,
				OpenOffice, Mozilla oder was Sie sonst auf Ihrem System wünschen,
				installieren.</p>

				<p>Abhängig von der gewählten Installationsstufe, von der Sie starten,
				hier die benötigten Schritte:</p>

				<table>
					<tr>
						<th>stage tar-Archiv</th>
						<th>Internetzugang erforderlich</th>
						<th>Benötigtes Medium</th>
						<th>Schritte</th>
					</tr>
					<tr>
						<ti>1</ti>
						<ti>Ja</ti>
						<ti><e>basic</e> oder <e>CD 1</e></ti>
						<ti>Partitions/Dateisystem Einrichtung, emerge sync, bootstrap,
						emerge system, emerge kernel sources, abschließende
						Konfiguration</ti>
					</tr>
					<tr>
						<ti>2</ti>
						<ti>Ja</ti>
						<ti><e>basic</e> oder <e>CD 1</e></ti>
						<ti>Partitions/Dateisystem Einrichtung, emerge sync, emerge system,
						abschließende Konfiguration</ti>
					</tr>
					<tr>
						<ti>3</ti>
						<ti>Nein, wenn <e>CD 1</e> verwendet wird, ansonsten Ja</ti>
						<ti><e>basic</e> oder <e>CD 1</e></ti>
						<ti>Partitions/Dateisystem Einrichtung, emerge sync (nicht nötig,
						wenn <e>CD 1</e> verwendet wird), abschließende Konfiguration</ti>
					</tr>
					<tr>
						<ti>3+GRP</ti>
						<ti>Nein</ti>
						<ti><e>CD 1</e>, <e>CD 2</e> optional</ti>
						<ti>Partitions/Dateisystem Einrichtung, abschließende Konfiguration,
						Installation der vorkompilierte Pakete von CD 1 (optional), reboot,
						Installation von zusätzlichen vorkompilierten Paketen wie KDE und
						GNOME (bei Verwendung von "CD 2")</ti>
					</tr>
				</table>

				<note>Hardware ATA RAID Benutzer sollten den Abschnitt über ATA RAID am
				Ende des Dokuments lesen, bevor sie fortfahren.</note>

			</body>
		</section>
	</chapter>


	<chapter>
	<title>Booten</title>
		<section>
			<body>

				<warn>Lesen Sie diesen kompletten Abschnitt bevor Sie fortfahren,
				besonders die verfügbaren Boot Optionen.  Andernfalls kann dieses zu
				falschen Tastatureinstellungen, nicht gestarteten PCMCIA-Diensten usw.
				führen.</warn>

				<p>Booten Sie nun die von Ihnen gewählte Live CD.  Sie sollten ein
				nettes Boot-Prompt mit dem Gentoo Linux Logo sehen.  Drücken Sie hier
				Enter, um den Boot-Prozess zu starten oder geben Sie optionale
				Boot-Optionen an, indem Sie einen Kernel auswählen, gefolgt weiteren
				Optionen und mit Enter abschließen.  Zum Beispiel <c>gentoo nousb
				nohotplug</c>.  Installieren Sie Gentoo Linux auf einem System mit mehr
				als einem Prozessor ("SMP"), dann geben Sie <c>smp</c> anstelle von
				<c>gentoo</c> am Prompt ein.  Dadurch erkennt die Live CD alle
				Prozessoren und nicht nur den ersten.</p>

				<p>Für eine detaillierte Beschreibung der möglichen Boot-Optionen,
				schauen Sie in die folgende Tabelle der verfügbaren Kernel und Optionen
				oder drücken Sie F2 und F3 am Boot-Prompt für die Hilfe.</p>

				<table>
					<tr><th>Verfügbare Kernel</th><th>Beschreibung</th></tr>

					<tr><ti>gentoo</ti><ti>normaler Gentoo Kernel (default)</ti></tr>
					<tr><ti>nofb</ti><ti>Framebuffer Modus abgeschaltet</ti></tr>
					<tr><ti>smp</ti><ti>lädt einen SMP Kernel (für mehrere Prozessoren)
					ohne Framebuffer Modus</ti></tr>
					<tr><ti>acpi</ti><ti>Schaltet ACPI ein und lädt ACPI Module während
					des init-Prozesses</ti></tr>
					<tr><ti>memtest</ti><ti>Bootet das RAM Test Programm</ti></tr>
				</table>

				<table>
					<tr><th>Verfübare Boot Optionen</th><th>Beschreibung</th></tr>

					<tr><ti>doataraid</ti><ti>Lädt IDE RAID Module in initrd</ti></tr>
					<tr><ti>dofirewire</ti><ti>Lädt FireWire Module in initrd (für
					FireWire CD-ROMs, usw.)</ti></tr>
					<tr><ti>dokeymap</ti><ti>Lässt die Tastaturbelegung für non-us
					Tastaturen auswählen</ti></tr>
					<tr><ti>dopcmcia</ti><ti>Startet PCMCIA Dienste</ti></tr>
					<tr><ti>doscsi</ti><ti>Scannt nach SCSI Geräten (kann zu
					Komplikationen mit manchen Ethernet-Karten führen)</ti></tr>
					<tr><ti>noapm</ti><ti>Verhindert das Laden vom APM (Powermanagement)
					Modul</ti></tr>
					<tr><ti>nodetect</ti><ti>Führt nicht die automatische
					Hardwareerkennung mit hwsetup/kudzu und hotplug aus</ti></tr>
					<tr><ti>nodhcp</ti><ti>DCHP wird nicht ausgeführt, wenn eine
					Netzwerkkarte erkannt wurde</ti></tr>
					<tr><ti>nohotplug</ti><ti>Verhindert das Laden von hotplug
					Diensten</ti></tr>
					<tr><ti>noraid</ti><ti>Verhindert das Laden von evms Modulen</ti></tr>
					<tr><ti>nousb</ti><ti>Verhindert das Laden von USB Modulen, impliziert
					nohotplug</ti></tr>
					<tr><ti>ide=nodma</ti><ti>Erzwingt das Abschalten von DMA für
					inkorrekt funktionierende IDE Geräte</ti></tr>
					<tr><ti>cdcache</ti><ti>Lädt alle benötigten Komponenten der Live CD
					in den RAM (etwa 40MB werden benötigt), wodurch
					<path>/mnt/cdrom</path> ungemountet und eine andere CD eingelegt
					werden kann</ti></tr>
				</table>

				<p>Sobald Sie Enter gedrückt haben, erscheint ein noch netterer
				Bootbildschirm und ein Statusbalken.</p>

				<!--
					<figure link="/images/install/livecd-1.4-boot.png" caption="The Gentoo
					Linux Live CD booting" />
				-->

				<p>Im "Live" Gentoo Linux werden Sie automatisch als "<c>root</c>", dem
				"super user", eingeloggt.  Sie sollten einen root-Prompt ("<c>#</c>")
				auf der aktuellen Console haben.  Sie können zu weiteren Consolen
				wechseln, indem Sie Alt-F2, Alt-F3 und Alt-F4 drücken.  Um zu der ersten
				Console zurückzukehren, drücken Sie Alt-F1.</p>

				<!--
					<figure link="/images/install/livecd-1.4-con.png" caption="The Gentoo
					Linux Live CD console" />
				-->

				<note><b>Erfahrene Benutzer:</b> Wenn Sie die Live CD booten wird das
				Live CD root Passwort aus Sicherheitsgründen auf eine zufällige
				Zeichenkette gesetzt.  Wenn Sie vorhaben <c>sshd</c> zu starten und
				einen Remote Login zur Live CD einrichten wollen, setzen Sie das Live CD
				root Passwort mit <c>passwd</c> und den folgenden Anweisungen.
				Ansonsten kennen Sie das entsprechende Passwort zum einloggen auf die
				Live CD über das Netzwerk nicht.</note>

				<p>Sie haben möglicherweise schon den Hilfetext oberhalb des
				<c>#</c>-Prompts gesehen.  Dieser erklärt, wie Sie ihr Netzwerk
				konfigurieren und wo Sie auf der CD die Gentoo Linux stage tar-Archive
				und Pakete finden.</p>

			</body>
		</section>
	</chapter>


	<chapter>
	<title>Optionale Hardware Konfiguration</title>
		<section>
			<body>

				<p>Wenn die Live CD bootet versucht sie Ihre Hardware zu erkennen und
				lädt die entsprechenden Kernel Module, damit Sie auf die Hardware
				zugreifen können.  Nahezu immer klappt dieses einwandfrei.  Jedoch wird
				manchmal das benötigte Kernel Modul nicht automatisch geladen.  Falls
				die PCI-Autoerkennung Ihre Hardware nicht komplett erkannt hat, müssen
				Sie die entsprechenden Kernel Module von Hand nachladen.  Für eine Liste
				aller vorhandenen Netzwerkkarten (NIC) Module, geben Sie <c>ls
				/lib/modules/`uname -r`/kernel/drivers/net/*</c> ein.  Um ein spezielles
				Modul zu laden, schreiben Sie:</p>

<pre caption="PCI Modul Konfiguration">
# <i>modprobe pcnet32</i>
<comment>(Ersetzen Sie pcnet32 durch das Modul Ihrer NIC)</comment>
</pre>

				<p>Wenn Sie noch auf SCSI Hardware zugreifen möchten, die nicht
				automatisch erkannt wurde, müssen Sie das passende Modul aus
				<path>/lib/modules</path> laden.  Verwenden Sie dazu wieder
				<c>modprobe</c>:</p>

<pre caption="SCSI Modul laden">
# <i>modprobe aic7xxx</i>
<comment>(ersetzen Sie aic7xxx durch das richtige SCSI-Modul für Ihren Controller)</comment>
# <i>modprobe sd_mod</i>
<comment>(sc_mod ist das Modul für SCSI Festplatten Unterstützung)</comment>
</pre>

				<note>Unterstützung für SCSI CD-ROMs und Festplatten ist schon im Kernel
				eingebaut.</note>

				<note><b>Erfahrene Benutzer:</b> Die Gentoo Live CD sollte DMA für den
				schnellstmöglichen Datentransfer an Ihren Festplatten aktiviert haben.
				Falls dem nicht so ist, können Sie mit <c>hdparm</c> DMA für Ihre
				Festplatten einschalten.

<pre caption="DMA einschalten">
<comment>(Ersetzen Sie hdX durch die entsprechende Bezeichnung für Ihre
Festplatte)</comment>
# <i>hdparm -d 1 /dev/hdX</i>
<comment>(DMA einschalten)</comment>
# <i>hdparm -d1 -A1 -m16 -u1 -a64 /dev/hdX</i>
<comment>(DMA einschalten und andere sichere Perfomance-steigernde Optionen)</comment>
# <i>hdparm -X66 /dev/hdX</i>
<comment>(Zwinge die Verwendung von Ultra-DMA -- gefährlich! -- kann manche
Festplatten in Unordnung bringen)</comment>
</pre>

				</note>

			</body>
		</section>
	</chapter>


	<chapter>
		<title>Optionale Konfiguration der Netzwerkanbindung</title>
		<section>
			<title>Vielleicht klappt es schon?</title>
			<body>

				<p>Wenn Ihr System an ein Ethernet Netzwerk angeschlossen ist, ist es
				möglich, dass Ihre Netzwerkanbindung schon automatisch konfiguriert
				wurde.  Falls es so ist, können Sie die auf der Live CD beinhalteten
				Programme für Netzwerke verwenden, wie z.B. <c>ssh</c> (Sichere Shell
				Verbindung zu einem anderen Rechner), <c>scp</c> (Sicheres Kopieren von
				Dateien zu/von einem anderen Rechner), <c>ping</c> (ICMP Test-Pakete
				senden), <c>irssi</c> (IRC Chat Programm), <c>wget</c> (Dateien von http
				oder ftp herunterladen), <c>lynx</c> (Textbasierter Web-Browser), sowie
				weitere Programme.</p>

				<p>Falls die Netzwerkanbindung schon konfiguriert wurde, sollte der
				Aufruf von <c>/sbin/ifconfig</c> die Netzwerkkarten neben <c>lo</c>
				anzeigen, wie z.B.  <c>eth0</c>:</p>

<pre caption="/sbin/ifconfig bei einer funktionierenden Netzwerkkarte">
eth0      Link encap:Ethernet  HWaddr 00:50:BA:8F:61:7A
          inet addr:192.168.0.2  Bcast:192.168.0.255  Mask:255.255.255.0
          inet6 addr: fe80::50:ba8f:617a/10 Scope:Link
          UP BROADCAST RUNNING MULTICAST  MTU:1500  Metric:1
          RX packets:1498792 errors:0 dropped:0 overruns:0 frame:0
          TX packets:1284980 errors:0 dropped:0 overruns:0 carrier:0
          collisions:1984 txqueuelen:100
          RX bytes:485691215 (463.1 Mb)  TX bytes:123951388 (118.2 Mb)
          Interrupt:11 Base address:0xe800
</pre>

				<p>Sie sollten außerdem versuchen, den DNS (Domain Name System) Server
				Ihres ISP (Internet Service Provider) anzu"ping"en (die IP-Adresse
				befindet sich in <path>/etc/resolv.conf</path>), als auch eine Web-Seite
				Ihrer Wahl, einfach nur um sicherzustellen, dass Ihre Pakete das Netz
				erreichen und die DNS Namensauflösung ordentlich funktioniert, usw.</p>

<pre caption="Weiteres Netzwerktesten">
# <c>ping -c 3 www.yahoo.com</c>
</pre>

				<p>Können Sie Ihre Netzwerkanbindung verwenden?  Falls dem so ist,
				können Sie den Rest dieses Kapitels überspringen.</p>

			</body>
		</section>

		<section>
			<title>PPPoE Konfiguration</title>
			<body>

				<p>Angenommen Sie benötigen PPPoE, um sich zum Internet zu verbinden, so
				macht es die Live CD (egal welche Version) für Sie mit <c>rp-pppoe</c>
				leicht.  Benutzen Sie das angebotene <c>adsl-setup</c> Skript, um Ihre
				Verbindung einzurichten.  Sie werden nach der Netzwerkkarte, die mit
				Ihrem ADSL-Modem verbunden ist, Ihrem Usernamen und dem Passwort, den
				IP-Adressen Ihres DNS Server und ob Sie eine einfache Firewall benötigen,
				gefragt.</p>

<pre caption="PPPoE Konfigurieren">
# <i>adsl-setup</i>
# <i>adsl-start</i>
</pre>

				<p>Wenn es nicht auf Anhieb funktioniert, überprüfen Sie, ob Sie Ihren
				Usernamen und das Passwort korrekt angegeben haben, indem Sie in
				<path>/etc/ppp/pap-secrets</path> oder
				<path>/etc/ppp/chap-secrets</path> nachschauen.  Außerdem seien Sie
				sicher, dass Sie die richtige Netzwerkkarte gewählt haben.</p>

			</body>
		</section>

		<section>
			<title>Automatische Netzwerkkonfiguration</title>
			<body>

				<p>Der einfachste Weg die Netzwerkanbindung zu konfigurieren, falls sie
				nicht automatisch eingerichtet wurde, ist mit dem <c>net-setup</c>
				Skript.</p>

<pre caption="net-setup Skript">
# <i>net-setup eth0</i>
</pre>

				<p>Natürlich, falls Sie es lieber wünschen, können Sie auch Ihre
				Netzwerkanbindung manuell konfigurieren.  Das wird als nächstes
				beschrieben.</p>

			</body>
		</section>

		<section>
			<title>Manuelle DHCP Konfiguration</title>
			<body>

				<p>Mit DHCP ist die Netzwerkkonfiguration sehr einfach. Wenn Ihr
				Provider kein DHCP anbietet, geht es für Sie bei der statischen
				Netzwerkkonfiguration weiter.</p>

<pre caption="Netzerk Konfiguration via DHCP">
# <i>dhcpcd eth0</i> 
</pre>

				<note>Einige Provider erwarten, dass Sie einen Hostnamen übergeben. Um
				dies zu tun, hängen Sie die Option <c>-h meinhostname</c> an das oben
				stehende Kommando.</note>

				<p>Wenn Sie <e>dhcpConfig</e> Warnungen bekommen, keine Panik. Die
				meisten Meldungen sind harmlos. Jetzt geht es für Sie unten, mit dem
				Test der Netzwerkanbindung, weiter.</p>

			</body>
		</section>

		<section>
			<title>Manuelle statische Konfiguration</title>
			<body>

				<p>Sie müssen Ihre Netzwerkanbindung so weit konfigurieren, dass Sie die
				Quellen für die Programme herunterladen können.  Außerdem wird das
				lokale (localhost) Interface benötigt.   Die benötigten Informationen
				stehen in der folgenden Tabelle:</p>

				<table>
					<tr>
						<th>Information</th>
						<th>Beschreibung</th>
						<th>Beispielwert</th>
					</tr>
					<tr>
						<ti>IP Adresse</ti>
						<ti>Die IP Adresse, die Sie Ihrer Netzwerkkarte zuweisen möchten</ti>
						<ti>192.168.1.2</ti>
					</tr>
					<tr>
						<ti>Broadcast Adresse</ti>
						<ti>Die IP Adresse, die Pakete an alle Hosts in einem Netzwerk
						broadcastet</ti>
						<ti>192.168.1.255</ti>
					</tr>
					<tr>
						<ti>Netzwerk Maske</ti>
						<ti>Die Netzwerk Maske wird zusammen mit der IP Adresse dafür
						verwendet, um herauszufinden, welcher Teil der Adresse als Netzwerk-
						und welcher als Hostbereich definiert ist</ti>
						<ti>255.255.255.0</ti>
					</tr>
					<tr>
						<ti>Gateway</ti>
						<ti>Die IP Adresse des Rechners, der Pakete weiterleitet, die nicht
						für das lokale Netzwerk bestimmt sind (meistens der Rechner, der mit
						dem Internet verbunden ist)</ti>
						<ti>192.168.1.1</ti>
					</tr>
				</table>

				<p>Bitte geben Sie dazu folgende Befehle ein und ersetzen <c>$IFACE</c>
				mit Ihrem Netzwerkinterface (typischerweise <c>eth0</c>), <c>$IPNUM</c>
				mit Ihrer IP Adresse, <c>$BCAST</c> mit Ihrer Broadcast Adresse und
				<c>$NMASK</c> mit Ihrer Netzwerkmaske. Bei dem <c>route</c> Befehl
				ersetzen Sie <c>$GTWAY</c> durch die IP Adresse Ihres default
				Gateway.</p>

<pre caption="Statische IP Netzwerkkonfiguration">
# <i>/sbin/ifconfig $IFACE $IPNUM broadcast $BCAST netmask $NMASK</i>
# <i>/sbin/route add -net default gw $GTWAY netmask 0.0.0.0 metric 1 $IFACE</i>
</pre>

				<p>Jetzt müssen Sie die Datei <path>/etc/resolv.conf</path> erstellen,
				damit die Namensauflösung (Web/FTP Server durch den Namen, anstatt der
				IP Adresse finden) funktioniert.  Sie können <c>nano -w
				/etc/resolv.conf</c> verwenden, um die Datei zu erstellen.  <c>nano</c>
				ist ein kleiner und einfach zu bedienender Editor.</p>

				<p>Hier ein Beispiel der Datei <path>/etc/resolv.conf</path>:</p>

<pre caption="/etc/resolv.conf Vorlage">
domain mydomain.com
nameserver 10.0.0.1
nameserver 10.0.0.2
</pre>

				<p>Ersetzen Sie <c>10.0.0.1</c> und <c>10.0.0.2</c> mit den IP Adressen
				Ihres primären und sekundären DNS Servers.</p>

			</body>
		</section>

		<section>
			<title>Proxy Konfiguration</title>
			<body>

				<p>Wenn Sie sich hinter einem Proxy befinden, kann es nötig sein, dass
				Sie Ihre Einstellungen entsprechend konfigurieren, bevor Sie fortfahren.
				Setzen Sie einige Umgebungsvariablen, um Ihren Proxy zu verwenden:</p>

<pre caption="Proxy konfigurieren">
<comment>Wenn der Proxy HTTP-Datenverkehr regelt:</comment>
# <c>export http_proxy="proxy.netzwerk.lan:1234"</c>
<comment>Wenn der Proxy FTP-Datenverkehr regelt:</comment>
# <c>export ftp_proxy="$http_proxy"</c>
<comment>Wenn der Proxy RSYNC-Datenverkehr regelt: </comment>
# <c>export RSYNC_PROXY="$http_proxy"</c>
</pre>

			</body>
		</section>

		<section>
			<title>Die Netzwerkanbindung funktioniert!</title>
			<body>

				<p>Die Netzwerkanbindung sollte nun konfiguriert und nutzbar sein.  Es
				sollte möglich sein, dass sie mit den vorhandenen Programmen <c>ssh</c>,
				<c>scp</c>, <c>lynx</c>, <c>irssi</c> und <c>wget</c> andere Rechner in
				Ihrem LAN oder dem Internet erreichen können.</p>

			</body>
		</section>
	</chapter>


	<chapter>
		<title>Einstellen Ihres Datums und der Zeit</title>
		<section>
			<body>

				<p>Nun müssen Sie Ihr Datum und Ihre Zeit einstellen.  Sie können dies
				mit dem <c>date</c> Kommando machen</p>

<pre caption="Setzen des Datums">
# <i>date</i>
Thu Feb 27 09:04:42 CST 2003
<comment>(Falls Ihr Datum falsch ist, setzen Sie es mit nächsten Kommando)</comment>
# <i>date 022709042003</i>
<comment>(date MMDDhhmmCCYY)</comment>
</pre>

			</body>
		</section>
	</chapter>


	<chapter>
		<title>Dateisysteme, Partitionen und Block-Geräte</title>
		<section>
			<title>Einführung in Block-Geräte</title>
			<body>

				<p>In diesem Abschnitt werden die Festplatten-orientierten Askpekte von
				Gentoo Linux und Linux im Allgemeinen, sowie die Linux Dateisysteme,
				Partitionen und Block-Geräte, erläutert.  Dann, sobald Sie damit
				vertraut sind, werden Sie durch den Prozess zur Einrichtung von
				Partitionen und Dateisystemen für Ihr Gentoo Linux geführt.</p>

				<p>Zu Beginn werden die "Block-Geräte" eingeführt.  Das
				bekannteste Block-Gerät ist vermutlich das, welches die erste
				IDE-Festplatte in einem Linux-System repräsentiert:</p>

<pre caption="/dev/hda, das Block-Gerät, das die erste IDE-Festplatte repräsentiert">
/dev/hda
</pre>

				<p>Falls Sie in Ihrem System SCSI-Laufwerke verwenden, dann wird Ihre
				erste Festplatte diese sein:</p>

<pre caption="/dev/sda, das Block-Gerät, das die erste logische SCSI-Festplatte repräsentiert">
/dev/sda
</pre>

				<p>Die oben genannten Block-Geräte repräsentieren eine <e>allgemeine</e>
				Schnittstelle zu der Festplatte.  Programme können diese Block-Geräte
				verwenden, um auf die Festplatte zuzugreifen, ohne sich darum zu
				kümmern, ob sie IDE, SCSI oder irgendwas anderes ist.  Das Programm kann
				einfach auf die gespeicherten Daten der Festplatte in Bündeln von
				zusammenhängenden, frei addressierbaren, 512-Byte-Blöcken zugreifen.</p>

			</body>
		</section>

		<section>
			<title>Partitionen und fdisk</title>
			<body>

				<p>Unter Linux erstellen Sie Dateisysteme mit dem speziellen Kommando
				<c>mkfs</c> (oder <c>mke2fs</c>, <c>mkreiserfs</c>, usw.) unter der
				Angabe eines bestimmten Block-Gerätes als Kommandozeilen-Argument.</p>

				<p>Wie auch immer, obwohl es theoretisch möglich ist, eine "komplette
				Festplatte" als Block-Gerät (eines, das die <e>gesamte</e> Festplatte
				repräsentiert) zu benutzen, um ein einziges Dateisystem darauf
				unterzubringen, wird dieses in der Praxis so gut wie nie angewandt.
				Stattdessen wird eine Festplatte in mehrere kleine Block-Geräte, so
				genannte "Partitionen", augfeteilt.  Die Partitionen werden mit dem
				Programm <c>fdisk</c> erzeugt, das benötigt wird, um die auf jeder
				Festplatte gespeicherte Partitionstabelle zu erzeugen und zu ändern.</p>

				<p>Sie können die Partitionstabelle ansehen, indem Sie <c>fdisk</c> mit
				dem Argument eines Block-Gerätes aufrufen:</p>

				<note>Alternative Programme, um die Partitionstabelle einer Festplatte
				anzusprechen, sind <c>cfdisk</c>, <c>parted</c> oder
				<c>partimage</c>.  Die Empfehlung lautet jedoch <c>fdisk</c>, da es sehr
				funktionsreich und in der Unix/Linux-Welt gut bekannt ist.</note>

<pre caption="fdisk starten">
# <i>fdisk /dev/hda</i>
</pre>
<p>oder</p>
<pre caption="fdisk starten und die Partitionstabelle auf /dev/sda ansehen">
# <i>fdisk /dev/sda</i>
</pre>

				<impo>Beachten Sie, dass Sie die Partitiontabelle <e>nicht</e> speichern
				oder Änderungen an ihr vornehmen, wenn irgendeine dieser Partitionen
				über Dateisysteme verfügen, die in Benutzung sind, oder wichtige Daten
				beinhalten.  Dieses führt meistens zum Verlust von Daten auf dieser
				Festplatte.</impo>

				<p>Bei fdisk werden Sie mit einer Ausgabe wie der folgenden begrüßt:</p>

<pre caption="Das fdisk Prompt">
Command (m for help):
</pre>

				<p>Geben Sie <c>p</c> ein, um Ihre aktuelle Partitionsaufteilung
				darzustellen:</p>

<pre caption="Eine beispielhafte Partitionsaufteilung">
Command (m for help): <i>p</i>

Disk /dev/hda: 240 heads, 63 sectors, 2184 cylinders
Units = cylinders of 15120 * 512 bytes

   Device Boot    Start       End    Blocks   Id  System
/dev/hda1   *         1        14    105808+  83  Linux
/dev/hda2            15        49    264600   82  Linux swap
/dev/hda3            50        70    158760   83  Linux
/dev/hda4            71      2184  15981840    5  Extended
/dev/hda5            71       209   1050808+  83  Linux
/dev/hda6           210       348   1050808+  83  Linux
/dev/hda7           349       626   2101648+  83  Linux
/dev/hda8           627       904   2101648+  83  Linux
/dev/hda9           905      2184   9676768+  83  Linux

Command (m for help):
</pre>

				<p>Diese Festplatte ist so partitioniert, dass sie sieben Linux
				Dateisysteme (jede entsprechene Partition wird als "Linux" gelistet),
				sowie eine swap Partition (gelistet als "Linux swap") besitzt.</p>

				<p>Beachten Sie die Namensgebung der Partitionen zugehörigen
				Block-Geräte auf der linken Seite, beginnend mit <path>/dev/hda1</path>
				bis zu <path>/dev/hda9</path>.  In den frühen PC-Tagen erlaubte die
				Partitionierungs-Software maximal vier Partitionen (sogenannte "primäre
				Partitionen").  Das war zu begrenzt, sodass eine Erweiterung mit
				<e>erweiterten Partitionen</e> geschaffen wurde.  Eine erweiterte
				Partition ist sehr ähnlich zu einer primären Partition und wird
				innerhalb der ersten vier primären Partitionen verwendet.  Dennoch,
				erweiterte Partitionen können eine beliebige Anzahl an sogenannten
				<e>logischen Partitionen</e> beinhalten, wodurch eine effiziente
				Möglichkeit, das Limit von vier Partitionen zu umgehen, geboten
				wird.</p>

				<p>Alle Partitionen von <path>/dev/hda5</path> an und höher sind
				logische Partitionen.  Die Nummern von 1 bis 4 sind reserviert für
				primäre oder erweiterte Partitionen.</p>

				<p>Somit sind in dem oben genannten Beispiel <path>/dev/hda1</path> bis
				<path>/dev/hda3</path> primäre Partitionen.  <path>/dev/hda4</path> ist
				eine erweiterte Partition, die die logischen Partitionen
				<path>/dev/hda5</path> bis <path>/dev/hda9</path> beinhaltet.  Sie
				werden niemals <path>/dev/hda4</path> benutzen, um darin direkt
				Dateisysteme zu generieren -- es arbeitet einfach nur als ein Container
				für die Partitionen <path>/dev/hda5</path> bis <path>hda9</path>.</p>

				<p>Beachten Sie außerdem, dass jede Partition eine "ID"
				besitzt, den sogenannten "Partitionstyp".  Immer, wenn Sie
				eine neue Partition erstellen, sollten Sie darauf achten, dass der
				Partitionstyp korrekt angegeben ist.  '83' ist der korrekte
				Partitionstyp für eine Partition, die ein Linux Dateisystem beinhaltet,
				'82' ist der korrekte Partitionstyp für eine Linux swap
				Partition und 'fd' der korrekte Partitionstyp für Software
				RAID-Partitionen.  Sie können den Partitionstypen mit der Option
				<c>t</c> in <c>fdisk</c> setzen.  Der Linux Kernel verwendet den
				Partitionstypen, um automatisch Dateisysteme und swap Partitionen beim
				Booten zu erkennen.</p>

			</body>
		</section>

		<section>
			<title>fdisk zur Partitionierung verwenden</title>
			<body>

				<p>Da Sie nun eine Einführung in die Festplattenpartitionierung unter
				Linux erhalten haben, ist es an der Zeit, den Prozess zur Erstellung von
				Partitionen für Ihre Gentoo Linux Installation zu durchschreiten.
				Dadurch werden Sie eine Partitionierung ähnlich zu dieser haben:</p>

<pre caption="Die Partitionierung nach Durchführung der folgenden
Schritte">
Disk /dev/hda: 30.0 GB, 30005821440 bytes
240 heads, 63 sectors/track, 3876 cylinders
Units = cylinders of 15120 * 512 = 7741440 bytes

   Device Boot    Start       End    Blocks   Id  System
/dev/hda1   *         1        14    105808+  83  Linux
/dev/hda2            15        81    506520   82  Linux swap
/dev/hda3            82      3876  28690200   83  Linux

Command (m for help):
</pre>

				<p>In unserer vorgeschlagenen "Anfänger" Partitionierung haben wir drei
				Partitionen.  Die erste (<path>/dev/hda1</path>) am Anfang der
				Festplatte ist eine kleine, sogenannte "boot Partition".  Der
				Zweck der boot Partition ist, alle kritischen Daten, die zum Booten
				benötigt werden zu beinhalten -- die GRUB Bootmanager Informationen
				(falls Sie GRUB verwenden werden) als auch Ihre(n) Linux Kernel.  Die
				boot Partition biete einen sicheren Ort, um alles wichtige zu speichern,
				was zum Booten von Linux benötigt wird.  Während der alltäglichen
				Benutzung von Gentoo Linux sollte Ihre boot Partition zur Sicherheit
				<e>ungemountet</e> bleiben.  Wenn Sie ein SCSI System aufsetzen, wird
				Ihre boot Partition <path>/dev/sda1</path> sein.</p>

				<p>Es ist empfohlen, eine boot Partition am Anfang einer Festplatte
				einzurichten.  Obwohl es inzwischen egal ist, wo sich die boot Partition
				befindet, ist es eine Tradition aus den Tagen, als der LILO Bootmanager
				noch keine Kernel von Dateisystemen oberhalb des Zylinders 1024 laden
				konnte.</p>

				<p>Die zweite Partition (<path>/dev/hda2</path>) wird für den "swap
				Bereich" benötigt.  Der Kernel verwendet swap Bereiche als einen
				virtuellen Speicher, wenn der vorhandene RAM zur Neige geht.  Diese
				Partition ist, relativ gesehen, auch nicht sehr groß; üblicherweise etwa
				512MB.  Wenn Sie ein SCSI-System verwenden, wird die Partition
				<path>/dev/sda2</path> heißen.</p>

				<p>Die dritte Partition (<path>/dev/hda3</path>) ist recht groß und
				nimmt den kompletten Rest der Festplatte ein.  Diese Partition wird
				"root Partition" genannt und wird für das Haupt-Dateisystem, das Gentoo
				Linux selbst beinhaltet, verwendet.  Bei einem SCSI-System wird diese
				Partition <path>/dev/sda3</path> sein.</p>

				<p>Bevor wir die Festplatte partitionieren, hier ein kurzer technischer
				Überblick der empfohlenen Partitionen und zugehörigen Dateisysteme für
				die Installation von Gentoo Linux:</p>

				<table>
					<tr>
						<th>Partition</th>
						<th>Größe</th>
						<th>Dateisystem</th>
						<th>beispielhaftes Gerät</th>
					</tr>
					<tr>
						<ti>boot Partition, beinhaltet Kernel und Boot Informationen</ti>
						<ti>32 Megabyte</ti>
						<ti>ext2/3 stark empfohlen (am einfachsten); falls ReiserFS, dann
						mit <c>-o notail</c> mounten.  Außerdem wird für ext3 oder ReiserFS
						Platz für das Journal benötigt; in diesem Fall sollte die Partition
						64MB groß sein.</ti>
						<ti><path>/dev/hda1</path></ti>
					</tr>
					<tr>
						<ti>swap Partition (kein 128MB limit, nun 2GB)</ti>
						<ti>üblicherweise hat der swap Bereich eine Größe von etwa ein bis
						zwei Mal des physischen RAM</ti>
						<ti>Linux swap</ti>
						<ti><path>/dev/hda2</path></ti>
					</tr>
					<tr>
						<ti>root Partition, beinhaltet das Haupt-Dateisystem
						(<path>/usr</path>, <path>/home</path>, usw.)</ti>
						<ti>&gt;=1.5 Gigabyte</ti>
						<ti>ReiserFS oder ext3 empfohlen; ext2 ist okay</ti>
						<ti><path>/dev/hda3</path></ti>
					</tr>
				</table>

				<p>Gut, nun erzeugen Sie die Partitionen wie in dem oben genannten
				Beispiel.  Zuerst rufen Sie fdisk mit <c>fdisk /dev/hda</c> oder
				<c>fdisk /dev/sda</c>, abhängig ob Sie IDE oder SCSI verwenden, auf.
				Dann geben Sie <c>p</c> ein, um Ihre aktuelle Partitionierung anzusehen.
				Gibt es etwas auf der Festplatte, das Sie weiterhin benötigen?  Dann
				<b>stoppen Sie jetzt</b>.  Wenn Sie mit diesen Anweisungen fortfahren,
				werden <b>alle Ihre vorhandenen Daten auf der Festplatte
				gelöscht</b>.</p>

				<impo>Eine weitere Befolgung der Anweisungen bewirkt ein <b>Löschen</b>
				aller ihrer bereits vorhanden Daten auf Ihrer Festplatte!  Falls sich
				etwas auf der Festplatte befindet, seien Sie sich sicher, dass es keine
				kritischen Daten sind und deren Verlust Sie verschmerzen können.  Seien
				Sie sich außerdem sicher, dass Sie die <b>korrekte Festplatte gewählt
				haben</b>, sodass Sie nicht aus Versehen Daten auf der falschen
				Festplatte löschen.</impo>

				<p>Nun ist es an der Zeit alle existierenden Partitionen zu löschen.
				Dazu tippen Sie <c>d</c> ein und drücken Enter.  Sie werden nach der
				Partitionsnummer gefragt, die Sie löschen möchten.  Um eine existierende
				<path>/dev/hda1</path> Partition zu löschen, gehen Sie so vor:</p>

<pre caption="Löschen einer Partition">
Command (m for help): <i>d</i>
Partition number (1-4): <i>1</i>
</pre>

				<p>Die Partition wird zum Löschen vorgesehen.  Sie wird nicht mehr
				angezeigt, wenn Sie <c>p</c> eingeben, aber sie wird nicht eher
				gelöscht, bevor Sie die Änderungen speichern.  Wenn Sie einen Fehler
				gemacht haben und ohne Speichern der Änderungen fdisk beenden wollen,
				geben Sie sofort <c>q</c> ein und drücken Sie Enter, sodass Ihre
				Partition nicht gelöscht wird.</p>

				<p>Nun, angenommen Sie wollen wirklich alle vorhandenen Partitionen
				löschen, geben Sie wiederholt <c>p</c> ein, um die Partitionen zu listen
				und dann <c>d</c>, gefolgt von der Partitionsnummer, die Sie löschen
				möchten.  Letzten Endes werden Sie eine leere Partitionstabelle
				erhalten:</p>

<pre caption="Eine leere Partitionstabelle">
Disk /dev/hda: 30.0 GB, 30005821440 bytes
240 heads, 63 sectors/track, 3876 cylinders
Units = cylinders of 15120 * 512 = 7741440 bytes

   Device Boot    Start       End    Blocks   Id  System

Command (m for help):
</pre>

				<p>Mit einer im Speicher befindlichen leeren Partitionstabelle, sind Sie
				bereit, eine boot Partition zu erstellen.  Dazu geben Sie <c>n</c> ein,
				um eine neue Partition zu erstellen, dann <c>p</c>, um fdisk
				mitzuteilen, dass Sie eine primäre Partition wünschen.  Dann drücken Sie
				<c>1</c> zur Generierung der ersten primären Partition.  Wenn Sie nach
				dem ersten Zylinder gefragt werden, drücken Sie einfach Enter.  Bei der
				Nachfrage zum letzten Zylinder geben Sie <c>+32M</c> ein, um eine 32MB
				große Partition zu erstellen.  Die Ausgabe dieser Schritte sieht
				folgendermaßen aus:</p>

				<note>Journalling Dateisysteme benötigen zusätzlichen Platz für das
				Journal.  Die Standardeinstellungen benötigen etwa 33MB an Platz.  Wenn
				Sie also ein Journalling Dateisystem für <path>/boot</path> verwenden,
				sollten Sie <c>+64M</c> eingeben, wenn Sie nach dem letzten Zylinder
				gefragt werden.</note>

<pre caption="Schritte zur Erstellung der boot Partition">
Command (m for help): <i>n</i>
Command action
   e   extended
   p   primary partition (1-4)
<i>p</i>
Partition number (1-4): <i>1</i>
First cylinder (1-3876, default 1): <comment>(Drücken Sie Enter)</comment>
Using default value 1
Last cylinder or +size or +sizeM or +sizeK (1-3876, default 3876): <i>+32M</i>
</pre>

				<p>Geben Sie nun <c>p</c> ein, sehen Sie die folgende
				Partitionierung:</p>

<pre caption="Die erste Partition wurde erzeugt">
Command (m for help): <i>p</i>

Disk /dev/hda: 30.0 GB, 30005821440 bytes
240 heads, 63 sectors/track, 3876 cylinders
Units = cylinders of 15120 * 512 = 7741440 bytes

   Device Boot    Start       End    Blocks   Id  System
/dev/hda1             1        14    105808+  83  Linux
</pre>

				<p>Als nächstes erzeugen Sie eine swap Partition.  Dazu geben Sie
				<c>n</c> ein, um eine neue Partition zu erzeugen, dann <c>p</c>, um
				fdisk mitzuteilen, dass Sie eine primäre Partition wünschen.  Darauf
				<c>2</c> zur Erzeugung der zweiten primären Partition, in diesem Fall
				also <path>/dev/hda2</path>.  Werden Sie nach dem ersten Zylinder
				gefragt, drücken Sie Enter.  Bei der Nachfrage zum letzten Zylinder,
				geben Sie <c>+512M</c> ein, um eine Partition von 512MB zu erstellen.
				Darauf drücken Sie <c>t</c>, um den Partitionstyp zu setzen, <c>2</c> um
				die soeben erzeugte Partition auszuwählen und geben <c>82</c> ein,
				entsprechend für einen "Linux Swap" Partitionstyp.  Nachdem
				Sie diese Schritte durchgeführt haben, sollte <c>p</c> ähnlich
				zu dem Folgenden aussehen:</p>

<pre caption="Die swap Partition wurde erzeugt">
Command (m for help): <i>p</i>

Disk /dev/hda: 30.0 GB, 30005821440 bytes
240 heads, 63 sectors/track, 3876 cylinders
Units = cylinders of 15120 * 512 = 7741440 bytes

   Device Boot    Start       End    Blocks   Id  System
/dev/hda1             1        14    105808+  83  Linux
/dev/hda2            15        81    506520   82  Linux swap
</pre>

				<p>Abschließend erzeugen Sie die root Partition.  Dazu drücken Sie
				<c>n</c>, um eine neue Partition zu erzeugen, dann <c>p</c>, um fdisk
				mitzuteilen, dass Sie eine primäre Partition wünschen.  Dann drücken Sie
				<c>3</c> für die Generierung der dritten primären Partition, also
				<path>/dev/hda3</path>.  Wird nach dem ersten Zylinder gefragt, drücken
				Sie Enter.  Bei der Nachfrage zum letzten Zylinder, drücken Sie ebenso
				Enter, um eine Partition, die den restlichen freien Platz einnimmt, zu
				erzeugen.  Darauf sollte <c>p</c> eine Ausgabe ähnlich zu der folgenden
				darstellen:</p>

<pre caption="Die root Partition wurde erzeugt">
Command (m for help): <i>p</i>

Disk /dev/hda: 30.0 GB, 30005821440 bytes
240 heads, 63 sectors/track, 3876 cylinders
Units = cylinders of 15120 * 512 = 7741440 bytes

   Device Boot    Start       End    Blocks   Id  System
/dev/hda1             1        14    105808+  83  Linux
/dev/hda2            15        81    506520   82  Linux swap
/dev/hda3            82      3876  28690200   83  Linux
</pre>

				<p>Abschließend muss noch das "bootable" Flag bei der boot
				Partition gesetzt und die Änderungen abgespeichert werden.  Um
				<path>/dev/hda1</path> als eine bootbare Partition zu kennzeichnen,
				geben Sie im Menu <c>a</c> gefolgt von <c>1</c> für die Partitionsnummer
				ein.  Schauen Sie sich nun die Partitiontabelle mit <c>p</c> an, sehen
				Sie, dass <path>/dev/hda1</path> ein <c>*</c> in der Spalte "Boot"
				besitzt.  Schreiben Sie nun die Änderungen auf die Festplatte.  Dazu
				geben Sie <c>w</c> ein und drücken Enter.  Ihre Festplatte ist nun
				ordentlich für ein Gentoo Linux konfiguriert.</p>

				<note>Falls <c>fdisk</c> oder <c>cfdisk</c> Sie anweist, neu zu starten,
				damit Ihr System die Änderungen an der Partitionstabelle akzeptiert,
				dann tun Sie dieses bitte.</note>

			</body>
		</section>

		<section>
			<title>Dateisysteme erzeugen</title>
			<body>

				<p>Da nun die Partionen erzeugt wurden, ist es an der Zeit, die
				Dateisysteme auf der boot und root Partition einzurichten, sodass sie
				zur Datenspeicherung in Ihr System eingebunden werden können.  Außerdem
				wird die swap Partition konfiguriert, um als swap Bereich zu dienen.</p>

				<p>Gentoo Linux unterstützt eine Auswahl von unterschiedlichen
				Dateisystemen; jedes hat seine Stärken und Schwächen und seine eigene
				Zusammenstellung an charakteristischer Leistung.  Derzeit wird die
				Generierung von ext2, ext3, XFS, JFS und ReiserFS Dateisystemen
				unterstützt.</p>

				<p>ext2 ist ein erprobtes Linux Dateisystem, unterstützt aber kein
				Journaling, was dazu führt, dass die routinemäßigen ext2 Dateisystem
				Prüfungen während des Startens recht zeitintensiv sind.  Daher gibt es
				eine Auswahl von neueren <e>Journaling</e> Dateisystemen, deren
				Konsistenz sehr schnell überprüft werden kann und von daher
				grundsätzlich den Nicht-Journaling Dateisystemen vorgezogen werden
				sollten.  Journaling Dateisysteme beugen langen Wartezeiten beim Booten
				Ihres System mit <e>inkonsistenten</e> Dateisystemen vor.</p>

				<p>ext3 ist das entsprechende Journaling Dateisystem zu ext2, welches
				Metadaten Journaling für eine schnelle Wiederherstellung nutzt,
				zusätzlich zu anderen verbesserten Journaling Modi, wie komplettes Daten
				und geordnetes Daten Journaling.  ext3 ist ein sehr gutes und
				zuverlässiges Dateisystem.  Es bietet grundsätzlich anständige Leistung
				während der meisten Einsatzumgebungen.  Dadurch, dass es im internen
				Design nicht sehr stark von "trees" Gebrauch macht, skaliert
				es nicht sehr gut; sprich, es ist keine gute Wahl für ein sehr großes
				Dateisystem oder Situationen in denen Sie mit großen Dateien oder einer
				großen Anzahl von Dateien in einem einzigen Verzeichnis arbeiten werden.
				Solange Sie es innerhalb seiner Design Parameter verwenden, ist ext3 ein
				exzellentes Dateisystem.</p>

				<p>ReiserFS ist ein B*-tree basiertes Dateisystem, das im Großen und
				Ganzen eine gute Leistung bietet und ext2 und ext3 weit übertrifft, wenn
				Sie mit kleinen Dateien (Dateien kleiner als 4kB) arbeiten, meistens mit
				einem Faktor von 10- bis 15-fach.  ReiserFS skaliert außerdem sehr gut
				und hat ein Metadata Journaling.  Seit Kernel 2.4.18 ist ReiserFS sicher
				und die erste Wahl sowohl für ein Mehrzweck-Dateisystem als auch für
				Extremfälle, wie großen Dateisystemen, die Verwendung von vielen kleinen
				Dateien, sehr großen Dateien und Verzeichnissen mit tausenden von
				Dateien.  ReiserFS ist das Dateisystem, das wir standardmäßig für alle
				nicht-boot Partitionen empfehlen.</p>

				<p>XFS ist ein Dateisystem mit Metadata Journaling, das durchgängig mit
				dem Gentoo Linux <c>xfs-sources</c> Kernel kooperiert.  Es beinhaltet
				ein robustes Feature-Set und ist für Skalierbarkeit optimiert.  Wir
				empfehlen, dieses Dateisystem nur in high-end SCSI und/oder
				Glasfaserkanal-Speicherung mit einer ununterbrechbaren Stromversorgung
				zu betreiben.  Dadurch, dass XFS sehr häufig die in Bewegung
				befindlichen Daten im RAM speichert, können unsachgemäß erstellte
				Programme (diese, die sich nicht um saubere Vorkehrungen beim Speichern
				auf Festplatte kümmern -- und davon gibt es einige) zu Datenverlust
				führen, wenn das Programm unvorhergesehen beendet wird.</p>

				<p>JFS ist ein eigens von IBM erstelltes hochleistungsfähiges Journaling
				Dateisystem.  Es wurde kürzlich erst produktionsreif, weshalb es noch
				keine Auflistung an Vor- oder Nachteilen zu seiner grundsätzlichen
				Stabilität gibt.</p>

				<p>Möchten Sie das stabilste Journaling Filesystem verwenden, wählen Sie
				ext3.  Für ein gutes Mehrzweck-Dateisystem mit hoher Leistung und
				Journaling, verwenden Sie ReiserFS.  Beide, ext3 und ReiserFS, sind voll
				entwickelt, raffiniert und empfohlen für den allgemeinen Gebrauch.</p>

				<p>Ausgehend von der oben genannten Beispielpartitionierung, geben Sie
				die folgenden Kommandos ein, um Ihre Dateisysteme zu generieren:</p>

<pre caption="Generierung der Dateisysteme (Beispiel)">
# <i>mke2fs -j /dev/hda1</i>
# <i>mkswap /dev/hda2</i>
# <i>mkreiserfs /dev/hda3</i>
</pre>

				<p>Für die boot Partition <path>/dev/hda1</path> wurde ext3 gewählt, da
				es ein robustes Jornaling Dateisystem ist, das von allen Bootmanagern
				unterstützt wird.  <c>mkswap</c> ist für die swap Partition
				<path>/dev/hda2</path> -- die Wahl ist hier offensichtlich.  Für die
				root Partition <path>/dev/hda3</path> wurde ReiseFS gewählt, da es ein
				solides Journaling Dateisystem mit einer hervorragenden Leistung ist.
				Nun, fahren Sie fort und generieren Sie die Dateisysteme.</p>

				<p>Als Referenz dient diese Auflistung der verschiedenen <c>mkfs</c>
				Kommandos, die während der Installation zur Verfügung stehen:</p>

				<p><c>mkswap</c> ist das Kommado, um eine swap Partition zu
				initialisieren:</p>

<pre caption="swap Partition initialisieren">
# <i>mkswap /dev/hda2</i>
</pre>

				<p>Sie können <c>mke2fs</c> benutzen, um ein ext2-Dateisystem zu
				erstellen:</p>

<pre caption="Ein ext2 Dateisystem erzeugen">
# <i>mke2fs /dev/hda1</i>
</pre>

				<p>Wenn Sie ext3 vorziehen, können Sie es mit <c>mke2fs -j</c>
				erzeugen:</p>

<pre caption="Ein ext3 Dateisystem erzeugen">
# <i>mke2fs -j /dev/hda3</i>
</pre>

				<note>Mehr Informationen über ext3 mit Linux 2.4 finden Sie unter
				<uri>http://www.zip.com.au/~akpm/linux/ext3/ext3-usage.html</uri>.</note>

				<p>Für die Erstellung eines ReiserFS Dateisystems, verwenden Sie das
				<c>mkreiserfs</c> Kommando:</p>

<pre caption="Ein ReiserFS Dateisystem erzeugen">
# <i>mkreiserfs /dev/hda3</i>
</pre>

				<p>Zum Erstellen eines XFS Dateisystems, rufen Sie <c>mkfs.xfs</c>
				auf:</p>

<pre caption="Ein XFS Dateisystem erzeugen">
# <i>mkfs.xfs /dev/hda3</i>
</pre>

				<note>Möglicherweise möchten Sie weitere Attribute an <c>mkfs.xfs</c>
				überreichen: <c>-d agcount=3 -l size=32m</c>.  Das <c>-d agcount=3</c>
				Attribut verringert die Anzahl der Zuordnungsgruppen.  XFS besteht auf
				mindestens eine Zuordnungsgruppe je vier Gigabyte Ihrer Partition,
				wodurch z.B. eine 20GB Partition mindestens ein agcount von 5 benötigt.
				Das <c>-l size=32m</c> Attribut erhöht die Journalgröße auf 32 MB und
				verbessert dadurch die Leistung.</note>

				<p>Um ein JFS Dateisystem zu erstellen, verwenden Sie
				<c>mkfs.jfs</c>:</p>

<pre caption="Ein JFS Dateisystem erzeugen">
# <i>mkfs.jfs /dev/hda3</i>
</pre>

			</body>
		</section>
	</chapter>


	<chapter>
		<title>Partitionen mounten</title>
		<section>
			<body>

				<p>Aktivieren Sie nun Ihre Swap Partition, denn es könnte sein, dass Sie
				den virtuellen Speicher später noch benötigen:</p>

<pre caption="swap Partition aktivieren">
# <i>swapon /dev/hda2</i>
</pre>

				<p>Als nächstes erstellen Sie das Verzeichnis
				<path>/mnt/gentoo/boot</path>, anschließend mounten Sie Ihre
				Partitionen.  Sobald diese gemountet sind, wird jede Datei, die in
				<path>/mnt/gentoo</path> kopiert wird, in das neue Dateisystem
				geschreiben.  Wenn Sie Gentoo Linux mit separaten <path>/usr</path> oder
				<path>/var</path> Partitionen installieren, müssen diese in die
				entsprechenden Verzeichnisse gemountet werden
				(<path>/mnt/gentoo/usr</path> und <path>/mnt/gentoo/var</path>).</p>

				<impo>Wenn Ihre <path>/boot</path> Partition, dort wo der Kernel
				unterbracht ist, eine ReiserFS Partition ist, achten Sie darauf, dass
				diese mit der <c>-o notail</c> Option gemountet wird, sonst wird GRUB
				nicht richtig installiert. Stellen Sie auch sicher, dass die Option
				<c>notail</c> mit in die <path>/etc/fstab</path> eingetragen wird, doch
				dazu später mehr.  Wenn Sie LILO mit ReiserFS verwenden möchten, dann
				ist die <c>-o notail</c> Option nicht notwendig.  Dennoch ist es immer
				sicher, die <c>-o notail</c> Option mit ReiserFS zu verwenden, wenn Sie
				nicht wissen, was zu tun ist.</impo>

<pre caption="Mount Verzeichnisse erzeugen">
# <i>mount /dev/hda3 /mnt/gentoo</i>
# <i>mkdir /mnt/gentoo/boot</i>
# <i>mount /dev/hda1 /mnt/gentoo/boot</i>
</pre>

				<impo>Sollten Sie Probleme haben, Ihre boot Partition als ext2 zu
				mounten, versuchen Sie es mit <c>mount /dev/hda1 /mnt/gentoo/boot -t
				ext2</c>.</impo>

			</body>
		</section>
	</chapter>


	<chapter>
		<title>Stage tar-Archive und chroot</title>
		<section>
			<title>Wahl des gewünschten stage tar-Archivs</title>
			<body>

				<p>Nun sollten Sie sich endgültig entscheiden, mit welchem stage Sie
				ihre Installation durchführen wollen.  Die stage tar-Archive sind auf
				der Live CD im Verscheichnis <path>/mnt/cdrom/stages</path> verfügbar;
				Sie können <c>ls /mnt/cdrom/stages</c> eingeben, um eine Liste der
				verfügbaren stage tar-Archive zu erhalten.</p>

				<p><b>GRP Benutzer</b> mögen das <path>stage3-xx-yy.tar.bz2</path>
				tar-Archiv verwenden.</p>

				<p>Wenn Sie eine Installation von einem stage tar-Archiv durchführen
				wollen, das <e>nicht</e> auf der CD ist (was der Fall sein wird, wenn
				Sie unsere "basic" Live CD verwenden), müssen Sie es mit den folgenden
				Anweisungen herunterladen.  Falls Sie das stage tar-Archiv, das Sie
				benutzen wollen, bereits besitzen (bei den meisten Benutzern üblich),
				dann fahren Sie mit "Entpacken des stage tar-Archivs" fort.</p>

<pre caption="Herunterladen des gewünschten stage tar-Archivs">
# <i>cd /mnt/gentoo</i>
<comment>Verwenden Sie lynx, um die URL des tar-Archivs zu erhalten:</comment>
# <i>lynx http://gentoo.oregonstate.edu/releases/x86/1.4/</i>
<comment>Verwenden Sie die <c>hoch</c> und <c>runter</c> Cursor-Tasten (oder
benutzen Sie <c>TAB</c>), um das gewünschte Verzeichnis hervorzuheben.
Drücken Sie dann <c>d</c> wodurch der Download gestartet wird.  Speichern Sie
die Datei und beenden Sie den Browser mit <c>q</c>.

<b>Oder</b> verwenden Sie <c>wget</c> von der Kommandozeile:</comment>
# <i>wget <comment>fügen Sie hier den URL zum gewünschten tar-Archiv ein</comment></i>
</pre>

			</body>
		</section>

		<section>
			<title>Entpacken des stage tar-Archivs</title>
			<body>

				<p>Nun ist es an der Zeit das gepackte stage tar-Archiv Ihrer Wahl in
				<path>/mnt/gentoo/</path> zu entpacken.  Bedenken Sie, dass Sie nur
				<b>ein</b> stage tar-Archiv zu entpacken brauchen, also nur stage1,
				stage2 oder stage3.  Möchten Sie eine Installation von Gentoo mit stage3
				durchführen, dann packen Sie es folgendermaßen aus:</p>

				<impo>Stellen Sie sicher, dass Sie das Argument <c>p</c> mit <c>tar</c>
				verwenden.  Vergessen Sie dieses, werden die Dateien mit falschen
				Rechten entpackt.</impo>

<pre caption="Entpacken des stage tar-Archivs">
# <i>cd /mnt/gentoo</i>
<comment>Ändern Sie "stage3" in "stage2" oder "stage1", wenn Sie von diesen
stages an installieren möchten.</comment>
<comment>Wenn Sie ein stage tar-Archiv heruntergeladen haben, ändern Sie den
Pfad von "/mnt/cdrom/stages/" in "/mnt/gentoo/".</comment>
# <i>tar -xvjpf /mnt/cdrom/stages/stage3-*.tar.bz2</i>
</pre>

				<p>Wenn sie Ihr stage tar-Archiv nach
				<path>/mnt/gentoo/</path>heruntergeladen haben, können Sie dieses mit
				<c>rm /mnt/gentoo/stage*.tar.bz2</c> löschen.</p>

			</body>
		</section>

		<section>
			<title>Schritte für ein GRP Portage Paket</title>
			<body>

				<impo>Die folgenden Anweisungen sind nur für GRP Benutzer.</impo>

				<p><b>GRP Benutzer:</b> Auf der CD existiert eine "Momentanaufnahme" von
				Portage.  Benutzen Sie diese Momentanaufnahme, um das später
				durchzuführende <c>emerge sync</c> auszulassen, denn <c>emerge sync</c>
				benötigt eine Netzwerkverbindung.  Entpacken Sie die die
				Momentanaufnahme folgendermaßen:</p>

<pre caption="Verwenden einer Portage Momentanaufnahme">
<comment>Ersetzen Sie yyyymmdd mit dem Zeitstempel im Dateinamen.</comment>
# <i>tar -xvjf /mnt/cdrom/snapshots/portage-yyyymmdd.tar.bz2 -C /mnt/gentoo/usr</i>
</pre>

				<p>Damit entpacken Sie die Momentanaufname des Portage Tree in Ihre
				frische Gentoo Installation.  Nun brauchen Sie keine Internetanbindung
				mehr, um <c>emerge sync</c> durchzuführen und den Portage Tree
				herunterzuladen.  Kopieren Sie jetzt die distfiles und Pakete von der
				Live CD an ihren zukünftigen Ort:</p>

<pre caption="Kopieren der GRP Dateien">
# <i>cp -R /mnt/cdrom/distfiles /mnt/gentoo/usr/portage/distfiles</i>
# <i>cp -a /mnt/cdrom/packages /mnt/gentoo/usr/portage/packages</i>
</pre>

				<p>Alle relevanten Dateien für GRP sind nun an ihrem benötigten Platz.
				Sie haben alles kopiert und entpackt, um Gentoo Linux zu installieren --
				selbst ohne eine Netzwerkanbindung.</p>

			</body>
		</section>

		<section>
			<title>Auswahl eines Mirror-Server (optional)</title>
			<body>

				<p><c>mirrorselect</c> ist ein Programm, dass dazu entwickelt wurde,
				automatisch den schnellsten Mirror-Server abhängig von Ihrem Ort zu
				wählen oder manuell einen Mirror-Server aus einer Liste auszuwählen.
				Unglücklicherweise läuft <c>mirrorselect</c> nicht hinter jedem Router
				einwandfrei.</p>

<pre caption="mirrorselect verwenden">
<comment>Automatische Auswahl eines Mirror-Server:</comment>
# <i>mirrorselect -a -s4 -o &gt;&gt; /mnt/gentoo/etc/make.conf</i>
<comment>Interaktive Auswahl eines Mirror-Server:</comment>
# <i>mirrorselect -i -o &gt;&gt; /mnt/gentoo/etc/make.conf</i>
</pre>

				<p>Falls <c>mirrorselect</c> aus irgendeinem Grund keinen Mirror-Server
				eintragen konnte, können Sie dennoch mit dieser Installationsanweisung
				fortfahren, da keine Änderungen vorgenommen wurden.</p>

			</body>
		</section>

		<section>
			<title>chroot betreten</title>
			<body>

				<p>Als nächstes werden Sie in Ihre neue Gentoo Linux
				Installationsumgebung <c>chroot</c>en, und dort Ihr neues Gentoo Linux
				System "betreten"</p>

				<note>Eventuell erhalten Sie während des <c>env-update</c> eine Meldung,
				dass <path>/etc/make.profile/make.defaults</path> nicht existiert:
				Ignorieren Sie diese.  Später in diesem Dokument wird <c>emerge sync</c>
				ausgeführt, das dieses Problem löst.</note>

<pre caption="Vorbereiten und betreten der chroot Umgebung">
# <i>mount -t proc proc /mnt/gentoo/proc</i>
# <i>cp /etc/resolv.conf /mnt/gentoo/etc/resolv.conf</i>
# <i>chroot /mnt/gentoo /bin/bash</i>
# <i>env-update</i>
Regenerating /etc/ld.so.cache...
# <i>source /etc/profile</i>
<comment>(Obiges erneuert die Pfade Ihrer Shell und frischt die Binärdateien auf.)</comment>
</pre>

				<p>Nachdem Sie diese Kommandos ausgeführt haben, befinden Sie sich
				"innerhalb" Ihrer neuen Gentoo Linux Umgebung in
				<path>/mnt/gentoo</path>.  Sie können den Rest der Installation
				innerhalb dieser chroot Umgebung ausführen.</p>

			</body>
		</section>
	</chapter>


	<chapter>
		<title>Den aktuellen Portage Tree mit sync herunterladen</title>
		<section>
			<body>

				<impo>Installieren Sie mittels GRP können Sie diesen Abschnitt über
				<c>emerge sync</c> überspringen.</impo>

				<p>Als nächstes ist es nötig, <c>emerge sync</c> aufzurufen.  Mit
				diesem Kommando teilen Sie Portage mit, die aktuelle Kopie des Gentoo
				Linux Portage Tree herunterzuladen.  Haben Sie schon vorher eine
				Momentanaufnahme des Portage Tree von <e>CD 1</e> entpackt, können Sie
				diesen Schritt überspringen.  Der Portage Tree beinhaltet alle Skripte
				(sogenannte ebuilds), die für das kompilieren aller Pakete unter
				Gentoo Linux verwendet werden.  Derzeit gibt es ebuild Skripte für knapp
				4000 Pakete.  Sobald <c>emerge sync</c> beendet ist, haben Sie einen
				kompletten Portage Tree in <path>/usr/portage</path>.</p>

<pre caption="Update des Portage Tree">
# <i>emerge sync</i>
</pre>

			</body>
		</section>
	</chapter>


	<chapter>
		<title>Gentoo Optimierungen festlegen (make.conf)</title>
		<section>
			<body>

				<p>Da Sie nun eine aktuelle Kopie des Portage Tree haben, ist es an der
				Zeit, die Optimierungen und optionalen Einstellungen zum Kompilieren für
				Ihr Gentoo Linux System vorzunehmen.  Portage wird diese Einstellungen
				beim Kompilieren von jedem Programm verwenden.  Dazu editieren Sie die
				Datei <path>/etc/make.conf</path>.  Darin sollten Sie die USE Flags
				setzen, wodurch - falls vorhanden - optionale Funktionen in Ihre
				Programme einkompiliert werden; üblicherweise sind die
				Standardeinstellungen (eine <e>leere</e> oder ungesetzte USE Variable)
				okay.  Mehr Informationen über USE Flags können im <uri
				link="http://www.gentoo.org/doc/de/use-howto.xml">USE-Howto</uri>
				gefunden werden.  Weiterhin gibt es eine Liste über alle aktuellen <uri
				link="http://www.gentoo.org/dyn/use-index.xml">USE Flags</uri>.</p>

				<p>Beginnen Sie mit einem stage1 tar-Archiv, sollten Sie auch
				entsprechende Einstellungen an CHOST, CFLAGS und CXXFLAGS für Ihr System
				(Beispiele befinden sich direkt in der <path>/etc/make.conf</path>)
				vornehmen.  Benutzen Sie ein stage2 oder stage3 tar-Archiv wurden diese
				Einstellungen schon von uns optimal vorgenommen und benötigen keiner
				weiteren Modifikation.</p>

				<impo><b>Erfahrene Benutzer:</b> Die CFLAGS und CXXFLAGS Einstellungen
				werden benötigt, um dem C und C++ Compiler mitzuteilen, welche
				Optimierungen während des Kompilierens vorgenommen werden sollen.  Es
				ist z.B. für Benutzer von Athlon XP Prozessoren üblich, eine
				"-march=athlon-xp" Einstellung in CFLAGS und CXXFLAGS vorzunehmen,
				sodass alle Pakete für ihre CPU entsprechend optimiert kompiliert
				werden.  Die Datei <path>/etc/make.conf</path> enthält eine allgemeine
				Beschreibung für die richtigen Einstellungen Ihrer CFLAGS und
				CXXFLAGS.</impo>

<!-- needs qa  (direkt aus der Englischen Installation übernommen)
<note>
<b>Advanced users:</b>If you are building from a stage1 and don't want
to manually configure CFLAGS and CXXFLAGS, you can use the <c>genflags</c>
utility, which will try to guess accurate flags for your CPU architecture.
Simply type <c>emerge -O genflags</c> and then execute
<c>info2flags</c>. <c>info2flags</c> will suggest CHOST, CFLAGS and
CXXFLAGS settings, which you can then add to
<path>/etc/make.conf</path>.
</note>
-->

				<p>Falls notwendig können Sie ebenso Informationen zum Proxy eintragen,
				z.B. wenn Sie sich hinter einer Firewall befinden.  Benutzen Sie das
				folgende Kommando, um die <path>/etc/make.conf</path> mit <c>nano</c>,
				einem simplen Editor, zu bearbeiten.</p>

<pre caption="Einstellen von make.conf Optionen">
# <i>nano -w /etc/make.conf</i>
</pre>

				<note><b>Erfahrene Benutzer:</b> Möchten Sie den build Prozess im
				wesentlichen verändern, sollten Sie sich die Datei
				<path>/etc/make.globals</path> ansehen.  Diese Datei umfasst die Gentoo
				Standardeinstellungen und darf niemals verändert werden.  Wenn Ihnen
				die Default Einstellungen nicht genügen, können Sie neue Einträge in der
				<path>/etc/make.conf</path> vornehmen, da diese die Einträge in der
				<path>/etc/make.globals</path> <e>überschreiben</e>.  Wenn Sie an den
				Anpassungen der USE Variablen interessiert sind, schauen Sie sich
				<path>/etc/make.profile/make.defaults</path> an.  Möchten Sie dort
				aktivierte USE Variable abschalten, tragen Sie in der
				<path>/etc/make.conf</path> entsprechend <c>USE="-foo"</c> ein, um jede
				<c>foo</c> USE Variable abzuschalten, die standardmäßig in
				<path>/etc/make.globals</path> oder
				<path>/etc/make.profile/make.defaults</path> aktiviert ist.</note>

				<warn>Fügen Sie nicht vor dem Abschluss von stage1 '<c>static</c>' zu
				Ihrer <c>USE</c> Variable hinzu.</warn>

			</body>
		</section>
	</chapter>


	<chapter>
		<title>Beginnen von stage1</title>

		<section>
			<body>

				<note>Wenn Sie nicht von einem stage1 tar-Archiv beginnen, können Sie
				diesen Abschnitt überspringen.</note>

				<p>Das stage1 tar-Archiv ist für komplette Anpassungen und Optimierungen
				gedacht.  Wenn Sie sich für dieses tar-Archiv entschieden haben, werden
				Sie auf ein über-optimiertes und aktuelles System blicken können.  Haben
				Sie Spaß, denn Optimierung ist das, was Gentoo Linux ausmacht!  Die
				Installation von stage1 benötigt eine Menge Zeit, aber das Ergebnis ist
				ein System, das von Grund auf für Ihren Computer und Ihren Anforderungen
				optimiert wurde.</p>

				<p>Nun ist es an der Zeit den "bootstrap" Prozess zu starten.  Dieser
				Prozess benötigt selbst auf einem 1200 MHz AMD Athlon System etwa zwei
				Stunden.  Während dieser Zeit werden die GNU C Library, der Compiler und
				andere wichtige Systemprogramme gebaut.  Starten Sie den boostrap
				Prozess wie folgt:</p>

<pre caption="Bootstrapping">
# <i>cd /usr/portage</i>
# <i>scripts/bootstrap.sh</i>
</pre>

				<p>Der bootstrap Prozess beginnt nun.</p>

				<note><c>bootstrap.sh</c> bietet eine <c>--fetchonly</c> Option.  Wenn
				Sie eine Dial-Up-Verbindung verwenden, kann diese nützlich sein.  Sie
				wird alle für den bootstrap-Prozeß benötigten Dateien für die spätere
				Kompilierung in einem Schritt herunterladen.  Schauen Sie sich
				<c>bootstrap.sh -h</c> für weitere Informationen an.</note>

				<note>Portage benutzt standardmäßig das Verzeichnis
				<path>/var/tmp</path> während der Kompilierung, oftmals unter Verwendung
				von mehreren hundert Megabyte an temporären Dateien.  Wenn Sie Portage
				ein anderes Verzeichnis zum Speichern der temporären Dateien mitteilen
				wollen, setzen Sie die Variable PORTAGE_TMPDIR <e>bevor</e> Sie mit dem
				bootstrap Prozess beginnen:</note>

<pre caption="Ändern des Portage Pfades für temporäre Auslagerungsdateien">
# <i>export PORTAGE_TMPDIR="/anderes/Verzeichnis/tmp"</i>
</pre>

				<p><c>bootstrap.sh</c> wird <c>binutils</c>, <c>gcc</c>, <c>gettext</c>
				und <c>glibc</c> bauen, daraufhin <c>gettext</c> mit der neuen
				<c>glibc</c> nochmals kompilieren.  Es ist unnötig darauf hinzuweisen,
				dass dieser Prozess eine ganze Weile in Anspruch nimmt.  Sobald der
				Prozess beendet ist, entspricht Ihr System einem "stage2" System,
				weshalb Sie mit den Anweisungen zu stage2 im kommenden Abschnitt
				fortfahren können.</p>

			</body>
		</section>
	</chapter>


	<chapter>
		<title>Beginnen von stage2 und Fortsetzung von stage1</title>

		<section>
			<body>

				<note>Dieser Abschnitt ist für jene, die eine Installation von stage1 an
				fortsetzen oder mit stage2 beginnen.  Falls Sie nicht dazu gehören (z.B.
				da Sie stage3 verwenden möchten), können Sie diesen Abschnitt
				überspringen.</note>

				<warn>Wenn Sie mit stage2 beginnen ändern Sie nicht die CHOST-Variable
				in <path>/etc/make.conf</path>.  Andernfalls gibt es komische und
				umfangreiche Fehler beim Kompilieren.</warn>

				<p>Das stage2 tar-Archiv hat den bootstrapping Prozess bereits hinter
				sich gebracht.  Sie müssen lediglich den Rest des Basissystems
				installieren.</p>

				<note>Wenn Sie von einem vorbereiteten stage2 die Installation beginnen
				und sicher sein wollen, dass der Compiler auf dem aktuellen Stand ist,
				fügen Sie zu den unten genannten Kommandos <c>-u</c> hinzu.  Wenn Sie
				nicht wissen, was diese Anmerkung bedeutet, können Sie sie
				ignorieren.</note>

<pre caption="Den Rest des Systems installieren">
# <i>emerge -p system</i>
<comment>(listet die zu installierenden Pakete auf)</comment>
# <i>emerge system</i>
</pre>

				<p>Es braucht einige Zeit, um das Kompilieren des Basissystems zu
				beenden.  Der Erfolg ist ein an Ihre Anpassungen optimiertes System.
				Der Nachteil ist, dass Sie sich während dieser Zeit irgendwie
				beschäftigen müssen.  Der Autor empfiehlt "Star Wars - Super Bombad
				Racing" für die Playstation 2...</p>

				<p>Das Basissystem ist nun kompiliert.  Fahren Sie mit dem Abschnitt
				"Einstellen der Zeitzone" fort.</p>

			</body>
		</section>
	</chapter>


	<chapter>
		<title>Beginnen von stage3</title>

		<section>
			<body>

				<note>Dieser Abschnitt ist für jene, die mit stage3 <e>beginnen</e>.
				Wenn Sie mit stage1 oder stage2 begonnen haben, können Sie ihn
				überspringen.  Auch Benutzer von GRP fahren erst im nächsten Abschnitt
				fort.</note>

				<warn>Wenn Sie mit stage3 beginnen ändern Sie nicht die CHOST-Variable
				in <path>/etc/make.conf</path>.  Andernfalls gibt es komische und
				umfangreiche Fehler beim Kompilieren.</warn>

				<p>Das stage3 tar-Archiv bietet ein voll funktionsfähiges Gentoo System,
				sodass keine Kompilierung benötigt wird.</p>

				<note><b>Erfahrene Benutzer:</b> Wie auch immer, das stage3 tar-Archiv
				ist vorkompiliert und kann ein wenig veraltet sein.  Falls Sie das
				beunruhigen sollte, können Sie automatisch Ihr existierendes stage3
				updaten, um die aktuellsten Versionen des Basissystems zu erhalten,
				indem Sie die <c>CONFIG_PROTECT="-* /etc/make.conf" emerge -u system</c>
				ausführen.  Bedenken Sie, dass dieses eine lange Zeit benötigen kann,
				wenn Ihr stage3 sehr alt ist; ansonsten wird dieser Prozess recht
				schnell mit dem Vorteil, die aktuellsten Gentoo Updates und
				Fehlerbehebungen zu beinhalten, beendet sein.  Wie dem auch sei, wenn
				Sie möchten, können Sie diesen Befehl überspringen und mit dem nächsten
				Abschnitt fortfahren.</note>

			</body>
		</section>
	</chapter>


	<chapter>
		<title>Einstellen der Zeitzone</title>

		<section>
			<body>

				<p>Nun ist es erforderlich, dass Sie Ihre Zeitzone einstellen.</p>

				<p>Suchen Sie Ihre Zeitzone (oder GMT falls Sie die Greenwich Mean Time
				verwendenden) in <path>/usr/share/zoneinfo</path>.  Dann erzeugen Sie
				eine symbolische Verknüpfung zu <path>/etc/localtime</path> durch:</p>

<pre caption="Erzeugen einer symbolischen Verknüpfung zu Ihrer Zeitzone">
# <c>ln -sf /usr/share/zoneinfo/Pfad/zur/Zeitzonendatei /etc/localtime</c>
</pre>

			</body>
		</section>
	</chapter>


	<chapter>
		<title>Ändern der /etc/fstab für Ihre Maschine</title>
		<section>
			<body>

				<p>Ihr Gentoo Linux System ist beinahe zur Benutzung fertig.  Es müssen
				lediglich noch ein paar wichtige Systemdateien konfiguriert und der
				Bootloader installiert werden.  Zuerst wird die Datei
				<path>/etc/fstab</path> editiert.  Beachten Sie, dass die <c>notail</c>
				Option zu Ihrer boot Partition hinzugefügt werden muss, wenn Sie diese
				als ReiserFS angelegt haben.  Weiterhin geben Sie die Dateisystemtypen
				<c>ext2</c>, <c>ext3</c> oder <c>reiserfs</c> entsprechend Ihrer
				Konfiguration an.</p>

				<p>Verwenden Sie eine <path>/etc/fstab</path> ähnlich zu der unten
				genannten, jedoch tauschen Sie "BOOT", "ROOT" und "SWAP" mit den
				tatsächlichen Partitionsnummern aus (wie z.B. <c>hda1</c> usw.).</p>

<pre caption="Editieren der fstab">
<comment># /etc/fstab: static file system information.
#
# noatime turns off atimes for increased performance (atimes normally aren't
# needed; notail increases performance of ReiserFS (at the expense of storage
# efficiency).  It is safe to drop the noatime options if you want and to 
# switch between notail and tail freely.

# &lt;fs&gt;           &lt;mount point&gt;   &lt;type&gt;   &lt;opts&gt;          &lt;dump/pass&gt;

# NOTE: If your BOOT partition is ReiserFS, add the notail option to opts.
</comment>
/dev/BOOT           /boot       ext2     noauto,noatime  1 2
/dev/ROOT           /           ext3     noatime         0 1
/dev/SWAP           none        swap     sw              0 0
/dev/cdroms/cdrom0  /mnt/cdrom  iso9660  noauto,ro       0 0
proc                /proc       proc     defaults        0 0
</pre>

				<warn>Bitte beachten Sie, dass <path>/boot</path> <e>nicht</e> während
				des Bootens gemountet wird.  Das geschieht aus dem Grunde, um die Daten
				in <path>/boot</path> zu schützen.  Wenn Sie Zugriff auf
				<path>/boot</path> benötigen, müssen Sie es mounten.</warn>

			</body>
		</section>
	</chapter>


	<chapter>
		<title>Installieren des Kernels und des System Logger</title>
		<section>
			<title>Auswahl des Kernels</title>
			<body>

				<p>Es gibt zwei Möglichkeiten zur Installation des Kernels.  Entweder
				konfigurieren Sie ihn von Hand oder benutzen das <c>genkernel</c>
				Hilfsprogramm, das automatisch Ihren Kernel konfiguriert und kompiliert.</p>

				<p>Ob Sie nun Ihren Kernel von Hand konfigurieren oder <c>genkernel</c>
				verwenden, müssen Sie vorab die Linux Kernel Sourcen, die Sie verwenden
				möchten, mergen.  Gentoo bietet Ihnen einige Kernel ebuilds; eine Liste
				mit Beschreibung gibt es im <uri
				link="http://www.gentoo.org/doc/de/gentoo-kernel.xml">Gentoo Kernel
				Handbuch</uri>.  Wenn Sie sich in der Wahl nicht schlüssig sind,
				empfehlen wir die <c>gentoo-sources</c>.  Benötigen Sie die
				Unterstützung für XFS, installieren Sie <c>xfs-sources</c> oder
				<c>gs-sources</c>.  Die Gentoo Live CD verwendet <c>gs-sources</c> und
				<c>xfs-sources</c>.  Es gibt außerdem die <c>gaming-sources</c>; ein
				Kernel optimiert für Spiele, der wunderbar mit der aktivierten
				"Preemtive Kernel" Option zusammenarbeitet.</p>

				<p>Wählen Sie einen Kernel und mergen Sie ihn wie folgt:</p>

<pre caption="Emergen der Kernel Quellen">
# <i>emerge sys-kernel/gentoo-sources</i>
</pre>

				<p>Die symbolische Verknüpfung <path>/usr/src/linux</path> zeigt auf den
				neu installierten Kernel Quellpfad.  Portage verwendet die symbolische
				Verknüpfung <path>/usr/src/linux</path> für spezielle Zwecke.  Jedes
				ebuild mit Kernel Modulen ist so konfiguriert, dass es den Kernel
				Quellcode in <path>/usr/src/linux</path> verwendet.
				<path>/usr/src/linux</path> wird erstellt, wenn Sie Ihr erstes Kernel
				Quellpaket installieren, jedoch modifiziert Portage diese symbolische
				Verknüpfung später nicht mehr.</p>

			</body>
		</section>

		<section>
			<title>Verwenden von genkernel zur Kernelkompilierung</title>
			<body>

				<p>Da Sie nun den Kernelquellcode installiert haben, müssen Sie Ihren
				Kernel kompilieren.  Dazu gibt es zwei Möglichkeiten.  Die erste
				verwendet unser neues <c>genkernel</c> Skript, das automatisch einen
				Kernel für Sie erzeugt.  <c>genkernel</c> funktioniert dabei in
				ähnlicher Weise wie unsere Live CD.  Das heißt, wenn Sie
				<c>genkernel</c> zur Erzeugung des Kernels verwenden, wird die Hardware
				Ihres System während des Bootens erkannt, wie es auch unsere Live CD
				macht.  Da <c>genkernel</c> keine manuelle Konfiguration benötigt, ist
				dieses eine ideale Lösung für Benutzer, die sich nicht mit dem
				Kompilieren eines eigenen Kernels auskennen.</p>

				<p>Schauen wir uns nun <c>genkernel</c> an.  Zuerst müssen Sie
				<c>genkernel</c> emergen:</p>

<pre caption="genkernel emergen">
# <i>emerge -k genkernel</i>
</pre>

				<p>Kompilieren Sie nun Ihren Kernel mittels <c>genkernel:</c></p>

				<note><b>Erfahrene Benutzer:</b> Sie können auch <c>genkernel
				--config</c> eingeben, damit Sie <c>genkernel</c> anpassen können, bevor
				die Kompilierung beginnt.</note>

<pre caption="genkernel ausführen">
<comment>Wenn Sie genkernel 1.2 (auf dem 1.4-20030803 x86/i686 GRP Set
enthalten) verwenden, geben Sie folgendes ein:</comment>
# <i>genkernel gentoo-sources</i>
<comment>Verwenden Sie gentkernel 1.4 oder neuer, brauchen Sie keinen speziellen
Kernel angeben:</comment>
# <i>genkernel</i>
Gentoo Linux genkernel, version 1.4
Copyright 2003 Gentoo Technologies, Inc., Bob Johnson, Daniel Robbins
Distributed under the GNU General Public License version 2

Settings:
compile optimization: 1 processor(s)
source tree: /usr/src/linux-2.4.20-gaming-r3
config: gentoo (customized)
config loc: /etc/kernels/config-2.4.20-gaming-r3
initrd config: (default) /etc/kernels/settings

* Running "make oldconfig"...                                                                     [ ok ]
* Logging to /var/log/genkernel.log...                                                            [ ok ]
* Starting 2.4.20-gaming-r3 build...                                                              [ ok ]
* Running "make dep"...                                                                           [ ok ]
* Running "make bzImage"...                                                                       [ ok ]
* Running "make modules"...                                                                       [ ok ]
* Running "make modules_install"...                                                               [ ok ]
* Moving bzImage to /boot/kernel-2.4.20-gaming-r3...                                              [ ok ]
* Building busybox...                                                                             [ ok ]
* Creating initrd...                                                                              [ ok ]

* Build completed successfully!

* Please specify /boot/kernel-2.4.20-gaming-r3 and /boot/initrd-2.4.20-gaming-r3
* when customizing your boot loader configuration files.
</pre>

				<p>Sobald <c>genkernel</c> beendet ist, wird ein neuer Kernel, ein
				kompletter Satz an Modulen und die <e>initial root disk</e> (initrd)
				erzeugt.  Wir werden den Kernel und die initrd später in diesem Dokument
				zum Konfigurieren des Bootloaders verwenden.  Die initrd wird direkt
				nach dem Booten gestartet, um eine Hardware Autoerkennung durchzuführen
				(wie auf unserer Live CD) bevor Ihr "wirkliches" System startet.</p>

				<p>Lassen Sie uns nun einen weiteren Schritt durchführen, damit Ihr
				System wie die Live CD startet -- emergen Sie <c>hotplug</c>.  Während
				die initrd die Hardware automatisch erkennt, wird <c>hotplug</c> alles
				andere erkennen.  Um <c>hotplug</c> zu emergen und aktivieren, geben Sie
				folgendes ein:</p>

<pre caption="emergen und aktivieren von hotplug">
# <i>emerge -k hotplug</i>
# <i>rc-update add hotplug default</i>
</pre>

				<p>Da Sie nun Ihr System mit <c>genkernel</c> konfiguriert haben, können
				Sie den folgenden Abschnitt "Manuelle Kernelkonfiguration"
				überspringen.</p>

			</body>
		</section>

		<section>
			<title>Manuelle Kernelkonfiguration</title>
			<body>

				<p>Haben Sie sich dazu entschieden, nicht <c>genkernel</c> zur
				Erstellung Ihres Kernels einzusetzen, wird Sie dieser Abschnitt durch
				die Konfiguration und Installation eines Kernels von Hand begleiten.
				Achten Sie darauf, dass <path>/usr/src/linux</path> eine symbolische
				Verknüpfung zu Ihren installierten Kernelquellen ist und automatisch von
				Portage während des emergen gesetzt wurde.  Haben Sie mehrere
				Kernelquellen installiert, müssen Sie <path>/usr/src/linux</path> auf
				die richtigen Kernelquellen ändern.</p>

				<warn>Konfigurieren Sie Ihren eigenen Kernel, seien Sie mit der
				<i>grsecurity</i> Option vorsichtig.  Wenn Sie zu scharfe
				Sicherheitseinstellungen vornehmen, kann dies dazu führen, dass
				bestimmte Programme (wie z.B. X) nicht einwandfrei laufen.  Zweifeln
				Sie, so lassen Sie es aus.</warn>

				<note>Wenn Sie dieselbe Konfiguration wie auf der Live CD wünschen oder
				Ihre Konfiguration ausgehend davon anpassen möchten, sollten Sie <c>cd
				/usr/src/linux &amp;&amp; cat /proc/config &gt; .config &amp;&amp; make
				oldconfig</c> ausführen.  Wenn Sie nicht die <c>xfs-sources</c>
				verwenden, werden Sie zu ein paar Unterschieden zwischen Ihrem gewählten
				Kernel und den <c>xfs-sources</c> gefragt.</note>

<pre caption="Konfigurieren des Linux Kernel">
# <i>cd /usr/src/linux</i>
# <i>make menuconfig</i>
</pre>

				<warn>Damit Ihr Kernel später ordentlich läuft, gibt es einige Optionen,
				die Sie überprüfen sollten -- das heisst, sie sollten <e>eingeschaltet
				und nicht als Modul kompiliert</e> werden.  Seien Sie sicher, dass
				"ReiserFS" eingeschaltet ist, wenn Sie ReiserFS Partitionen verwenden;
				das selbe gilt für "Ext3".  Wenn Sie XFS verwenden, schalten Sie die
				"SGI XFS filesystem support" Option ein.  Außerdem ist es immer eine
				gute Idee "Ext2" eingeschaltet zu haben, gleichgültig ob Sie es
				verwenden oder nicht.</warn>

				<p>Hier sind einige allgemeine Optionen, die Sie benötigen:</p>

<pre caption="make menuconfig Optionen">
Code maturity level options ---&gt;
  [*] Prompt for development and/or incomplete code/drivers"
<comment>(Sie benötigen dieses, um einige der Optionen weiter unten zu aktivieren.)</comment>
     ...

File systems ---&gt;
  &lt;*&gt; Reiserfs support
<comment>(Nur benötigt, wenn Sie ReiserFS verwenden.)</comment>
       ...
  &lt;*&gt; Ext3 journalling file system support
<comment>(Nur benötigt, wenn Sie ext3 verwenden.)</comment>
       ...
  [*] Virtual memory file system support (former shm fs)
<comment>(Erforderlich für Gentoo Linux.)</comment>
       ...
  &lt;*&gt; JFS filesystem support
<comment>(Nur benötigt, wenn Sie JFS verwenden.)</comment>
       ...
  [*] /proc file system support
<comment>(Erforderlich für Gentoo Linux.)</comment>
  [*] /dev file system support (EXPERIMENTAL)
  [*]   Automatically mount at boot          
<comment>(Erforderlich für Gentoo Linux.)</comment>
  [ ] /dev/pts file system for Unix98 PTYs
<comment>(Schaltet Sie es aus, es wird NICHT gebraucht.)</comment>
       ...
  &lt;*&gt; Second extended fs support
<comment>(Nur benötigt, wenn Sie ext2 verwenden.)</comment>
       ...
  &lt;*&gt; XFS filesystem support
<comment>(Nur benötigt, wenn Sie XFS verwenden.)</comment>
</pre>

				<p>Verwenden Sie PPPoE um sich ins Internet einzuwählen (z.B. mit DSL),
				benötigen Sie die folgen Optionen (fest im Kernel kompiliert oder besser
				als Modul): "PPP (point-to-point protocol)", "PPP support for async
				serial ports", "PPP support for sync tty ports".  Die beiden
				Komprimierungs-Optionen stören nicht, aber werden auch nicht unbedingt
				benötigt; gleiches gilt für die "PPP over Ethernet" Option, die nur von
				<e>rp-pppoe</e> verwendet wird, wenn es das PPPoE-Modul des Kernels
				verwenden soll.</p>

				<p>Besitzen Sie einen IDE CD-Brenner, dann sollten Sie die SCSI
				Emulation aktivieren.  Schalten Sie "ATA/IDE/MFM/RLL support" ---&gt;
				"IDE, ATA, and ATAPI Block devices" ---&gt; "SCSI emulation support"
				(üblicherweise als Modul) ein, dann unter "SCSI support" den "SCSI
				support", "SCSI CD-ROM support" und "SCSI generic support" (wieder als
				Modul) einschalten.  Um diese Module automatisch beim Booten zu laden,
				führen Sie das Kommando <c>echo -e "ide-scsi\nsg\nsr_mod" &gt;&gt;
				/etc/modules.autoload.d/kernel-2.4</c> aus.</p>

				<p>Vergessen Sie nicht die Unterstützung für Ihre Netzwerkkarte in den
				Kernel hinzuzufügen.</p>

				<note>Wenn Sie es vorziehen, können Sie Gentoo Linux mit einem 2.2
				Kernel installieren.  Jedoch zahlen Sie dafür einen hohen Preis: Sie
				werden alle raffinierten Erweiterungen der 2.4er Kernel Serie verlieren
				(wie z.B. XFS und tmpfs Dateisysteme, iptables und mehr), obwohl der 2.2
				Kernel mit ReiserFS und devfs Unterstützung erweitert werden kann.  Die
				Gentoo Linux Boot Skripte benötigen entweder Unterstützung für tmpfs
				oder ramdisk im Kernel, sodass Benutzer von 2.2 Kernel die ramdisk
				Unterstützung im Kernel einkompiliert haben müssen (nicht als Modul).
				Es ist <e>wichtig</e>, dass die <c>gentoo=notmpfs</c> Option zur Kernel
				Startzeile in der <path>/boot/grub/grub.conf</path> oder der append
				Zeile in der <path>/etc/lilo.conf</path> hinzugefügt wird, sodass der
				2.2 Kernel eine ramdisk für die Boot Skripte mounted anstelle von tmpfs.
				Wenn Sie sich dafür entscheiden, nicht devfs zu verwenden, dann sollte
				stattdessen die Option <c>gentoo=notmpfs,nodevfs</c> eingesetzt
				werden.</note>

<pre caption="Kompilieren und Installieren des Kernels">
# <i>make dep &amp;&amp; make clean bzImage modules modules_install</i>
# <i>cp /usr/src/linux/arch/i386/boot/bzImage /boot</i>
</pre>

			</body>
		</section>

		<section>
			<title>Installation von zusätzlichen Hardware-spezifischen ebuilds</title>
			<body>

				<p>Abschließend installieren Sie noch kernelabhängig ebuilds für
				zusätzliche Hardware Ihres Systems.  Hier ist eine Liste von ebuilds,
				die Sie emergen können:</p>

				<table>
					<tr>
						<th>Ebuild</th>
						<th>Zweck</th>
						<th>Kommando</th>
					</tr>
					<tr>
						<ti>nvidia-kernel</ti>
						<ti>Beschleunigte NVIDIA Grafik für XFree86</ti>
						<ti><c>emerge -k nvidia-kernel</c></ti>
					</tr>
					<tr>
						<ti>nforce-net</ti>
						<ti>On-board Audio auf NVIDIA NForce(2) Mainboards</ti>
						<ti><c>emerge nforce-audio</c></ti>
					</tr>
					<tr>
						<ti>e100</ti>
						<ti>Intel e100 Fast Ethernet Adapters</ti>
						<ti><c>emerge e100</c></ti>
					</tr>
					<tr>
						<ti>e1000</ti>
						<ti>Intel e1000 Gigabit Ethernet Adapters</ti>
						<ti><c>emerge e1000</c></ti>
					</tr>
					<tr>
						<ti>emu10k1</ti>
						<ti>Creative Sound Blaster Live!/Audigy Unterstützung</ti>
						<ti><c>emerge emu10k1</c></ti>
					</tr>
					<tr>
						<ti>ati-drivers</ti>
						<ti>Beschleunigte ATI Radeon 8500+/FireGL Grafik für XFree86</ti>
						<ti><c>emerge ati-drivers</c></ti>
					</tr>
					<tr>
						<ti>xfree-drm</ti>
						<ti>Beschleunigte Grafik für ATI Redeon bis 9200, Rage128, Matrox,
						Voodoo und andere Karten für XFree86</ti>
						<ti><c>VIDEO_CARDS="ihrekarte" emerge xfree-drm</c></ti>
					</tr>
				</table>

				<p>Die Pakete <c>nvidia-kernel</c>, <c>ati-drivers</c> und
				<c>xfree-drm</c> bedürfen einer weiteren Konfiguration zum Aktivieren.
				Alle anderen gelisteten ebuilds sollten während des Bootens durch das
				<c>hotplug</c> Paket automatisch erkannt werden.  Wenn Sie
				<c>hotplug</c> nicht verwenden, fügen Sie die entsprechenden Module zu
				<path>/etc/modules.autoload.d/kernel-2.4</path> hinzu.</p>

				<p>Mehr Informationen zu <c>xfree-drm</c> finden Sie in unserem <uri
				link="http://www.gentoo.org/doc/en/dri-howto.xml">Direct Rendering
				Guide</uri> (derzeit nur in Englisch).</p>

			</body>
		</section>

		<section>
			<title>Installation eines System Loggers</title>
			<body>

				<p>Ihr neuer angepasster Kernel (und seine Module) sind nun installiert.
				Wählen Sie jetzt einen System Logger, den Sie installieren möchten.  Es
				gibt <c>sysklogd</c>, eine traditionelle Reihe an System Logger
				Diensten, als auch <c>msyslog</c>, <c>syslog-ng</c> und <c>metalog</c>.
				Power User scheinen sich inzwischen von sysklogd abzuwenden (keine gute
				Performance) und wählen eine der neueren Alternativen.  Im Zweifelsfalle
				versuchen Sie metalog, da es recht populär ist.  Um den System Logger
				Ihrer Wahl zu emergen, geben Sie <e>eine</e> der folgenden vier
				Möglichkeiten ein:</p>

<pre caption="System Logger emergen">
# <i>emerge -k app-admin/sysklogd</i>
# <i>rc-update add sysklogd default</i>
<comment>oder</comment>
# <i>emerge -k app-admin/syslog-ng</i>
# <i>rc-update add syslog-ng default</i>
<comment>oder</comment>
# <i>emerge -k app-admin/metalog</i>
# <i>rc-update add metalog default</i>
<comment>oder</comment>
# <i>emerge -k app-admin/msyslog</i>
# <i>rc-update add msyslog default</i>
</pre>

				<impo>Metalog schreibt die Ausgaben in Blöcken auf die Festplatte,
				sodass Nachrichten nicht direkt in den System-Logs erscheinen.  Wenn Sie
				versuchen ein Programm von Fehlern zu beseitigen, ist dieses
				Performanz-steigernde Verhalten nicht sehr hilfreich.  In Ihrem
				laufenden Gentoo Linux System können Sie an metalog ein USR1 Signal
				senden, um vorübergehend das Zwischenspeichern abzuschalten (d.h.
				<c>tail -f /var/log/everything/current</c> arbeitet nun wie erwartet in
				Echtzeit); und ein USR2 Signal, um die Zwischenspeicherung wieder
				einzuschalten.  Wenn Sie die Zwischenspeicherung generell abschalten
				wollen, können Sie <c>METALOG_OPTS="-B"</c> in
				<c>METALOG_OPTS="-B -s"</c> in der
				<path>/etc/conf.d/metalog</path> ändern.

<pre caption="metalog Zwischenspeicherung ein-/ausschalten">
<comment>Zwischenspeicherung ausschalten:</comment>
# <i>killall -USR1 metalog</i>
<comment>Zwischenspeicherung wieder einschalten:</comment>
# <i>killall -USR2 metalog</i>
</pre>

				</impo>

				<p>Sie können nun wahlweise zusätzlich einen Cron Dienst wählen, um
				regelmäßig auftretende Vorgänge automatisiert ausführen zu lassen.  Zur
				Zeit gibt es <c>dcron</c>, <c>fcron</c> und <c>vcron</c>.  Wenn Sie sich
				nicht entscheiden können, wählen Sie vcron.</p>

<pre caption="Cron Dienst installieren">
# <i>emerge -k sys-apps/dcron</i>
# <i>rc-update add dcron default</i>
# <i>crontab /etc/crontab</i>
<comment>oder</comment>
# <i>emerge -k sys-apps/fcron</i>
# <i>rc-update add fcron default</i>
# <i>crontab /etc/crontab</i>
<comment>oder</comment>
# <i>emerge -k sys-apps/vcron</i>
# <i>rc-update add vcron default</i>
<comment>Sie brauchen nicht <c>crontab /etc/crontab</c> bei Verwendung von vcron ausführen.</comment> 
</pre>

				<p>Weitere Informationen, um Programme und Dienste während des
				Systemsstarts auszuführen, finden Sie im <uri
				link="http://www.gentoo.org/doc/de/rc-scripts.xml">rc-script
				Leitfaden</uri>.</p>

			</body>
		</section>
	</chapter>


	<chapter>
		<title>Installation von diversen benötigten Paketen</title>

		<section>
			<body>

				<p>Falls Sie <c>rp-pppoe</c> benötigen, um sich ins Internet
				einzuwählen, so sollten Sie dieses nun installieren.</p>

<pre caption="Installation von rp-pppoe">
# <i>USE="-X" emerge rp-pppoe</i>
<comment>GRP Benutzer geben folgendes ein:</comment>
# <i>USE="-X bindist" emerge -K rp-pppoe</i>
</pre>

				<note>Das <c>USE="-X"</c> verhindert die Installation des optionalen X
				Interface für pppoe, was an dieser Stelle angegeben werden sollte, da
				sonst X und alle seine abhängigen Pakete installiert werden.  Sie können
				<c>rp-pppoe</c> mit X Unterstützung später noch einmal neu bauen.</note>

				<note>Bedenken Sie, dass <c>rp-pppoe</c> nur installiert, aber nicht
				konfiguriert wird.  Dazu rufen Sie nochmals <c>adsl-setup</c> auf,
				nachdem Sie das erste Mal von Ihrem Gentoo System gebootet haben.</note>

				<p>Sie benötigen weitere Pakete aus dem Portage Tree, wenn Sie optionale
				Erweiterungen wie XFS, ReiserFS oder LVM verwenden.</p>

<pre caption="Hilfsprogramme für Dateisysteme emergen">
<comment>Wenn Sie XFS verwenden, sollten Sie die XFS Tools installieren:</comment>
# <i>emerge -k sys-apps/xfsprogs</i>
<comment>Wenn Sie ReiserFS verwenden, sollten Sie die ReiserFS Tools installieren:</comment>
# <i>emerge -k sys-apps/reiserfsprogs</i>
<comment>Wenn Sie JFS verwenden, sollten Sie die JFS Tools installieren:</comment>
# <i>emerge -k sys-apps/jfsutils</i>
<comment>Wenn Sie LVM verwenden, sollten Sie das <c>lvm-user</c> Paket installieren:</comment>
# <i>emerge -k sys-apps/lvm-user</i>
</pre>

				<p>Sind Sie ein Laptop-Benutzer und möchten auf die PCMCIA Slots während
				Ihres ersten richtigen Reboots zugreifen, installieren Sie das
				<c>pcmcia-cs</c> Paket.</p>

<pre caption="PCMCIA-cs emergen">
# <i>emerge -k sys-apps/pcmcia-cs</i>
</pre>

<!-- fix the bug or fix the docs, don't send the user in circles (drobbins)
				<warn>Sie müssen <c>pcmcia-cs</c> nach der Installation nochmals
				kompilieren, um PCMCIA benutzen zu können.</warn>
-->

			</body>
		</section>
	</chapter>


	<chapter>
		<title>Benutzer-Management</title>

		<section>
			<title>Das Passwort für root vergeben</title>
			<body>

				<p>Bevor Sie es vergessen, sollten Sie root ein Passwort vergeben:</p>

<pre caption="root Passwort vergeben">
# <i>passwd</i>
</pre>

			</body>
		</section>

		<section>
			<title>Einen Benutzer anlegen</title>
			<body>

				<p>Als root auf einem Unix/Linux System zu arbeiten ist
				<e>gefährlich</e> und sollte so oft wie möglich vermieden werden.  Von
				daher ist es <e>stark</e> empfehlenswert einen Benutzer für die
				allgemeinen Aufgaben anzulegen.</p>

				<pre caption="Benutzer hinzufügen">
# <i>useradd Neuer_Benutzer -m -G users,wheel,audio -s /bin/bash</i>
# <i>passwd Neuer_Benutzer</i>
				</pre>

				<p>Ersetzen Sie natürlich <c>Neuer_Benutzer</c> durch Ihren
				Benutzernamen.</p>

				<p>Wann immer Sie eine Aufgabe erledigen wollen, die nur root ausführen
				darf, benutzen Sie <c>su -</c> um die root-Privilegien zu erhalten oder
				werfen Sie einen Blick in das <c>sudo</c>-Paket.</p>

			</body>
		</section>
	</chapter>


	<chapter>
		<title>Festlegen des Hostname</title>

		<section>
			<body>

				<p>Ändern Sie die Datei <path>/etc/hostname</path> so, dass sie den
				Namen Ihres Rechners in einer einzelnen Zeile enthält, z.B.
				<c>meinrechner</c>.</p>

<pre caption="Hostname festlegen">
# <i>echo meinrechner &gt; /etc/hostname</i>
</pre>

				<p>Editieren Sie daraufhin die Datei <path>/etc/dnsdomainname</path>,
				sodass Sie Ihren DNS Domainnamen enthält, z.B.
				<c>meinedomain.lan</c>.</p>

<pre caption="DNS Domainnamen konfigurieren">
# <i>echo meinedomain.lan &gt; /etc/dnsdomainname</i>
</pre>

				<p>Befinden Sie sich in einer NIS Domain, schreiben Sie den Domainnamen
				in <path>/etc/nisdomainname</path>.</p>

<pre caption="NIS Domainnamen konfigurieren">
# <i>echo nis.meinedomain.lan &gt; /etc/nisdomainname</i>
</pre>

				<note>Eventuell erhalten Sie die Daten von Ihrem Netzwerkadminstrator.
				Benutzen Sie den Rechner ausschließlich in einem lokalen Netzwerk,
				verwenden Sie keine echten Domains, mit denen Sie in Kollision geraten
				können.  Aus diesem Grund gibt es die Top-Level-Domain <c>.lan</c>, um
				zu kennzeichnen, dass es sich ausschließlich um eine lokale Domain
				handelt.</note>

			</body>
		</section>
	</chapter>


	<chapter>
		<title>Die /etc/hosts anpassen</title>

		<section>
			<body>

				<p>Die Datei <path>/etc/hosts</path> enthält eine Liste von IP-Adressen
				und ihren zugehörigen Hostnamen.  Sie wird vom System verwendet, um
				IP-Adressen von Hostnamen aufzulösen, die nicht in Ihrem Nameserver
				verzeichnet sind.  Hier ist ein Beispiel für diese Datei:</p>

<pre caption="Beispiel für /etc/hosts">
127.0.0.1      localhost
<comment># Die nächste Zeile enthält die IP-Adresse Ihres Rechners in Verbindung mit dem Hostnamen</comment>
192.168.1.1    meinrechner.meinedomain.lan
</pre>

				<note>Wenn Sie Ihre IP-Adresse von einem DHCP-Server beziehen, ist es
				hilfreich, ihren Hostnamen hinter <i>localhost</i> einzutragen.  Das
				hilft GNOME als auch anderen Programmen in der Namensauflösung.</note>

			</body>
		</section>
	</chapter>


	<chapter>
		<title>Abschließende Netzwerkkonfiguration</title>

		<section>
			<body>

				<p>Fügen Sie die Namen von allen Modulen, die für ein ordentliches
				Funktionieren Ihres Systems erforderlich sind, in die Datei
				<path>/etc/modules.autoload.d/kernel-2.4</path> hinzu (Sie können
				zusätzliche Optionen für ein Modul auf derselben Zeile hinzufügen).
				Wenn Gentoo Linux bootet lädt es diese Module automatisch.  Besonders
				wichtig sind Ihre Netzwerkkarten Module, wenn Sie diese als Modul
				kompiliert haben:</p>

<pre caption="/etc/modules.autoload.d/kernel-2.4">
<comment>Angenommen Sie verwenden eine 3COM Karte, schauen Sie im Verzeichnis
<path>/lib/modules/`uname -r`/kernel/drivers/net</path> nach dem Namen des
Moduls.  In diesem Fall wäre es:</comment>
3c59x
</pre>

				<p>Passen Sie die Datei <path>/etc/conf.d/net</path> für das erste
				Booten an:</p>

<pre caption="Netzwerk Konfiguration zum Booten">
# <i>nano -w /etc/conf.d/net</i>
<comment>Nur für nicht-PCMCIA Netzwerkkarten:</comment>
# <i>rc-update add net.eth0 default</i>
</pre>

				<p>Haben Sie mehr als eine Netzwerkkarte oder Tokenring-Karten, müssen
				Sie zusätzliche <path>net.eth<comment>x</comment></path> oder
				<path>net.tr<comment>x</comment></path> Skripte erstellen, eins für jede
				Karte (<comment>x</comment> = 1, 2, ...):</p>

<pre caption="Mehrere Netzwerkkarten">
# <i>cd /etc/init.d</i>
# <i>cp net.eht0 net.eth<comment>x</comment></i>
<comment>Nur für nicht-PCMCIA Netzwerkkarten:</comment>
# <i>rc-update add net.eth<comment>x</comment> default</i>
</pre>

				<p>Verwenden Sie eine PCMCIA Karte, schauen Sie in die Datei
				<path>/etc/init.d/pcmcia</path>, um festzustellen, dass alle
				Einstellungen korrekt sind und führen Sie das folgende Kommando aus:</p>

<pre caption="PCMCIA Dienst automatisch starten">
# <i>rc-update add pcmcia boot</i>
</pre>

				<p>Dadurch werden die PCMCIA Treiber automatisch beim Ausführen des
				Netzwerkkarten Skripts mitgeladen.  Die entsprechende Datei
				<path>/etc/init.d/net.eth*</path> wird durch den PCMCIA Dienst
				automatisch gestartet.</p>

			</body>
		</section>
	</chapter>


	<chapter>
		<title>Abschließende Konfiguration: Basiseinstellungen vornehmen (inklusive
		Tastaturbelegung einstellen)</title>

		<section>
			<body>

<pre caption="Basiseingellungen">
# <c>nano -w /etc/rc.conf</c>
</pre>

				<p>Folgen Sie den Anleitungen in dieser Datei, um die Basiseinstellungen
				vorzunehmen.  Die <c>CLOCK</c> Einstellung sollte Ihrem System
				entsprechend eingestellt werden.  Für eine deutsche Tastaturbelegung
				ändern Sie <c>KEYMAP</c> in <c>KEYMAP="de-latin1"</c> (für weitere
				Tastaturbelegungen schauen Sie in das Verzeichnis
				<path>/usr/share/keymaps</path>).</p>

			</body>
		</section>
	</chapter>


	<chapter>
		<title>Konfigurieren des Bootloaders</title>

		<section>
			<title>Anmerkungen</title>
			<body>

				<p>Ganz im Sinne von Gentoo haben Benutzer mehr als einen Bootloader zur
				Wahl.  Grundsätzlich können Sie derzeit zwischen den beiden Bootloadern
				<c>GRUB</c> und <c>LILO</c> wählen.</p>

				<p>Es ist nicht nötig, beide Bootloader zu installieren, es wird sogar
				ein Nachteil sein!  Von daher wählen Sie bitte nur einen.</p>

				<p>Außerdem werden Sie Ihren Bootloader anders anpassen müssen, wenn Sie
				<c>genkernel</c> (mit dem Kernel und der initrd) verwenden oder einen
				Kernel von Hand kompiliert haben.  Achten Sie auf die wichtigen
				Unterschiede.</p>

				<impo>Wenn Sie Gentoo Linux auf einem System mit einem NVIDIA nForce
				oder nForce2 Chipsatz mit einer integrierten GeForce Grafikkarte
				installieren, sollten Sie LILO wählen und GRUB vermeiden.  Mit
				eingeschalteter on-board Grafikkarte, wird der untere Bereich des RAMs
				als Grafikspeicher verwendet.  Da GRUB ebenso den unteren
				Speicherbereich beim Booten verwendet, kann es zu "out of memory"
				Fehlern kommen.  Von daher: haben Sie einen nForce Chipsatz oder ein
				anderes Mainboard mit einer on-board Grafikkarte, verwenden Sie LILO.
				Selbst, wenn Sie eine weitere Grafikkarte eingebaut haben, ist es
				praktisch, dass Sie die Möglichkeit haben, diese entfernen und weiterhin
				Gentoo Linux booten zu können.</impo>

			</body>
		</section>

		<section>
			<title>GRUB konfigurieren</title>
			<body>

				<p>Der kritische Teil bei GRUB ist das Verständnis, wie GRUB Festplatten
				und Partitionen anspricht.  Ihre Linux Partition <path>/dev/hda1</path>
				ist unter GRUB <path>(hd0,0)</path>.  Beachten Sie die Klammern um hd0,0
				-- Sie sind von Nöten.  Festplatten werden von Null an gezählt anstatt
				mit "a" beginnend.  Partitionen starten mit Null anstatt mit Eins.
				Außerdem werden mit hd nur Festplatten angesprochen und keine ATAPI-IDE
				Geräte, wie CDROMs oder Brenner, jedoch kann es auch für
				SCSI-Festplatten verwendet werden (normalerweise erhalten sie höhere
				Nummern als IDE Geräte, außer das BIOS wurde so konfiguriert, dass es
				von SCSI booten soll).  Angenommen, Sie haben eine Festplatte an
				/dev/hda, ein CDROM an /dev/hdb und einen Brenner an /dev/hdc, eine
				weitere Festplatte an /dev/hdd und keine SCSI Festplatten, dann ist
				<path>/dev/hdd7</path> unter GRUB <path>(hd1,6)</path>.  Es mag ein
				wenig trickreich klingen - und es ist wirklich trickreich - aber Sie
				werden sehen, dass GRUB eine Tab-Completion anbietet, die sehr hilfreich
				ist, wenn Sie mehrere Festplatten und Partitionen besitzen und wenn Sie
				sich ein wenig in der GRUB-Nummerierung verlieren.  Wenn Sie ein wenig
				Gefühl dafür haben, dann sollten Sie GRUB installieren.</p>

				<p>Der einfachste Weg GRUB zu installieren, ist <c>grub</c> am Prompt
				einzugeben:</p>

<pre caption="GRUB installieren">
# <i>emerge -k grub</i>
# <i>grub</i>
</pre>

				<p>Sie sehen nun das GRUB Prompt <c>grub&gt;</c>.  Geben Sie hier die
				richtigen Kommandos ein, um den GRUB boot record auf Ihre Festplatte zu
				installieren.  In diesem Beispiel wird GRUB in den MBR (Master Boot
				Record) installiert, sodass Sie als erstes das GRUB Prompt sehen, wenn
				Sie Ihren Rechner einschalten.  In diesem Fall sind die Kommandos
				folgende:</p>

<pre caption="GRUB in den MBR installieren">
grub&gt; <i>root (hd0,0)</i> <comment>Ihre Boot Partition</comment>
grub&gt; <i>setup (hd0)</i> <comment>Wo der boot record installiert werden soll (hier ist es der MBR)</comment>
</pre>

<pre caption="GRUB nicht in den MBR installieren">
<comment>Alternativ können Sie GRUB auch woanders als den MBR installieren</comment>
grub&gt; <i>root (hd0,0)</i> <comment>Ihre Boot Partition</comment>
grub&gt; <i>setup (hd0,4)</i> <comment>Wo der boot record installiert werden soll (hier ist es /dev/hda5)</comment>
grub&gt; <i>quit</i>
</pre>

				<p>Die Kommandos bewirken folgendes: Zuerst teilen Sie mit <c>root ( )</c>
				GRUB Ihre Boot Partition mit (im obigen Beispiel <path>/dev/hda1</path>
				oder <path>(hd0,0)</path> in der GRUB Schreibweise).  Das zweite
				Kommando <c>setup ( )</c> berichtet GRUB wo es den boot record hin
				installieren soll, sodass es nach seinen speziellen Dateien an der mit
				<c>root ( )</c> angegebenen Stelle schauen soll.  In dem obigen Fall
				wird der boot record in den MBR der Festplatte geschrieben, sodass nur
				<path>/dev/hda</path> (oder auch als <path>(hd0)</path> bekannt)
				angegeben werden muss.  Wenn Sie einen anderen primären Bootloader und
				GRUB als den sekundären Bootloader verwenden wollen, können Sie den boot
				record in einer bestimmten Partition installieren.  In diesem Fall geben
				Sie die bestimmte Partition anstatt der ganzen Festplatte an.  Sobald
				der boot record erfolgreich installiert wurde, geben Sie <c>quit</c>
				ein, um GRUB zu beenden.</p>

				<note>Die Tab Completion von GRUB kann so benutzt werden, dass Sie
				beispielsweise <c> root (</c> eingeben und danach die TAB-Taste drücken,
				wodurch Sie eine Liste der verfügbaren Geräte (nicht nur Festplatten)
				erhalten.  Drücken Sie die TAB-Taste nachdem Sie <c> root (hd</c>
				eingegeben haben, wird GRUB Ihnen die verfügbaren Festplatten anzeigen.
				Und drücken Sie die TAB-Taste nachdem Sie <c> root (hd0,</c> eingegeben
				haben, zeigt GRUB Ihnen eine Liste der Partitionen auf dieser
				Festplatte.  Mittels der Tab Completion können Sie recht effektiv den
				Syntax überprüfen und die richtige Wahl treffen.</note>

				<p>Gentoo Linux ist nun installiert, aber Sie müssen noch die Datei
				<path>/boot/grub/grub.conf</path> erzeugen, um ein nettes GRUB Boot Menu
				zu erhalten.</p>

				<impo>Um eine rückwärtige Kompatibilität zu GRUB zu bewahren, erzeugen
				Sie eine Verknüpfung von <path>grub.conf</path> zu
				<path>menu.lst</path>.  Sie können dieses mit <c>ln -s
				/boot/grub/grub.conf /boot/grub/menu.lst</c> erledigen.</impo>

				<p>Erstellen Sie jetzt die Datei <path>grub.conf</path> (<c>nano -w
				/boot/grub/grub.conf</c>) mit dem folgendem Inhalt:</p>

<pre caption="grub.conf für GRUB">
default 0
timeout 30
splashimage=(hd0,0)/boot/grub/splash.xpm.gz

<comment># Wenn Sie Ihren eigenen Kernel erstellt haben, konfigurieren Sie ähnlich
zu dem folgenden:</comment>
title=Mein Gentoo Linux
root (hd0,0)
kernel (hd0,0)/boot/bzImage root=/dev/hda3

<comment># Haben Sie genkernel verwendet, benutzen Sie folgendes:</comment>
title=Mein Gentoo Linux (genkernel)
root (hd0,0)
kernel (hd0,0)/boot/kernel-KV root=/dev/hda3
initrd (hd0,0)/boot/initrd-KV

<comment># Beispiel für ein weiteres Betriebssytem</comment>
title=Windows XP
root (hd0,5)
chainloader (hd0,5)+1
</pre>

				<p>Ersetzen Sie <c>KV</c> durch den installierten Kernel.</p>

				<note>(hd0,0) muss ohne Leerzeichen in den Klammern geschrieben
				werden.</note>

				<impo>Wenn Sie vorab die SCSI Emulation für einen IDE CD Brenner
				eingerichtet haben, dann benötigen Sie den Zusatz <c>hdx=ide-scsi</c> in
				der <c>kernel</c> Zeile der <path>grub.conf</path> ("hdx" entspricht
				dem Gerät Ihres Brenners).</impo>

				<p>Nachdem Sie die Datei gespeichert haben, ist Ihre Gentoo Linux
				Installation beendet.  Wenn Sie den ersten Eintrag im Boot Menu wählen,
				wird GRUB ohne weiteres Gentoo Linux starten.  Der zweite Teil in der
				Datei ist optional und zeigt Ihnen, wie Sie GRUB nutzen können, um
				Windows zu booten.</p>

				<note><path>(hd0,0)</path> sollte zu Ihrer boot Partition
				(<path>/dev/hda1</path> in dem obigen Beispiel) und
				<path>/dev/hda3</path> zu Ihrer root Partition zeigen.
				<path>(hd0,5)</path> beinhaltet den Windows Bootloader.</note>

				<note>Der Pfad zum Kernel Image ist relativ zur Boot Partition.  Wenn
				Sie beispielsweise eine separate Boot Partition unter
				<path>(hd0,0)</path> und Root Partition in <path>(hd0,1)</path> haben,
				werden alle Pfade in der obigen <path>grub.conf</path> zu
				<path>/bzImage</path>.</note>

				<p>Wenn Sie weitere Optionen dem Kernel mitteilen müssen, fügen Sie
				diese einfach am Ende der <c>kernel</c> Zeile ein.  Im obigen Beispiel
				wurde bereits eine Option (<c>root=/dev/hda3</c>) übergeben, aber Sie
				können weitere hinzufügen.  Beispielsweise können Sie devfs ausschalten
				(nicht empfehlenswert, ausser Sie wissen, was Sie tun), indem Sie
				<c>gentoo=nodevfs</c> zum <c>kernel</c> Kommando hinzufügen.</p>

				<note>Anders als in früheren Versionen von Gentoo Linux brauchen Sie
				nicht mehr <c>devfs=mount</c> hinzufügen, um devfs einzuschalten, da es
				nun standardmäßig aktiviert ist.</note>

			</body>
		</section>

		<section>
			<title>LILO konfigurieren</title>
			<body>

				<p>Während GRUB eine neue Alternative für viel Benutzer ist, ist es
				nicht immer die beste Wahl.  LILO, der LInuxLOader ist der erprobtere
				Linux Bootloader.  Hier sehen Sie, wie Sie LILO installieren, wenn Sie
				es anstelle von GRUB verwenden möchten:</p>

				<p>Zuerst müssen Sie LILO emergen:</p>

<pre caption="LILO emergen">
# <i>emerge -k lilo</i>
</pre>

				<p>Nun muss LILO mit der Datei <path>/etc/lilo.conf</path> konfiguriert
				werden.  Folgend eine Beispielkonfiguration:</p>

<pre caption="Beispiel zur lilo.conf">
boot=/dev/hda
map=/boot/map
install=/boot/boot.b
prompt
timeout=50
lba32
default=linux

<comment># Benutzen Sie die folgenden vier Zeilen als Beispiel, wenn Sie Ihren
Kernel selbst kompiliert haben</comment>
image=/boot/bzImage
  label=linux
  read-only
  root=/dev/hda3

<comment># Haben Sie genkernel verwendet, tragen Sie folgendes ein</comment>
image=/boot/kernel-KV
  label=gk_linux
  root=/dev/hda3
  initrd=/boot/initrd-KV
  append="root=/dev/ram0 init=/linuxrc"

<comment># Beispiel für ein weiteres Betriebssystem</comment>
other=/dev/hda1
  label=dos
</pre>

				<p>Ersetzen Sie <c>KV</c> durch die Kernel-Version, die Sie installiert
				haben.  Beachten Sie außerdem, dass <c>default=</c> auf das
				entsprechende Label zeigt (<c>gk_linux</c> bei Verwendung von
				genkernel).</p>

				<ul>
					<li><c>boot=/dev/hda</c> teilt LILO mit, dass es sich auf die erste
					Festplatte am ersten IDE Controller installieren soll.</li>
					<li><c>map=/boot/map</c> gibt die map Datei an.  Üblicherweise braucht
					dieser Eintrag nicht geändert werden.</li>
					<li><c>install=/boot/boot.b</c> veranlassst LILO die angegebene Datei
					als den neuen Boot Sektor zu installieren.  Üblicherweise braucht
					dieser Eintrag nicht geändert werden.  Wenn dieser Eintrag fehlt,
					verwendet LILO eine eigene Datei anstatt von
					<path>/boot/boot.b</path>.</li>
					<li>Wenn <c>prompt</c> angegeben wird, zeigt LILO den klassischen
					<c>lilo:</c> Prompt beim Booten an.  Obwohl es nicht empfehlenswert
					ist, dass Sie den Prompt abschalten, können Sie ihn entfernen und
					weiterhin einen Prompt erhalten, wenn Sie die [Shift]-Taste während
					des Bootens halten.</li>
					<li><c>timeout=50</c> setzt die Zeitspanne, wie lange LILO auf eine
					Eingabe des Benutzers warten soll, bis es von selbst mit dem Eintrag
					default fortfährt.  Der Wert ist in Zehntelsekunden anzugeben, und 50
					der vorgegebene Wert.</li>
					<li><c>lba32</c> beschreibt die Geometrie der Festplatte.  Ein anderer
					hier üblicher Eintrag ist <c>linear</c>.  Sie sollten diesen Eintrag
					nicht ändern, außer Sie wissen wirklich, was Sie tun.  Möglicherweise
					können Sie Ihr System dazu bringen, dass es nicht bootet.</li>
					<li><c>default=linux</c> zeigt auf das automatisch von LILO zu
					bootende Betriebssystem, das in einer Liste weiter unten in der Datei
					angegeben wird.  Der Wert muss zu einem von <c>label=</c> angegebenen
					Wert gleich sein.</li>
					<li><c>image=/boot/bzImage</c> gibt den mit diesem Abschnitt zu
					bootenden Linux Kernel an.</li>
					<li><c>label=linux</c> ist ein Name für das Betriebssystem im
					aktuellen Abschnitt.  In diesem Fall zeigt der <c>default</c> Eintrag
					auch auf diesen Namen.</li>
					<li><c>read-only</c> veranlasst, dass die Root Partition (siehe
					nächste Zeile) zunächst schreibgeschützt gemountet wird und somit
					nicht während des Bootens verändert werden kann.</li>
					<li><c>root=/dev/hda3</c> gibt LILO an, welche Partition als Root
					Partition verwendet werden soll.</li>
				</ul>

				<p>Nachdem Sie Ihre <path>lilo.conf</path> editiert haben, müssen Sie
				LILO dazu bringen, die Informationen in den MBR (Master Boot Record) zu
				laden:</p>

<pre caption="LILO ausführen">
# <i>/sbin/lilo</i>
</pre>

				<p>LILO ist nun konfiguriert und Ihr Rechner ist bereit, um Gentoo Linux
				zu starten!</p>

			</body>
		</section>

		<section>
			<title>Framebuffer verwenden</title>
			<body>

				<p>Möchten Sie Framebuffer verwenden sollten Sie <c>vga=xxx</c> als
				Parameter Ihrem Bootloader übergeben.  <c>xxx</c> ist einer der Werte
				aus der folgenden Tabelle:</p>

				<table>
					<tr><th>Bittiefe</th><th>640x480</th><th>800x600</th><th>1024x768</th><th>1280x1024</th></tr>
					<tr><ti>8 bpp</ti><ti>769</ti><ti>771</ti><ti>773</ti><ti>775</ti></tr>
					<tr><ti>16 bpp</ti><ti>785</ti><ti>788</ti><ti>791</ti><ti>794</ti></tr>
					<tr><ti>32 bpp</ti><ti>786</ti><ti>789</ti><ti>892</ti><ti>795</ti></tr>
				</table>

				<p>LILO Benutzer setzen <c>vga=xxx</c> am Kopf der Konfigurationsdatei
				ein.</p>

				<p>GRUB Benutzer hängen <c>vga=xxx</c> an die Zeile <c>kernel
				(hd0,0)...</c> an.</p>

			</body>
		</section>
	</chapter>


	<chapter>
		<title>Bootdiskette erstellen</title>

		<section>
			<title>GRUB Bootdiskette</title>
			<body>

				<impo>Vergessen Sie nicht eine Diskette in Ihr Laufwerk einzulegen bevor
				Sie fortfahren.</impo>

				<p>Es ist immer eine gute Idee eine Bootdiskette bei der ersten
				Installation einer Linux Distribution zu erstellen.  Dieses ist eine
				Sicherheitsmaßnahme und generell empfehlenswert.  Lässt sich der
				Bootloader nicht in der chroot Umgebung installieren, <e>müssen</e> Sie
				eine Bootdiskette mit GRUB erstellen.  Gehören Sie also zu dieser Gruppe
				von Benutzern, erstellen Sie eine GRUB Bootdiskette und wenn Sie das
				erste Mal Rebooten, installieren Sie GRUB in den MBR.  Erstellen Sie
				Ihre Bootdiskette folgendermaßen:</p>

<pre caption="GRUB Bootdiskette erstellen">
# <i>cd /usr/share/grub/i386-pc/</i>
# <i>cat stage1 stage2 > /dev/fd0</i>
</pre>

				<p>Sie können nun von dieser Diskette rebooten.  Beim <c>grub&gt;</c>
				Prompt der Diskette können Sie die oben beschriebenen benötigten
				<c>root</c> und <c>setup</c> Kommados ausführen.</p>

			</body>
		</section>

		<section>
			<title>LILO Bootdiskette</title>
			<body>

				<impo>Vergessen Sie nicht eine Diskette in Ihr Laufwerk einzulegen bevor
				Sie fortfahren.</impo>

				<p>Wenn Sie LILO verwenden ist es ebenso eine gute Idee eine
				Bootdiskette zu erstellen:</p>

<pre caption="LILO Bootdiskette erstellen">
# <i>dd if=/boot/ihr_kernel of=/dev/fd0</i>
<comment>(Das funktioniert nur, wenn Ihr Kernel kleiner als 1.4MB ist.)</comment>
</pre>

			</body>
		</section>
	</chapter>


	<chapter>
		<title>GRP verwenden</title>
		<section>
			<body>

				<p>GRP Benutzer können an dieser Stelle Binärpakete installieren:</p>

<pre caption="Installieren von GRP">
# <i>USE="bindist" emerge -k xfree</i>
<codenote>(USE="bindist" installiert weitere von XFree86 benötigte GRP Pakete)</codenote>
</pre>

				<p>CD 1 enthält genügend Pakete, um ein funktionierendes System mit
				XFree86 zu installieren.  Zusätzlich enthält CD 2 des 2-CD GRP Set
				weitere Applikationen, wie KDE, GNOME, Mozilla und andere.  Zur
				Installation dieser Pakete müssen Sie erst Ihr neues Gentoo System
				rebooten (weiter unten im Abschnitt "Installation beendet!"
				beschrieben).  Wenn Ihr Gentoo System von der Festplatte aus läuft,
				können Sie die zweite CD mounten und Dateien kopieren:</p>

<pre caption="Binärpakete von CD 2 laden">
# <i>mount /dev/cdrom /mnt/cdrom</i>
# <i>cp -a /mnt/cdrom/packages/* /usr/portage/packages/</i>
</pre>

				<p>Nun können weitere Pakete in ählicher Weise installiert werden.  Zum
				Beispiel:</p>

<pre caption="KDE mittels GRP installieren">
# <i>USE="bindist" emerge -k kde</i>
</pre>

			</body>
		</section>
	</chapter>


	<chapter>
		<title>Installation beendet!</title>

		<section>
			<body>

				<p>Nun, Ihr Gentoo Linux ist installiert.  Der einzige verbleibende
				Schritt ist, einige Konfigurationsdateien anzupassen, die chroot
				Umgebung zu beenden, Ihre Partitionen sicher zu unmounten und das System
				zu rebooten:</p>

				<warn><c>etc-update</c> zeigt Ihnen eine Liste der zu überarbeitenden
				Konfigurationsdateien von denen es eine neuere Version gibt.  Beachten
				Sie, dass keine der Konfigurationsdateien mit essentiellen
				Systemveränderungen darunter sind (wie z.B. <path>/etc/fstab</path>,
				<path>/etc/make.conf</path>, <path>/etc/rc.conf</path>, ...).  Spielen
				Sie die weniger wichigen Dateien ein, löschen Sie die Updates der
				anderen oder schauen Sie sich die Unterschiede an und updaten diese von
				Hand.</warn>

<pre caption="Rebooten des Systems">
# <i>etc-update</i>
# <i>exit</i>
<comment>(Dieses beeendet die chroot Umgebung; Sie können genausogut <c>^D</c> (ctrl-d) drücken)</comment>
# <i>cd /</i>
# <i>umount /mnt/gentoo/boot</i>
# <i>umount /mnt/gentoo/proc</i>
# <i>umount /mnt/gentoo</i>
# <i>reboot</i>
</pre>

				<note>Nach dem Rebooten sollten Sie <c>update-modules</c> ausführen, um
				die Datei <path>/etc/modules.conf</path> zu erstellen.  Anstatt diese
				Datei direkt zu verändern, sollten Sie Änderungen in den Dateien in
				<path>/etc/modules.d/</path> vornehmen.</note>

				<p>Wenn Sie Fragen haben oder sich bei der Entwicklung von Gentoo Linux
				beteiligen wollen, abonnieren Sie die gentoo-user und gentoo-dev
				Mailingliste (weitere Informationen auf der <uri
				link="http://www.gentoo.org/main/en/lists.xml">Mailinglisten
				Webseite</uri>).  Außerdem ist die <uri
				link="http://www.gentoo.org/doc/de/desktop.xml">Anleitung zur Gentoo
				Linux Desktop Konfiguration</uri> recht praktisch für die weitere
				Konfiguration Ihres Gentoo Linux Systems.  Der nützliche <uri
				link="http://www.gentoo.org/doc/de/portage-user.xml">Portage Benutzer
				Leitfaden</uri> hilft Ihnen die Grundlagen von Portage zu verstehen.
				Darüberhinaus gibt es etliche weitere <uri
				link="http://www.gentoo.org/main/en/docs.xml">Gentoo Linux
				Dokumente</uri>.  Haben Sie Fragen, die die Installation betreffen,
				schauen Sie in die <uri link="http://www.gentoo.org/doc/de/faq.xml">FAQ
				(häufig gestellte Fragen)</uri>.  Herzlich Willkommen und haben Sie Spaß
				mit Gentoo Linux!</p>

			</body>
		</section>
	</chapter>


	<chapter>
		<title>Gentoo-Stats</title>

		<section>
			<body>

				<p>Das "Gentoo Linux usage statistics project" wurde gestartet, um den
				Entwicklern einen Überblick der Gentoo-Benutzer zu verschaffen.  Es
				sammelt Informationen über die Verbreitung von Gentoo Linux und hilft,
				Prioritäten in unserer Entwicklung zu setzen.  Die Installation ist
				freiwillig, jedoch freuen wir uns, wenn Sie sich daran beteiligen
				würden.  Generierte Statistiken können Sie sich unter
				<uri>http://stats.gentoo.org/</uri> ansehen.</p>

				<p>Der gentoo-stats Server wird Ihrem System eine eindeutige ID
				vergeben.  Diese ID wird dafür benötigt, dass jedes System nur einmal
				gezählt wird.  Diese ID wird weder dafür verwendet, um ihr System
				individuell zu identifizieren, noch wird sie mit der IP-Adresse oder
				anderen persönlichen Informationen abgeglichen.  Alle Vorkehrungen
				wurden getroffen, um Ihre Privatsphäre in der Entwicklung dieses Systems
				zu wahren.  Nachfolgend sind die Informationen gelistet, die das
				<c>gentoo-stats</c> Programm übermitteln wird:</p>

			<ul>
				<li>installierte Pakete und ihre Versionsnummer</li>
				<li>CPU Informationen: Geschwindigkeit (MHz), Hersteller, Modell, CPU
				Flags (wie "mmx" oder "3dnow")</li>
				<li>Speicher Informationen: installierter physischer RAM, Größe der
				angelegten Swap Partition(en)</li>
				<li>PCI Karten und Netzwerkkarten Chipsätze</li>
				<li>das Gentoo Linux Profil, das Ihr Rechner verwendet (dorthin, wo der
				Link <path>/etc/make.profile</path> zeigt)</li>
			</ul>

				<p>Wir sind uns bewusst, dass eine Bekanntmachung von sensiblen
				Informationen eine Gefährdung für viele Gentoo Linux Benutzer (als auch
				für die Entwickler) darstellt.</p>

				<!-- Ich hoffe die Aussage des Satzes richtig getroffen zu haben: We are
				aware that disclosure of sensitive information is a threat to most
				Gentoo Linux users (just as it is to the developers).-->

				<ul>
					<li>Solange Sie nicht das gentoo-stats Programm ändern, wird niemals
					eine sensible Information, wie Ihr Passwort, Konfigurationsdateien,
					der Lieblingsfilm, ... übertragen</li>
					<li>Die Übermittlung Ihrer E-Mail Adresse ist optional und
					standardmäßig ausgeschaltet.</li>
					<li>Die IP-Adresse von der Ihre Datenübertragung stammt wird niemals
					in der Art gespeichert, dass wir Sie identifizieren können.  Es gibt
					keine Zusammenhänge zwischen der IP-Adresse und der System-ID.</li>
				</ul>

				<p>Die Installation ist einfach -- führen Sie die folgenden Befehle
				aus:</p>

<pre caption="gentoo-stats installieren">
# <i>emerge gentoo-stats</i> <comment>Installiert gentoo-stats</comment>
# <i>gentoo-stats --new</i>  <comment>Bezieht eine neue System-ID</comment>
</pre>

				<p>Der zweite Befehl wird eine neue System-ID anfragen und sie in
				<path>/etc/gentoo-stats/gentoo-stats.conf</path> ablegen.  Sie können
				diese Datei anschauen, um zusätzliche Konfigurationsparameter zu
				sehen.</p>

				<p>Sie sollten das Programm regelmäßig laufen lassen (gentoo-stats
				benötigt keine Superuser Rechte).  Fügen Sie folgende Zeile in Ihre
				<path>crontab</path> (<c>crontab -e</c>)ein:</p>

<pre caption="gentoo-stats mit cron updaten">
0 0 * * 0,4 /usr/sbin/gentoo-stats --update &gt; /dev/null
</pre>

				<p>Das <c>gentoo-stats</c> Programm ist ein einfaches Perl-Script, das
				Sie sich mit Ihrem bevorzugten Textbetrachter oder Editor anschauen
				können: <path>/usr/sbin/gentoo-stats</path>.</p>

			</body>
		</section>
	</chapter>


	<chapter>
		<title>Gentoo auf nicht weit verbreiteter Hardware</title>
		<section>
			<title>Hardware ATA RAID</title>
			<body>

				<p>Benutzer, die Gentoo auf einem Hardware ATA RAID installieren
				möchten, müssen bei den folgenden Schritten aufpassen, um ordnungsgemäß
				Gentoo Linux zu installieren:</p>

				<ul>
					<li>Starten Sie die Live CD mit der <c>doataraid</c> Kerneloption.</li>
					<li>
						Falls Sie vergessen haben <c>doataraid</c> während des Bootens
						auszuwählen oder eines der Module auf mysteriöse Weise nicht lädt,
						laden Sie dieses von Hand:
						<pre caption="Laden des RAID Modul">
# <i>modprobe ataraid</i>
<comment>Für Promise RAID Controller:</comment>
# <i>modprobe pdcraid</i>
<comment>Für Highpoint RAID Controller:</comment>
# <i>modprobe hptraid</i>
						</pre>
					</li>
					<li>Manche ATA RAID Controller verlangen einen Reboot nach der
					Partitionierung; andernfalls wird die Formatierung fehlschlagen.</li>
					<li>
						Mounten Sie den dev-Verzeichnisbaum in die neue Gentoo Umgebung,
						bevor Sie chroot ausführen:
						<pre caption="/dev nach /mnt/gentoo/dev mounten">
# <i>mount -o bind /dev /mnt/gentoo/dev</i>
						</pre>
					</li>
					<li>
						Während der Kernel Konfiguration wählen Sie die benötigten RAID
						Optionen aus:
						<pre caption="RAID während der Linux Kernel Konfiguration">
<comment>Für Highpoint RAID Controller:</comment>
ATA/IDE/HFM/RLL support  ---&gt;
  [*] HPT36X/37X chipset support
  [*] Support for IDE Raid controllers
  [*] Highpoint 370 software RAID
<comment>Für Promise RAID Controllers:</comment>
ATA/IDE/MFM/RLL support  ---&gt;
  [*] PROMISE PDC202{46|62|65|67} support 
  <comment>und/oder</comment>
  [*] PROMISE PDC202{68|69|70|71|75|76|77} support
  [*] Support for IDE Raid controllers
  [*] Support Promise software RAID (Fasttrak(tm))
						</pre>
					</li>
					<li>
						Verwenden Sie GRUB, so fügen Sie <c>--stage2=/boot/grub/stage2</c>
						zum <c>setup</c> Kommando hinzu, während Sie <c>grub</c> ausführen:
						<pre caption="Installation von GRUB auf Hardware RAID Systemen">
grub&gt; <i>root (hd0,0)</i>
grub&gt; <i>setup --stage2=/boot/grub/stage2 (hd0)</i>
grub&gt; <i>quit</i>
						</pre>
						Stellen Sie ebenso in der GRUB Konfiguration sicher, dass
						<c>root</c> auf das entsprechende RAID Device zeigt:
						<pre caption="grub.conf für RAID">
title=Mein Gentoo Linux mit RAID
root (hd0,0)
kernel (hd0,0)/boot/bzImage root=/dev/ataraid/dXpY
						</pre>
					</li>
					<li>
						Benutzer von LILO müssen die <c>root</c> Option auf das
						entsprechende RAID Device zeigen lassen:
						<pre caption="lilo.conf für RAID">
image=/boot/bzImage
label=linux
read-only
root=/dev/ataraid/dXpY
						</pre>
					</li>
				</ul>


				<p>Haben Sie weiterhin Probleme Gentoo Linux mit Ihrem Hardware RAID zum
				Laufen zu bekommen, schreiben Sie diese an <uri>http://bugs.gentoo.org</uri>.</p>

				<p>Danke für die Wahl von Gentoo Linux und haben Sie viel Spaß mit Ihrer
				neuen Installation!</p>

			</body>
		</section>
	</chapter>


</guide>
