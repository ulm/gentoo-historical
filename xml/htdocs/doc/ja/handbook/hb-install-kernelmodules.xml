<?xml version='1.0' encoding="UTF-8"?>
<!-- $Header: /var/cvsroot/gentoo/xml/htdocs/doc/ja/handbook/hb-install-kernelmodules.xml,v 1.1 2008/08/01 22:47:35 shindo Exp $ -->
<!DOCTYPE included SYSTEM "/dtd/guide.dtd">

<included>

<version>1</version>
<date>2008-03-02</date>

<!-- Original revision: 1.1 -->
<!-- Translator: masuda -->

<section id="kernelmodules">
<title>モジュールを設定する</title>
<body>

<p>
自動的にロードさせる必要があるモジュールは、
<path>/etc/modules.autoload.d/kernel-2.6</path>で記述しておく必要があります。
また必要に応じて、モジュールに対し追加オプションを加えることもできます。
</p>

<p>
利用可能なモジュールを全て確認するには、以下の<c>find</c>コマンドを実行します。
なお、コンパイルされたカーネルのバージョンに合わせ、
"&lt;kernel version&gt;"を置き換える必要があります。
</p>

<pre caption="利用可能なモジュールを全て確認">
# <i>find /lib/modules/&lt;kernel version&gt;/ -type f -iname '*.o' -or -iname '*.ko'</i>
</pre>

<p>
たとえば、<c>3c59x.ko</c>モジュールを自動的にロードさせるには、
<path>kernel-2.6</path>ファイルを編集しモジュール名を入力します。
</p>

<pre caption="/etc/modules.autoload.d/kernel-2.6の編集">
# <i>nano -w /etc/modules.autoload.d/kernel-2.6</i>
</pre>

<pre caption="/etc/modules.autoload.d/kernel-2.6">
3c59x
</pre>

<p>
<uri link="?part=1&amp;chap=8">システムの設定</uri>に進み、インストールを続けてください.
</p>

</body>
</section>
</included>
