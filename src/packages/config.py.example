"""Configuration for the packages.gentoo.org package"""

# Database info
DBHOST = 'DBHOST'
DBUSER = 'DBUSER'
DBPASS = 'DBPASS'
DBDB   = 'DBDB'

PORTAGE_DIR = '/usr/portage'
ARCHLIST_EXCLUDE = ['m68k', 'ppc-od', 'x86-obsd', 'x86-fbsd', 'x86-od']

# Directory for package images
PACKAGE_IMAGE_DIR = '/home/marduk/Projects/gentoo-images/jpg'

# What to show if image doesn't exist
PACKAGE_IMAGE_DEFAULT = '/home/marduk/My Icons/g2.png'

# For the RSS Feeds
RSS_EDITOR = 'marduk@gentoo.org'
RSS_PUBLISHER = 'Gentoo Foundation, Inc / www.gentoo.org'

# How many days a package is introduced before it is no longer considered "new"
DAYS_NEW = 5

# Default limit to display for updates and searches
DEFAULT_LIMIT = 25

##############################################################################
##                    DO NOT MODIFY ANYTHING BELOW                          ##
##############################################################################
from time import tzset
from os import environ
# Force UTC timezone
environ['TZ'] = 'UTC'
tzset()
import MySQLdb
db = MySQLdb.connect(host = DBHOST, user = DBUSER, passwd = DBPASS, db = DBDB)
ARCHLIST = [i for i in open('%s/profiles/arch.list' % PORTAGE_DIR).read().split()
    if i not in ARCHLIST_EXCLUDE]
