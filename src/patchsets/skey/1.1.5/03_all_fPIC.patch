--- skey-1.1.5-orig/Makefile.in
+++ skey-1.1.5/Makefile.in
@@ -50,6 +50,9 @@
 
 ${LIBOBJS}: config.h
 
+${LIBOBJS}: %.o: %.c
+	${CC} ${CFLAGS} -fPIC -c $< -o $@
+
 libskey.a: ${LIBOBJS}
 	${AR} rv $@ ${LIBOBJS}
 	${RANLIB} $@
