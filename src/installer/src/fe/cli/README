This is the basic automated installer for GLI.

The 'installer-cli' script is the recommended method of running it.
Two arguments are required. The first is your client-controller configuration,
and the second is your install profile.

Example:
installer-cli client-config.xml install-profile.xml
