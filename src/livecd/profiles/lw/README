



script does :

Add Hotplug to default runlevel ,change /etc/init.d/hotplug to run before local
  -need to blacklist usbcore or will error on module load (built in kernel)

add new user aliases:
alias cp='cp -i'
alias ls='ls --color'
alias mv='mv -i'
alias pico='nano -w'
alias rm='rm -i'

change /etc/conf.d/net for dhcp (for hotplug)

rm /lib/evms/libe2fsim.1.2.1.so (broken un-needed plugin that causes errors) until fixed by evms ppl

cp lw/md /etc/init.d / add md to runlevel default (noraid boot option)

rc-update add serial default

mv modules.conf. > modules.conf so ppp works automagically

cp install.txt to cdroot, change linuxrc to create symlink  

add hwsetup init

