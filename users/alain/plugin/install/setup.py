#!/usr/bin/env python

from distutils.core import setup

setup(name = "plugin",
      version = "1.2",
      description = "Python Plugin Framework",
      py_modules = ["plugin"],
      author = "Alain Penders <alain@rexorient.com>"
      )
